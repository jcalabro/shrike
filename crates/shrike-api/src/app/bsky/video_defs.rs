// Code generated by lexgen. DO NOT EDIT.

/// VideoDefsJobStatus object from app.bsky.video.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct VideoDefsJobStatus {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub blob: Option<crate::Blob>,
    pub did: shrike_syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<String>,
    pub job_id: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub progress: Option<i64>,
    pub state: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl VideoDefsJobStatus {
    pub fn to_cbor(&self) -> Result<Vec<u8>, shrike_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), shrike_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 3u64;
            if self.blob.is_some() {
                count += 1;
            }
            if self.error.is_some() {
                count += 1;
            }
            if self.message.is_some() {
                count += 1;
            }
            if self.progress.is_some() {
                count += 1;
            }
            shrike_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            shrike_cbor::Encoder::new(&mut *buf).encode_text("did")?;
            shrike_cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            if self.blob.is_some() {
                shrike_cbor::Encoder::new(&mut *buf).encode_text("blob")?;
                if let Some(ref val) = self.blob {
                    val.encode_cbor(buf)?;
                }
            }
            if self.error.is_some() {
                shrike_cbor::Encoder::new(&mut *buf).encode_text("error")?;
                if let Some(ref val) = self.error {
                    shrike_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            shrike_cbor::Encoder::new(&mut *buf).encode_text("jobId")?;
            shrike_cbor::Encoder::new(&mut *buf).encode_text(&self.job_id)?;
            shrike_cbor::Encoder::new(&mut *buf).encode_text("state")?;
            shrike_cbor::Encoder::new(&mut *buf).encode_text(&self.state)?;
            if self.message.is_some() {
                shrike_cbor::Encoder::new(&mut *buf).encode_text("message")?;
                if let Some(ref val) = self.message {
                    shrike_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.progress.is_some() {
                shrike_cbor::Encoder::new(&mut *buf).encode_text("progress")?;
                if let Some(ref val) = self.progress {
                    shrike_cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                shrike_cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            if self.blob.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.blob {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("blob", vbuf));
            }
            if self.error.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.error {
                    shrike_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("error", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                shrike_cbor::Encoder::new(&mut vbuf).encode_text(&self.job_id)?;
                pairs.push(("jobId", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                shrike_cbor::Encoder::new(&mut vbuf).encode_text(&self.state)?;
                pairs.push(("state", vbuf));
            }
            if self.message.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.message {
                    shrike_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("message", vbuf));
            }
            if self.progress.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.progress {
                    shrike_cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("progress", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| shrike_cbor::cbor_key_cmp(a.0, b.0));
            shrike_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                shrike_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, shrike_cbor::CborError> {
        let mut decoder = shrike_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(shrike_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut shrike_cbor::Decoder,
    ) -> Result<Self, shrike_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            shrike_cbor::Value::Map(entries) => entries,
            _ => return Err(shrike_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<shrike_syntax::Did> = None;
        let mut field_blob: Option<crate::Blob> = None;
        let mut field_error: Option<String> = None;
        let mut field_job_id: Option<String> = None;
        let mut field_state: Option<String> = None;
        let mut field_message: Option<String> = None;
        let mut field_progress: Option<i64> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let shrike_cbor::Value::Text(s) = value {
                        field_did =
                            Some(shrike_syntax::Did::try_from(s).map_err(|e| {
                                shrike_cbor::CborError::InvalidCbor(e.to_string())
                            })?);
                    } else {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "blob" => {
                    let raw = shrike_cbor::encode_value(&value)?;
                    let mut dec = shrike_cbor::Decoder::new(&raw);
                    field_blob = Some(crate::Blob::decode_cbor(&mut dec)?);
                }
                "error" => {
                    if let shrike_cbor::Value::Text(s) = value {
                        field_error = Some(s.to_string());
                    } else {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "jobId" => {
                    if let shrike_cbor::Value::Text(s) = value {
                        field_job_id = Some(s.to_string());
                    } else {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "state" => {
                    if let shrike_cbor::Value::Text(s) = value {
                        field_state = Some(s.to_string());
                    } else {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "message" => {
                    if let shrike_cbor::Value::Text(s) = value {
                        field_message = Some(s.to_string());
                    } else {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "progress" => match value {
                    shrike_cbor::Value::Unsigned(n) => {
                        field_progress = Some(n as i64);
                    }
                    shrike_cbor::Value::Signed(n) => {
                        field_progress = Some(n);
                    }
                    _ => {
                        return Err(shrike_cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                _ => {
                    let raw = shrike_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(VideoDefsJobStatus {
            did: field_did.ok_or_else(|| {
                shrike_cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            blob: field_blob,
            error: field_error,
            job_id: field_job_id.ok_or_else(|| {
                shrike_cbor::CborError::InvalidCbor("missing required field 'jobId'".into())
            })?,
            state: field_state.ok_or_else(|| {
                shrike_cbor::CborError::InvalidCbor("missing required field 'state'".into())
            })?,
            message: field_message,
            progress: field_progress,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}
