// Code generated by lexgen. DO NOT EDIT.

/// TeamDefsMember object from tools.ozone.team.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TeamDefsMember {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<ratproto_syntax::Datetime>,
    pub did: ratproto_syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disabled: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_updated_by: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub profile: Option<crate::app::bsky::ActorDefsProfileViewDetailed>,
    pub role: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<ratproto_syntax::Datetime>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl TeamDefsMember {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.profile.is_some() {
                count += 1;
            }
            if self.disabled.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.updated_at.is_some() {
                count += 1;
            }
            if self.last_updated_by.is_some() {
                count += 1;
            }
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("did")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("role")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text(&self.role)?;
            if self.profile.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("profile")?;
                if let Some(ref val) = self.profile {
                    val.encode_cbor(buf)?;
                }
            }
            if self.disabled.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("disabled")?;
                if let Some(ref val) = self.disabled {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.created_at.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.updated_at.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("updatedAt")?;
                if let Some(ref val) = self.updated_at {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.last_updated_by.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("lastUpdatedBy")?;
                if let Some(ref val) = self.last_updated_by {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_text(&self.role)?;
                pairs.push(("role", vbuf));
            }
            if self.profile.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.profile {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("profile", vbuf));
            }
            if self.disabled.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.disabled {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("disabled", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.updated_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.updated_at {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("updatedAt", vbuf));
            }
            if self.last_updated_by.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.last_updated_by {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("lastUpdatedBy", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| ratproto_cbor::cbor_key_cmp(a.0, b.0));
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => return Err(ratproto_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<ratproto_syntax::Did> = None;
        let mut field_role: Option<String> = None;
        let mut field_profile: Option<crate::app::bsky::ActorDefsProfileViewDetailed> = None;
        let mut field_disabled: Option<bool> = None;
        let mut field_created_at: Option<ratproto_syntax::Datetime> = None;
        let mut field_updated_at: Option<ratproto_syntax::Datetime> = None;
        let mut field_last_updated_by: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_did =
                            Some(ratproto_syntax::Did::try_from(s).map_err(|e| {
                                ratproto_cbor::CborError::InvalidCbor(e.to_string())
                            })?);
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "role" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_role = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "profile" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_profile = Some(
                        crate::app::bsky::ActorDefsProfileViewDetailed::decode_cbor(&mut dec)?,
                    );
                }
                "disabled" => {
                    if let ratproto_cbor::Value::Bool(b) = value {
                        field_disabled = Some(b);
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected bool".into(),
                        ));
                    }
                }
                "createdAt" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_created_at =
                            Some(ratproto_syntax::Datetime::try_from(s).map_err(|e| {
                                ratproto_cbor::CborError::InvalidCbor(e.to_string())
                            })?);
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "updatedAt" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_updated_at =
                            Some(ratproto_syntax::Datetime::try_from(s).map_err(|e| {
                                ratproto_cbor::CborError::InvalidCbor(e.to_string())
                            })?);
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "lastUpdatedBy" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_last_updated_by = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                _ => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(TeamDefsMember {
            did: field_did.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            role: field_role.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor("missing required field 'role'".into())
            })?,
            profile: field_profile,
            disabled: field_disabled,
            created_at: field_created_at,
            updated_at: field_updated_at,
            last_updated_by: field_last_updated_by,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// Token constant.
pub const TEAM_DEFS_ROLE_ADMIN: &str = "tools.ozone.team.defs#roleAdmin";

/// Token constant.
pub const TEAM_DEFS_ROLE_MODERATOR: &str = "tools.ozone.team.defs#roleModerator";

/// Token constant.
pub const TEAM_DEFS_ROLE_TRIAGE: &str = "tools.ozone.team.defs#roleTriage";

/// Token constant.
pub const TEAM_DEFS_ROLE_VERIFIER: &str = "tools.ozone.team.defs#roleVerifier";
