// Code generated by lexgen. DO NOT EDIT.

#[derive(Debug, Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ModerationGetAccountTimelineParams {
    pub did: String,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ModerationGetAccountTimelineOutput {
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub timeline: Vec<ModerationGetAccountTimelineTimelineItem>,
    /// Extra fields not defined in the schema.
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
}

/// ModerationGetAccountTimeline — Get timeline of all available events of an account. This includes moderation events, account hist...
pub async fn moderation_get_account_timeline(
    client: &ratproto_xrpc::Client,
    params: &ModerationGetAccountTimelineParams,
) -> Result<ModerationGetAccountTimelineOutput, ratproto_xrpc::Error> {
    client
        .query("tools.ozone.moderation.getAccountTimeline", params)
        .await
}

/// ModerationGetAccountTimelineTimelineItem object from tools.ozone.moderation.getAccountTimeline.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ModerationGetAccountTimelineTimelineItem {
    pub day: String,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub summary: Vec<ModerationGetAccountTimelineTimelineItemSummary>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ModerationGetAccountTimelineTimelineItem {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 2u64;
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("day")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text(&self.day)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("summary")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_array_header(self.summary.len() as u64)?;
            for item in &self.summary {
                item.encode_cbor(buf)?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_text(&self.day)?;
                pairs.push(("day", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.summary.len() as u64)?;
                for item in &self.summary {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("summary", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| ratproto_cbor::cbor_key_cmp(a.0, b.0));
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => return Err(ratproto_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_day: Option<String> = None;
        let mut field_summary: Vec<ModerationGetAccountTimelineTimelineItemSummary> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "day" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_day = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "summary" => {
                    if let ratproto_cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = ratproto_cbor::encode_value(&item)?;
                            let mut dec = ratproto_cbor::Decoder::new(&raw);
                            field_summary.push(
                                ModerationGetAccountTimelineTimelineItemSummary::decode_cbor(
                                    &mut dec,
                                )?,
                            );
                        }
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected array".into(),
                        ));
                    }
                }
                _ => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ModerationGetAccountTimelineTimelineItem {
            day: field_day.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor("missing required field 'day'".into())
            })?,
            summary: field_summary,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// ModerationGetAccountTimelineTimelineItemSummary object from tools.ozone.moderation.getAccountTimeline.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ModerationGetAccountTimelineTimelineItemSummary {
    pub count: i64,
    pub event_subject_type: String,
    pub event_type: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ModerationGetAccountTimelineTimelineItemSummary {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 3u64;
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("count")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_i64(self.count)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("eventType")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text(&self.event_type)?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("eventSubjectType")?;
            ratproto_cbor::Encoder::new(&mut *buf).encode_text(&self.event_subject_type)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_i64(self.count)?;
                pairs.push(("count", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_text(&self.event_type)?;
                pairs.push(("eventType", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                ratproto_cbor::Encoder::new(&mut vbuf).encode_text(&self.event_subject_type)?;
                pairs.push(("eventSubjectType", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| ratproto_cbor::cbor_key_cmp(a.0, b.0));
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => return Err(ratproto_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_count: Option<i64> = None;
        let mut field_event_type: Option<String> = None;
        let mut field_event_subject_type: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "count" => match value {
                    ratproto_cbor::Value::Unsigned(n) => {
                        field_count = Some(n as i64);
                    }
                    ratproto_cbor::Value::Signed(n) => {
                        field_count = Some(n);
                    }
                    _ => {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "eventType" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_event_type = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "eventSubjectType" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_event_subject_type = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                _ => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ModerationGetAccountTimelineTimelineItemSummary {
            count: field_count.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor("missing required field 'count'".into())
            })?,
            event_type: field_event_type.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor("missing required field 'eventType'".into())
            })?,
            event_subject_type: field_event_subject_type.ok_or_else(|| {
                ratproto_cbor::CborError::InvalidCbor(
                    "missing required field 'eventSubjectType'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}
