// Code generated by lexgen. DO NOT EDIT.

#[derive(Debug, Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct GraphGetStarterPacksWithMembershipParams {
    pub actor: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct GraphGetStarterPacksWithMembershipOutput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub starter_packs_with_membership:
        Vec<GraphGetStarterPacksWithMembershipStarterPackWithMembership>,
    /// Extra fields not defined in the schema.
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
}

/// GraphGetStarterPacksWithMembership — Enumerates the starter packs created by the session user, and includes membership information abo...
pub async fn graph_get_starter_packs_with_membership(
    client: &ratproto_xrpc::Client,
    params: &GraphGetStarterPacksWithMembershipParams,
) -> Result<GraphGetStarterPacksWithMembershipOutput, ratproto_xrpc::Error> {
    client
        .query("app.bsky.graph.getStarterPacksWithMembership", params)
        .await
}

/// GraphGetStarterPacksWithMembershipStarterPackWithMembership — A starter pack and an optional list item indicating membership of a target user to that starter p...
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct GraphGetStarterPacksWithMembershipStarterPackWithMembership {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub list_item: Option<crate::app::bsky::GraphDefsListItemView>,
    pub starter_pack: crate::app::bsky::GraphDefsStarterPackView,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl GraphGetStarterPacksWithMembershipStarterPackWithMembership {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.list_item.is_some() {
                count += 1;
            }
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.list_item.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("listItem")?;
                if let Some(ref val) = self.list_item {
                    val.encode_cbor(buf)?;
                }
            }
            ratproto_cbor::Encoder::new(&mut *buf).encode_text("starterPack")?;
            self.starter_pack.encode_cbor(buf)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.list_item.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.list_item {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("listItem", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                self.starter_pack.encode_cbor(&mut vbuf)?;
                pairs.push(("starterPack", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| ratproto_cbor::cbor_key_cmp(a.0, b.0));
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => return Err(ratproto_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_list_item: Option<crate::app::bsky::GraphDefsListItemView> = None;
        let mut field_starter_pack: Option<crate::app::bsky::GraphDefsStarterPackView> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "listItem" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_list_item = Some(crate::app::bsky::GraphDefsListItemView::decode_cbor(
                        &mut dec,
                    )?);
                }
                "starterPack" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_starter_pack = Some(
                        crate::app::bsky::GraphDefsStarterPackView::decode_cbor(&mut dec)?,
                    );
                }
                _ => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(
            GraphGetStarterPacksWithMembershipStarterPackWithMembership {
                list_item: field_list_item,
                starter_pack: field_starter_pack.ok_or_else(|| {
                    ratproto_cbor::CborError::InvalidCbor(
                        "missing required field 'starterPack'".into(),
                    )
                })?,
                extra: std::collections::HashMap::new(),
                extra_cbor,
            },
        )
    }
}
