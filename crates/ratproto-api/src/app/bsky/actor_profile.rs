// Code generated by lexgen. DO NOT EDIT.

/// NSID for the ActorProfile record.
pub const NSID_ACTOR_PROFILE: &str = "app.bsky.actor.profile";

/// ActorProfile record from app.bsky.actor.profile.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorProfile {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avatar: Option<crate::Blob>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub banner: Option<crate::Blob>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<ratproto_syntax::Datetime>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub joined_via_starter_pack: Option<crate::com::atproto::RepoStrongRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<ActorProfileLabelsUnion>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_post: Option<crate::com::atproto::RepoStrongRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pronouns: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub website: Option<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

/// ActorProfileLabelsUnion is a union type.
#[derive(Debug, Clone)]
pub enum ActorProfileLabelsUnion {
    LabelDefsSelfLabels(Box<crate::com::atproto::LabelDefsSelfLabels>),
    Unknown(crate::UnknownUnionVariant),
}

impl serde::Serialize for ActorProfileLabelsUnion {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorProfileLabelsUnion::LabelDefsSelfLabels(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("com.atproto.label.defs#selfLabels".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorProfileLabelsUnion::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom(
                        "no JSON data for unknown union variant",
                    ))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de> for ActorProfileLabelsUnion {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "com.atproto.label.defs#selfLabels" => {
                let inner: crate::com::atproto::LabelDefsSelfLabels =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorProfileLabelsUnion::LabelDefsSelfLabels(Box::new(
                    inner,
                )))
            }
            _ => Ok(ActorProfileLabelsUnion::Unknown(
                crate::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: Some(value),
                    cbor: None,
                },
            )),
        }
    }
}

impl ActorProfileLabelsUnion {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        match self {
            ActorProfileLabelsUnion::LabelDefsSelfLabels(inner) => inner.encode_cbor(buf),
            ActorProfileLabelsUnion::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(ratproto_cbor::CborError::InvalidCbor(
                        "no CBOR data for unknown union variant".into(),
                    ))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => {
                return Err(ratproto_cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                ratproto_cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "com.atproto.label.defs#selfLabels" => {
                let mut dec = ratproto_cbor::Decoder::new(raw);
                let inner = crate::com::atproto::LabelDefsSelfLabels::decode_cbor(&mut dec)?;
                Ok(ActorProfileLabelsUnion::LabelDefsSelfLabels(Box::new(
                    inner,
                )))
            }
            _ => Ok(ActorProfileLabelsUnion::Unknown(
                crate::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: None,
                    cbor: Some(raw.to_vec()),
                },
            )),
        }
    }
}

impl ActorProfile {
    pub fn to_cbor(&self) -> Result<Vec<u8>, ratproto_cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), ratproto_cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.avatar.is_some() {
                count += 1;
            }
            if self.banner.is_some() {
                count += 1;
            }
            if self.labels.is_some() {
                count += 1;
            }
            if self.website.is_some() {
                count += 1;
            }
            if self.pronouns.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.pinned_post.is_some() {
                count += 1;
            }
            if self.description.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            if self.joined_via_starter_pack.is_some() {
                count += 1;
            }
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.avatar.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("avatar")?;
                if let Some(ref val) = self.avatar {
                    val.encode_cbor(buf)?;
                }
            }
            if self.banner.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("banner")?;
                if let Some(ref val) = self.banner {
                    val.encode_cbor(buf)?;
                }
            }
            if self.labels.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                if let Some(ref val) = self.labels {
                    val.encode_cbor(buf)?;
                }
            }
            if self.website.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("website")?;
                if let Some(ref val) = self.website {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.pronouns.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("pronouns")?;
                if let Some(ref val) = self.pronouns {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.created_at.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.pinned_post.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("pinnedPost")?;
                if let Some(ref val) = self.pinned_post {
                    val.encode_cbor(buf)?;
                }
            }
            if self.description.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.display_name.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    ratproto_cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.joined_via_starter_pack.is_some() {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text("joinedViaStarterPack")?;
                if let Some(ref val) = self.joined_via_starter_pack {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.avatar.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.avatar {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("avatar", vbuf));
            }
            if self.banner.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.banner {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("banner", vbuf));
            }
            if self.labels.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.labels {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            if self.website.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.website {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("website", vbuf));
            }
            if self.pronouns.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pronouns {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("pronouns", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.pinned_post.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pinned_post {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("pinnedPost", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    ratproto_cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            if self.joined_via_starter_pack.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.joined_via_starter_pack {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("joinedViaStarterPack", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| ratproto_cbor::cbor_key_cmp(a.0, b.0));
            ratproto_cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                ratproto_cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, ratproto_cbor::CborError> {
        let mut decoder = ratproto_cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(ratproto_cbor::CborError::InvalidCbor(
                "trailing data".into(),
            ));
        }
        Ok(result)
    }

    pub fn decode_cbor(
        decoder: &mut ratproto_cbor::Decoder,
    ) -> Result<Self, ratproto_cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            ratproto_cbor::Value::Map(entries) => entries,
            _ => return Err(ratproto_cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_avatar: Option<crate::Blob> = None;
        let mut field_banner: Option<crate::Blob> = None;
        let mut field_labels: Option<ActorProfileLabelsUnion> = None;
        let mut field_website: Option<String> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<ratproto_syntax::Datetime> = None;
        let mut field_pinned_post: Option<crate::com::atproto::RepoStrongRef> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_joined_via_starter_pack: Option<crate::com::atproto::RepoStrongRef> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "avatar" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_avatar = Some(crate::Blob::decode_cbor(&mut dec)?);
                }
                "banner" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_banner = Some(crate::Blob::decode_cbor(&mut dec)?);
                }
                "labels" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_labels = Some(ActorProfileLabelsUnion::decode_cbor(&mut dec)?);
                }
                "website" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_website = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "pronouns" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "createdAt" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_created_at =
                            Some(ratproto_syntax::Datetime::try_from(s).map_err(|e| {
                                ratproto_cbor::CborError::InvalidCbor(e.to_string())
                            })?);
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "pinnedPost" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_pinned_post =
                        Some(crate::com::atproto::RepoStrongRef::decode_cbor(&mut dec)?);
                }
                "description" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "displayName" => {
                    if let ratproto_cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(ratproto_cbor::CborError::InvalidCbor(
                            "expected text".into(),
                        ));
                    }
                }
                "joinedViaStarterPack" => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    let mut dec = ratproto_cbor::Decoder::new(&raw);
                    field_joined_via_starter_pack =
                        Some(crate::com::atproto::RepoStrongRef::decode_cbor(&mut dec)?);
                }
                _ => {
                    let raw = ratproto_cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorProfile {
            avatar: field_avatar,
            banner: field_banner,
            labels: field_labels,
            website: field_website,
            pronouns: field_pronouns,
            created_at: field_created_at,
            pinned_post: field_pinned_post,
            description: field_description,
            display_name: field_display_name,
            joined_via_starter_pack: field_joined_via_starter_pack,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}
