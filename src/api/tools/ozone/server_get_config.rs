// Code generated by lexgen. DO NOT EDIT.

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ServerGetConfigOutput {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub appview: Option<ServerGetConfigServiceConfig>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub blob_divert: Option<ServerGetConfigServiceConfig>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub chat: Option<ServerGetConfigServiceConfig>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pds: Option<ServerGetConfigServiceConfig>,
    /// The did of the verifier used for verification.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub verifier_did: Option<crate::syntax::Did>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viewer: Option<ServerGetConfigViewerConfig>,
    /// Extra fields not defined in the schema.
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
}

/// ServerGetConfig — Get details about ozone's server configuration.
pub async fn server_get_config(
    client: &crate::xrpc::Client,
) -> Result<ServerGetConfigOutput, crate::xrpc::Error> {
    client.query("tools.ozone.server.getConfig", &()).await
}

/// ServerGetConfigServiceConfig object from tools.ozone.server.getConfig.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ServerGetConfigServiceConfig {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ServerGetConfigServiceConfig {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.url.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.url.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("url")?;
                if let Some(ref val) = self.url {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.url.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.url {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("url", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_url: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "url" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_url = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ServerGetConfigServiceConfig {
            url: field_url,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_url: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "url" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_url = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ServerGetConfigServiceConfig {
            url: field_url,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ServerGetConfigServiceConfigCborView<'a> {
    pub url: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ServerGetConfigServiceConfigCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ServerGetConfigServiceConfig, crate::cbor::CborError> {
        Ok(ServerGetConfigServiceConfig {
            url: self.url.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_url: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x75\x72\x6c", |decoder| {
            field_url = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"url" => {
                    field_url = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            url: field_url,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ServerGetConfigViewerConfig object from tools.ozone.server.getConfig.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ServerGetConfigViewerConfig {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role: Option<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ServerGetConfigViewerConfig {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.role.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.role.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("role")?;
                if let Some(ref val) = self.role {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.role.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.role {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("role", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_role: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "role" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_role = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ServerGetConfigViewerConfig {
            role: field_role,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_role: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "role" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_role = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ServerGetConfigViewerConfig {
            role: field_role,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ServerGetConfigViewerConfigCborView<'a> {
    pub role: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ServerGetConfigViewerConfigCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ServerGetConfigViewerConfig, crate::cbor::CborError> {
        Ok(ServerGetConfigViewerConfig {
            role: self.role.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_role: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x72\x6f\x6c\x65", |decoder| {
            field_role = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"role" => {
                    field_role = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            role: field_role,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}
