// Code generated by lexgen. DO NOT EDIT.

/// ActorDefsDirectConvoMember — [NOTE: This is under active development and should be considered unstable while this note is here].
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsDirectConvoMember {
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsDirectConvoMember {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 0u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsDirectConvoMember {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// ActorDefsGroupConvoMember — [NOTE: This is under active development and should be considered unstable while this note is here]. A current group convo member.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsGroupConvoMember {
    /// Who added this member. Only present if the member was added (instead of joining via link).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub added_by: Option<ActorDefsProfileViewBasic>,
    /// The member's role within this conversation. Only present in group conversation member lists.
    pub role: ActorDefsMemberRole,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsGroupConvoMember {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.added_by.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("role")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.role)?;
            if self.added_by.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("addedBy")?;
                if let Some(ref val) = self.added_by {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.role)?;
                pairs.push(("role", vbuf));
            }
            if self.added_by.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.added_by {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("addedBy", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_role: Option<ActorDefsMemberRole> = None;
        let mut field_added_by: Option<ActorDefsProfileViewBasic> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "role" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_role = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "addedBy" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_added_by = Some(ActorDefsProfileViewBasic::decode_cbor(&mut dec)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsGroupConvoMember {
            role: field_role.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'role'".into())
            })?,
            added_by: field_added_by,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// ActorDefsMemberRole is a string type from chat.bsky.actor.defs.
pub type ActorDefsMemberRole = String;

/// ActorDefsPastGroupConvoMember — [NOTE: This is under active development and should be considered unstable while this note is here]. A past group convo member.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsPastGroupConvoMember {
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsPastGroupConvoMember {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 0u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsPastGroupConvoMember {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// ActorDefsProfileViewBasic object from chat.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileViewBasic {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub associated: Option<crate::api::app::bsky::ActorDefsProfileAssociated>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avatar: Option<String>,
    /// Set to true when the actor cannot actively participate in conversations
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub chat_disabled: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<crate::syntax::Datetime>,
    pub did: crate::syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    pub handle: crate::syntax::Handle,
    /// Union field that has data specific to different kinds of convos.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<ActorDefsProfileViewBasicKindUnion>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub verification: Option<crate::api::app::bsky::ActorDefsVerificationState>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viewer: Option<crate::api::app::bsky::ActorDefsViewerState>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

/// Union field that has data specific to different kinds of convos.
#[derive(Debug, Clone)]
pub enum ActorDefsProfileViewBasicKindUnion {
    ActorDefsDirectConvoMember(Box<ActorDefsDirectConvoMember>),
    ActorDefsGroupConvoMember(Box<ActorDefsGroupConvoMember>),
    ActorDefsPastGroupConvoMember(Box<ActorDefsPastGroupConvoMember>),
    Unknown(crate::api::UnknownUnionVariant),
}

impl serde::Serialize for ActorDefsProfileViewBasicKindUnion {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorDefsProfileViewBasicKindUnion::ActorDefsDirectConvoMember(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "chat.bsky.actor.defs#directConvoMember".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsProfileViewBasicKindUnion::ActorDefsGroupConvoMember(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "chat.bsky.actor.defs#groupConvoMember".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsProfileViewBasicKindUnion::ActorDefsPastGroupConvoMember(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "chat.bsky.actor.defs#pastGroupConvoMember".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsProfileViewBasicKindUnion::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom(
                        "no JSON data for unknown union variant",
                    ))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de> for ActorDefsProfileViewBasicKindUnion {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "chat.bsky.actor.defs#directConvoMember" => {
                let inner: ActorDefsDirectConvoMember =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsProfileViewBasicKindUnion::ActorDefsDirectConvoMember(Box::new(inner)))
            }
            "chat.bsky.actor.defs#groupConvoMember" => {
                let inner: ActorDefsGroupConvoMember =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsProfileViewBasicKindUnion::ActorDefsGroupConvoMember(Box::new(inner)))
            }
            "chat.bsky.actor.defs#pastGroupConvoMember" => {
                let inner: ActorDefsPastGroupConvoMember =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(
                    ActorDefsProfileViewBasicKindUnion::ActorDefsPastGroupConvoMember(Box::new(
                        inner,
                    )),
                )
            }
            _ => Ok(ActorDefsProfileViewBasicKindUnion::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: Some(value),
                    cbor: None,
                },
            )),
        }
    }
}

impl ActorDefsProfileViewBasicKindUnion {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        match self {
            ActorDefsProfileViewBasicKindUnion::ActorDefsDirectConvoMember(inner) => {
                inner.encode_cbor(buf)
            }
            ActorDefsProfileViewBasicKindUnion::ActorDefsGroupConvoMember(inner) => {
                inner.encode_cbor(buf)
            }
            ActorDefsProfileViewBasicKindUnion::ActorDefsPastGroupConvoMember(inner) => {
                inner.encode_cbor(buf)
            }
            ActorDefsProfileViewBasicKindUnion::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(crate::cbor::CborError::InvalidCbor(
                        "no CBOR data for unknown union variant".into(),
                    ))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => {
                return Err(crate::cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                crate::cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "chat.bsky.actor.defs#directConvoMember" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsDirectConvoMember::decode_cbor(&mut dec)?;
                Ok(ActorDefsProfileViewBasicKindUnion::ActorDefsDirectConvoMember(Box::new(inner)))
            }
            "chat.bsky.actor.defs#groupConvoMember" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsGroupConvoMember::decode_cbor(&mut dec)?;
                Ok(ActorDefsProfileViewBasicKindUnion::ActorDefsGroupConvoMember(Box::new(inner)))
            }
            "chat.bsky.actor.defs#pastGroupConvoMember" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsPastGroupConvoMember::decode_cbor(&mut dec)?;
                Ok(
                    ActorDefsProfileViewBasicKindUnion::ActorDefsPastGroupConvoMember(Box::new(
                        inner,
                    )),
                )
            }
            _ => Ok(ActorDefsProfileViewBasicKindUnion::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: None,
                    cbor: Some(raw.to_vec()),
                },
            )),
        }
    }
}

impl ActorDefsProfileViewBasic {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.kind.is_some() {
                count += 1;
            }
            if self.avatar.is_some() {
                count += 1;
            }
            if !self.labels.is_empty() {
                count += 1;
            }
            if self.viewer.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.associated.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            if self.chat_disabled.is_some() {
                count += 1;
            }
            if self.verification.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            if self.kind.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("kind")?;
                if let Some(ref val) = self.kind {
                    val.encode_cbor(buf)?;
                }
            }
            if self.avatar.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("avatar")?;
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("handle")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.handle.as_str())?;
            if !self.labels.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(buf)?;
                }
            }
            if self.viewer.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("viewer")?;
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(buf)?;
                }
            }
            if self.created_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.associated.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("associated")?;
                if let Some(ref val) = self.associated {
                    val.encode_cbor(buf)?;
                }
            }
            if self.display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.chat_disabled.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("chatDisabled")?;
                if let Some(ref val) = self.chat_disabled {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.verification.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("verification")?;
                if let Some(ref val) = self.verification {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            if self.kind.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.kind {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("kind", vbuf));
            }
            if self.avatar.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("avatar", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.handle.as_str())?;
                pairs.push(("handle", vbuf));
            }
            if !self.labels.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            if self.viewer.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("viewer", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.associated.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.associated {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("associated", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            if self.chat_disabled.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.chat_disabled {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("chatDisabled", vbuf));
            }
            if self.verification.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.verification {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("verification", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let val = decoder.decode()?;
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_kind: Option<ActorDefsProfileViewBasicKindUnion> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_viewer: Option<crate::api::app::bsky::ActorDefsViewerState> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<crate::api::app::bsky::ActorDefsProfileAssociated> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_chat_disabled: Option<bool> = None;
        let mut field_verification: Option<crate::api::app::bsky::ActorDefsVerificationState> =
            None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "kind" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_kind = Some(ActorDefsProfileViewBasicKindUnion::decode_cbor(&mut dec)?);
                }
                "avatar" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::decode_cbor(&mut dec)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "viewer" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_viewer = Some(crate::api::app::bsky::ActorDefsViewerState::decode_cbor(
                        &mut dec,
                    )?);
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_associated = Some(
                        crate::api::app::bsky::ActorDefsProfileAssociated::decode_cbor(&mut dec)?,
                    );
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "chatDisabled" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_chat_disabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "verification" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_verification = Some(
                        crate::api::app::bsky::ActorDefsVerificationState::decode_cbor(&mut dec)?,
                    );
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileViewBasic {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            kind: field_kind,
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            viewer: field_viewer,
            created_at: field_created_at,
            associated: field_associated,
            display_name: field_display_name,
            chat_disabled: field_chat_disabled,
            verification: field_verification,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}
