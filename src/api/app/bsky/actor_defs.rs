// Code generated by lexgen. DO NOT EDIT.

/// ActorDefsAdultContentPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsAdultContentPref {
    pub enabled: bool,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsAdultContentPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("enabled")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.enabled)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.enabled)?;
                pairs.push(("enabled", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_enabled: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "enabled" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_enabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsAdultContentPref {
            enabled: field_enabled.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'enabled'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_enabled: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "enabled" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_enabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsAdultContentPref {
            enabled: field_enabled.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'enabled'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsAdultContentPrefCborView<'a> {
    pub enabled: bool,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsAdultContentPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsAdultContentPref, crate::cbor::CborError> {
        Ok(ActorDefsAdultContentPref {
            enabled: self.enabled,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_enabled: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x67\x65\x6e\x61\x62\x6c\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_enabled = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"enabled" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_enabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            enabled: field_enabled.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'enabled'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsBskyAppProgressGuide — If set, an active progress guide. Once completed, can be set to undefined. Should have unspecced fields tracking progress.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsBskyAppProgressGuide {
    pub guide: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsBskyAppProgressGuide {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("guide")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.guide)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.guide)?;
                pairs.push(("guide", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_guide: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "guide" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_guide = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsBskyAppProgressGuide {
            guide: field_guide.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'guide'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_guide: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "guide" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_guide = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsBskyAppProgressGuide {
            guide: field_guide.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'guide'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsBskyAppProgressGuideCborView<'a> {
    pub guide: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsBskyAppProgressGuideCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsBskyAppProgressGuide, crate::cbor::CborError> {
        Ok(ActorDefsBskyAppProgressGuide {
            guide: self.guide.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_guide: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x67\x75\x69\x64\x65", |decoder| {
            field_guide = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"guide" => {
                    field_guide = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            guide: field_guide.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'guide'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsBskyAppStatePref — A grab bag of state that's specific to the bsky.app program. Third-party apps shouldn't use this.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsBskyAppStatePref {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub active_progress_guide: Option<ActorDefsBskyAppProgressGuide>,
    /// Indicates if the user is participating in the beta features program.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_beta_user: Option<bool>,
    /// Storage for NUXs the user has encountered.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub nuxs: Vec<ActorDefsNux>,
    /// An array of tokens which identify nudges (modals, popups, tours, highlight dots) that should be shown to the user.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub queued_nudges: Vec<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsBskyAppStatePref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if !self.nuxs.is_empty() {
                count += 1;
            }
            if self.is_beta_user.is_some() {
                count += 1;
            }
            if !self.queued_nudges.is_empty() {
                count += 1;
            }
            if self.active_progress_guide.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if !self.nuxs.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("nuxs")?;
                crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.nuxs.len() as u64)?;
                for item in &self.nuxs {
                    item.encode_cbor(buf)?;
                }
            }
            if self.is_beta_user.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isBetaUser")?;
                if let Some(ref val) = self.is_beta_user {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if !self.queued_nudges.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("queuedNudges")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.queued_nudges.len() as u64)?;
                for item in &self.queued_nudges {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(item)?;
                }
            }
            if self.active_progress_guide.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("activeProgressGuide")?;
                if let Some(ref val) = self.active_progress_guide {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if !self.nuxs.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.nuxs.len() as u64)?;
                for item in &self.nuxs {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("nuxs", vbuf));
            }
            if self.is_beta_user.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_beta_user {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isBetaUser", vbuf));
            }
            if !self.queued_nudges.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.queued_nudges.len() as u64)?;
                for item in &self.queued_nudges {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item)?;
                }
                pairs.push(("queuedNudges", vbuf));
            }
            if self.active_progress_guide.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.active_progress_guide {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("activeProgressGuide", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_nuxs: Vec<ActorDefsNux> = Vec::new();
        let mut field_is_beta_user: Option<bool> = None;
        let mut field_queued_nudges: Vec<String> = Vec::new();
        let mut field_active_progress_guide: Option<ActorDefsBskyAppProgressGuide> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "nuxs" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_nuxs.push(ActorDefsNux::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "isBetaUser" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_beta_user = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "queuedNudges" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_queued_nudges.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "activeProgressGuide" => {
                    field_active_progress_guide =
                        Some(ActorDefsBskyAppProgressGuide::decode_cbor(decoder)?);
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsBskyAppStatePref {
            nuxs: field_nuxs,
            is_beta_user: field_is_beta_user,
            queued_nudges: field_queued_nudges,
            active_progress_guide: field_active_progress_guide,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_nuxs: Vec<ActorDefsNux> = Vec::new();
        let mut field_is_beta_user: Option<bool> = None;
        let mut field_queued_nudges: Vec<String> = Vec::new();
        let mut field_active_progress_guide: Option<ActorDefsBskyAppProgressGuide> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "nuxs" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_nuxs.push(ActorDefsNux::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "isBetaUser" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_beta_user = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "queuedNudges" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_queued_nudges.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "activeProgressGuide" => {
                    field_active_progress_guide =
                        Some(ActorDefsBskyAppProgressGuide::from_cbor_value(value)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsBskyAppStatePref {
            nuxs: field_nuxs,
            is_beta_user: field_is_beta_user,
            queued_nudges: field_queued_nudges,
            active_progress_guide: field_active_progress_guide,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsBskyAppStatePrefCborView<'a> {
    pub nuxs: Vec<ActorDefsNux>,
    pub is_beta_user: Option<bool>,
    pub queued_nudges: Vec<String>,
    pub active_progress_guide: Option<ActorDefsBskyAppProgressGuideCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsBskyAppStatePrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsBskyAppStatePref, crate::cbor::CborError> {
        Ok(ActorDefsBskyAppStatePref {
            nuxs: self.nuxs.clone(),
            is_beta_user: self.is_beta_user,
            queued_nudges: self.queued_nudges.clone(),
            active_progress_guide: self
                .active_progress_guide
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_nuxs: Vec<ActorDefsNux> = Vec::new();
        let mut field_is_beta_user: Option<bool> = None;
        let mut field_queued_nudges: Vec<String> = Vec::new();
        let mut field_active_progress_guide: Option<ActorDefsBskyAppProgressGuideCborView<'a>> =
            None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x6e\x75\x78\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_nuxs.push(ActorDefsNux::from_cbor_value(item)?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x6a\x69\x73\x42\x65\x74\x61\x55\x73\x65\x72", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_is_beta_user = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(
            b"\x6c\x71\x75\x65\x75\x65\x64\x4e\x75\x64\x67\x65\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Array(items) = value {
                    for item in items {
                        if let crate::cbor::Value::Text(s) = item {
                            field_queued_nudges.push(s.to_string());
                        } else {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected text in array".into(),
                            ));
                        }
                    }
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x73\x61\x63\x74\x69\x76\x65\x50\x72\x6f\x67\x72\x65\x73\x73\x47\x75\x69\x64\x65",
            |decoder| {
                field_active_progress_guide =
                    Some(ActorDefsBskyAppProgressGuideCborView::decode_cbor(decoder)?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"nuxs" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_nuxs.push(ActorDefsNux::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"isBetaUser" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_beta_user = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"queuedNudges" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_queued_nudges.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"activeProgressGuide" => {
                    field_active_progress_guide =
                        Some(ActorDefsBskyAppProgressGuideCborView::decode_cbor(decoder)?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            nuxs: field_nuxs,
            is_beta_user: field_is_beta_user,
            queued_nudges: field_queued_nudges,
            active_progress_guide: field_active_progress_guide,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsContentLabelPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsContentLabelPref {
    pub label: String,
    /// Which labeler does this preference apply to? If undefined, applies globally.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labeler_did: Option<crate::syntax::Did>,
    pub visibility: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsContentLabelPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.labeler_did.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("label")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.label)?;
            if self.labeler_did.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labelerDid")?;
                if let Some(ref val) = self.labeler_did {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("visibility")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.visibility)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.label)?;
                pairs.push(("label", vbuf));
            }
            if self.labeler_did.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.labeler_did {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("labelerDid", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.visibility)?;
                pairs.push(("visibility", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_label: Option<String> = None;
        let mut field_labeler_did: Option<crate::syntax::Did> = None;
        let mut field_visibility: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "label" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_label = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labelerDid" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_labeler_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "visibility" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_visibility = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsContentLabelPref {
            label: field_label.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'label'".into())
            })?,
            labeler_did: field_labeler_did,
            visibility: field_visibility.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'visibility'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_label: Option<String> = None;
        let mut field_labeler_did: Option<crate::syntax::Did> = None;
        let mut field_visibility: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "label" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_label = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labelerDid" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_labeler_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "visibility" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_visibility = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsContentLabelPref {
            label: field_label.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'label'".into())
            })?,
            labeler_did: field_labeler_did,
            visibility: field_visibility.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'visibility'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsContentLabelPrefCborView<'a> {
    pub label: &'a str,
    pub labeler_did: Option<crate::syntax::Did>,
    pub visibility: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsContentLabelPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsContentLabelPref, crate::cbor::CborError> {
        Ok(ActorDefsContentLabelPref {
            label: self.label.to_owned(),
            labeler_did: self.labeler_did.clone(),
            visibility: self.visibility.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_label: Option<&'a str> = None;
        let mut field_labeler_did: Option<crate::syntax::Did> = None;
        let mut field_visibility: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x6c\x61\x62\x65\x6c", |decoder| {
            field_label = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x6a\x6c\x61\x62\x65\x6c\x65\x72\x44\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_labeler_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x6a\x76\x69\x73\x69\x62\x69\x6c\x69\x74\x79", |decoder| {
            field_visibility = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"label" => {
                    field_label = Some(decoder.text()?);
                }
                b"labelerDid" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_labeler_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"visibility" => {
                    field_visibility = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            label: field_label.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'label'".into())
            })?,
            labeler_did: field_labeler_did,
            visibility: field_visibility.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'visibility'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsDeclaredAgePref — Read-only preference containing value(s) inferred from the user's declared birthdate. Absence of this preference object in the response indicates that the user has not made a declaration.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsDeclaredAgePref {
    /// Indicates if the user has declared that they are over 13 years of age.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_over_age13: Option<bool>,
    /// Indicates if the user has declared that they are over 16 years of age.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_over_age16: Option<bool>,
    /// Indicates if the user has declared that they are over 18 years of age.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_over_age18: Option<bool>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsDeclaredAgePref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.is_over_age13.is_some() {
                count += 1;
            }
            if self.is_over_age16.is_some() {
                count += 1;
            }
            if self.is_over_age18.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.is_over_age13.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isOverAge13")?;
                if let Some(ref val) = self.is_over_age13 {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.is_over_age16.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isOverAge16")?;
                if let Some(ref val) = self.is_over_age16 {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.is_over_age18.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isOverAge18")?;
                if let Some(ref val) = self.is_over_age18 {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.is_over_age13.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_over_age13 {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isOverAge13", vbuf));
            }
            if self.is_over_age16.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_over_age16 {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isOverAge16", vbuf));
            }
            if self.is_over_age18.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_over_age18 {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isOverAge18", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_is_over_age13: Option<bool> = None;
        let mut field_is_over_age16: Option<bool> = None;
        let mut field_is_over_age18: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "isOverAge13" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age13 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "isOverAge16" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age16 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "isOverAge18" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age18 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsDeclaredAgePref {
            is_over_age13: field_is_over_age13,
            is_over_age16: field_is_over_age16,
            is_over_age18: field_is_over_age18,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_is_over_age13: Option<bool> = None;
        let mut field_is_over_age16: Option<bool> = None;
        let mut field_is_over_age18: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "isOverAge13" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age13 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "isOverAge16" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age16 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "isOverAge18" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age18 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsDeclaredAgePref {
            is_over_age13: field_is_over_age13,
            is_over_age16: field_is_over_age16,
            is_over_age18: field_is_over_age18,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsDeclaredAgePrefCborView<'a> {
    pub is_over_age13: Option<bool>,
    pub is_over_age16: Option<bool>,
    pub is_over_age18: Option<bool>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsDeclaredAgePrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsDeclaredAgePref, crate::cbor::CborError> {
        Ok(ActorDefsDeclaredAgePref {
            is_over_age13: self.is_over_age13,
            is_over_age16: self.is_over_age16,
            is_over_age18: self.is_over_age18,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_is_over_age13: Option<bool> = None;
        let mut field_is_over_age16: Option<bool> = None;
        let mut field_is_over_age18: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x6b\x69\x73\x4f\x76\x65\x72\x41\x67\x65\x31\x33",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_is_over_age13 = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x69\x73\x4f\x76\x65\x72\x41\x67\x65\x31\x36",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_is_over_age16 = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x69\x73\x4f\x76\x65\x72\x41\x67\x65\x31\x38",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_is_over_age18 = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"isOverAge13" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age13 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"isOverAge16" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age16 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"isOverAge18" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_over_age18 = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            is_over_age13: field_is_over_age13,
            is_over_age16: field_is_over_age16,
            is_over_age18: field_is_over_age18,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsFeedViewPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsFeedViewPref {
    /// The URI of the feed, or an identifier which describes the feed.
    pub feed: String,
    /// Hide quote posts in the feed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_quote_posts: Option<bool>,
    /// Hide replies in the feed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_replies: Option<bool>,
    /// Hide replies in the feed if they do not have this number of likes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_replies_by_like_count: Option<i64>,
    /// Hide replies in the feed if they are not by followed users.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_replies_by_unfollowed: Option<bool>,
    /// Hide reposts in the feed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_reposts: Option<bool>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsFeedViewPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.hide_replies.is_some() {
                count += 1;
            }
            if self.hide_reposts.is_some() {
                count += 1;
            }
            if self.hide_quote_posts.is_some() {
                count += 1;
            }
            if self.hide_replies_by_like_count.is_some() {
                count += 1;
            }
            if self.hide_replies_by_unfollowed.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("feed")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.feed)?;
            if self.hide_replies.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideReplies")?;
                if let Some(ref val) = self.hide_replies {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.hide_reposts.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideReposts")?;
                if let Some(ref val) = self.hide_reposts {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.hide_quote_posts.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideQuotePosts")?;
                if let Some(ref val) = self.hide_quote_posts {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.hide_replies_by_like_count.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideRepliesByLikeCount")?;
                if let Some(ref val) = self.hide_replies_by_like_count {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.hide_replies_by_unfollowed.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideRepliesByUnfollowed")?;
                if let Some(ref val) = self.hide_replies_by_unfollowed {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.feed)?;
                pairs.push(("feed", vbuf));
            }
            if self.hide_replies.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_replies {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideReplies", vbuf));
            }
            if self.hide_reposts.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_reposts {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideReposts", vbuf));
            }
            if self.hide_quote_posts.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_quote_posts {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideQuotePosts", vbuf));
            }
            if self.hide_replies_by_like_count.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_replies_by_like_count {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("hideRepliesByLikeCount", vbuf));
            }
            if self.hide_replies_by_unfollowed.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_replies_by_unfollowed {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideRepliesByUnfollowed", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_feed: Option<String> = None;
        let mut field_hide_replies: Option<bool> = None;
        let mut field_hide_reposts: Option<bool> = None;
        let mut field_hide_quote_posts: Option<bool> = None;
        let mut field_hide_replies_by_like_count: Option<i64> = None;
        let mut field_hide_replies_by_unfollowed: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "feed" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_feed = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "hideReplies" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideReposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideQuotePosts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_quote_posts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideRepliesByLikeCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_hide_replies_by_like_count =
                                Some(i64::try_from(n).map_err(|_| {
                                    crate::cbor::CborError::InvalidCbor(
                                        "integer out of i64 range".into(),
                                    )
                                })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_hide_replies_by_like_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "hideRepliesByUnfollowed" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies_by_unfollowed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsFeedViewPref {
            feed: field_feed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'feed'".into())
            })?,
            hide_replies: field_hide_replies,
            hide_reposts: field_hide_reposts,
            hide_quote_posts: field_hide_quote_posts,
            hide_replies_by_like_count: field_hide_replies_by_like_count,
            hide_replies_by_unfollowed: field_hide_replies_by_unfollowed,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_feed: Option<String> = None;
        let mut field_hide_replies: Option<bool> = None;
        let mut field_hide_reposts: Option<bool> = None;
        let mut field_hide_quote_posts: Option<bool> = None;
        let mut field_hide_replies_by_like_count: Option<i64> = None;
        let mut field_hide_replies_by_unfollowed: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "feed" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_feed = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "hideReplies" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideReposts" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideQuotePosts" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_quote_posts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hideRepliesByLikeCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_hide_replies_by_like_count =
                            Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_hide_replies_by_like_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "hideRepliesByUnfollowed" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies_by_unfollowed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsFeedViewPref {
            feed: field_feed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'feed'".into())
            })?,
            hide_replies: field_hide_replies,
            hide_reposts: field_hide_reposts,
            hide_quote_posts: field_hide_quote_posts,
            hide_replies_by_like_count: field_hide_replies_by_like_count,
            hide_replies_by_unfollowed: field_hide_replies_by_unfollowed,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsFeedViewPrefCborView<'a> {
    pub feed: &'a str,
    pub hide_replies: Option<bool>,
    pub hide_reposts: Option<bool>,
    pub hide_quote_posts: Option<bool>,
    pub hide_replies_by_like_count: Option<i64>,
    pub hide_replies_by_unfollowed: Option<bool>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsFeedViewPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsFeedViewPref, crate::cbor::CborError> {
        Ok(ActorDefsFeedViewPref {
            feed: self.feed.to_owned(),
            hide_replies: self.hide_replies,
            hide_reposts: self.hide_reposts,
            hide_quote_posts: self.hide_quote_posts,
            hide_replies_by_like_count: self.hide_replies_by_like_count,
            hide_replies_by_unfollowed: self.hide_replies_by_unfollowed,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_feed: Option<&'a str> = None;
        let mut field_hide_replies: Option<bool> = None;
        let mut field_hide_reposts: Option<bool> = None;
        let mut field_hide_quote_posts: Option<bool> = None;
        let mut field_hide_replies_by_like_count: Option<i64> = None;
        let mut field_hide_replies_by_unfollowed: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x66\x65\x65\x64", |decoder| {
            field_feed = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x68\x69\x64\x65\x52\x65\x70\x6c\x69\x65\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_hide_replies = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x68\x69\x64\x65\x52\x65\x70\x6f\x73\x74\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_hide_reposts = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6e\x68\x69\x64\x65\x51\x75\x6f\x74\x65\x50\x6f\x73\x74\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_hide_quote_posts = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(b"\x76\x68\x69\x64\x65\x52\x65\x70\x6c\x69\x65\x73\x42\x79\x4c\x69\x6b\x65\x43\x6f\x75\x6e\x74", |decoder| {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => { field_hide_replies_by_like_count = Some(i64::try_from(n).map_err(|_| crate::cbor::CborError::InvalidCbor("integer out of i64 range".into()))?); }
                        crate::cbor::Value::Signed(n) => { field_hide_replies_by_like_count = Some(n); }
                        _ => return Err(crate::cbor::CborError::InvalidCbor("expected integer".into())),
                    }
            Ok(())
        })?;
        entries.try_field(b"\x77\x68\x69\x64\x65\x52\x65\x70\x6c\x69\x65\x73\x42\x79\x55\x6e\x66\x6f\x6c\x6c\x6f\x77\x65\x64", |decoder| {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies_by_unfollowed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"feed" => {
                    field_feed = Some(decoder.text()?);
                }
                b"hideReplies" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"hideReposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"hideQuotePosts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_quote_posts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"hideRepliesByLikeCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_hide_replies_by_like_count =
                                Some(i64::try_from(n).map_err(|_| {
                                    crate::cbor::CborError::InvalidCbor(
                                        "integer out of i64 range".into(),
                                    )
                                })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_hide_replies_by_like_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"hideRepliesByUnfollowed" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_replies_by_unfollowed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            feed: field_feed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'feed'".into())
            })?,
            hide_replies: field_hide_replies,
            hide_reposts: field_hide_reposts,
            hide_quote_posts: field_hide_quote_posts,
            hide_replies_by_like_count: field_hide_replies_by_like_count,
            hide_replies_by_unfollowed: field_hide_replies_by_unfollowed,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsHiddenPostsPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsHiddenPostsPref {
    /// A list of URIs of posts the account owner has hidden.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub items: Vec<crate::syntax::AtUri>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsHiddenPostsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("items")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.items.len() as u64)?;
            for item in &self.items {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item.as_str())?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.items.len() as u64)?;
                for item in &self.items {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item.as_str())?;
                }
                pairs.push(("items", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_items: Vec<crate::syntax::AtUri> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_items.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsHiddenPostsPref {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_items: Vec<crate::syntax::AtUri> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "items" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_items.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsHiddenPostsPref {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsHiddenPostsPrefCborView<'a> {
    pub items: Vec<crate::syntax::AtUri>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsHiddenPostsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsHiddenPostsPref, crate::cbor::CborError> {
        Ok(ActorDefsHiddenPostsPref {
            items: self.items.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_items: Vec<crate::syntax::AtUri> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x69\x74\x65\x6d\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_items.push(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_items.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            items: field_items,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsInterestsPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsInterestsPref {
    /// A list of tags which describe the account owner's interests gathered during onboarding.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<String>,
    /// The timestamp when the account owner last updated their interests.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<crate::syntax::Datetime>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsInterestsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.updated_at.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("tags")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.tags.len() as u64)?;
            for item in &self.tags {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item)?;
            }
            if self.updated_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("updatedAt")?;
                if let Some(ref val) = self.updated_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.tags.len() as u64)?;
                for item in &self.tags {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item)?;
                }
                pairs.push(("tags", vbuf));
            }
            if self.updated_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.updated_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("updatedAt", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_tags: Vec<String> = Vec::new();
        let mut field_updated_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "tags" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_tags.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "updatedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_updated_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsInterestsPref {
            tags: field_tags,
            updated_at: field_updated_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_tags: Vec<String> = Vec::new();
        let mut field_updated_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "tags" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_tags.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "updatedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_updated_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsInterestsPref {
            tags: field_tags,
            updated_at: field_updated_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsInterestsPrefCborView<'a> {
    pub tags: Vec<String>,
    pub updated_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsInterestsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsInterestsPref, crate::cbor::CborError> {
        Ok(ActorDefsInterestsPref {
            tags: self.tags.clone(),
            updated_at: self.updated_at.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_tags: Vec<String> = Vec::new();
        let mut field_updated_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x74\x61\x67\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_tags.push(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x75\x70\x64\x61\x74\x65\x64\x41\x74", |decoder| {
            field_updated_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"tags" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_tags.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"updatedAt" => {
                    field_updated_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            tags: field_tags,
            updated_at: field_updated_at,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsKnownFollowers — The subject's followers whom you also follow
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsKnownFollowers {
    pub count: i64,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub followers: Vec<ActorDefsProfileViewBasic>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsKnownFollowers {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 2u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("count")?;
            crate::cbor::Encoder::new(&mut *buf).encode_i64(self.count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("followers")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.followers.len() as u64)?;
            for item in &self.followers {
                item.encode_cbor(buf)?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_i64(self.count)?;
                pairs.push(("count", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.followers.len() as u64)?;
                for item in &self.followers {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("followers", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_count: Option<i64> = None;
        let mut field_followers: Vec<ActorDefsProfileViewBasic> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "count" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "followers" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_followers.push(ActorDefsProfileViewBasic::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsKnownFollowers {
            count: field_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'count'".into())
            })?,
            followers: field_followers,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_count: Option<i64> = None;
        let mut field_followers: Vec<ActorDefsProfileViewBasic> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "count" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "followers" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_followers.push(ActorDefsProfileViewBasic::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsKnownFollowers {
            count: field_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'count'".into())
            })?,
            followers: field_followers,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsKnownFollowersCborView<'a> {
    pub count: i64,
    pub followers: Vec<ActorDefsProfileViewBasic>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsKnownFollowersCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsKnownFollowers, crate::cbor::CborError> {
        Ok(ActorDefsKnownFollowers {
            count: self.count,
            followers: self.followers.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_count: Option<i64> = None;
        let mut field_followers: Vec<ActorDefsProfileViewBasic> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x63\x6f\x75\x6e\x74", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_count = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_count = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x66\x6f\x6c\x6c\x6f\x77\x65\x72\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_followers.push(ActorDefsProfileViewBasic::from_cbor_value(item)?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"count" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"followers" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_followers.push(ActorDefsProfileViewBasic::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            count: field_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'count'".into())
            })?,
            followers: field_followers,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsLabelerPrefItem object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsLabelerPrefItem {
    pub did: crate::syntax::Did,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsLabelerPrefItem {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsLabelerPrefItem {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsLabelerPrefItem {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsLabelerPrefItemCborView<'a> {
    pub did: crate::syntax::Did,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsLabelerPrefItemCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsLabelerPrefItem, crate::cbor::CborError> {
        Ok(ActorDefsLabelerPrefItem {
            did: self.did.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x64\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsLabelersPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsLabelersPref {
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labelers: Vec<ActorDefsLabelerPrefItem>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsLabelersPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("labelers")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.labelers.len() as u64)?;
            for item in &self.labelers {
                item.encode_cbor(buf)?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labelers.len() as u64)?;
                for item in &self.labelers {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labelers", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_labelers: Vec<ActorDefsLabelerPrefItem> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "labelers" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labelers.push(ActorDefsLabelerPrefItem::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsLabelersPref {
            labelers: field_labelers,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_labelers: Vec<ActorDefsLabelerPrefItem> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "labelers" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labelers.push(ActorDefsLabelerPrefItem::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsLabelersPref {
            labelers: field_labelers,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsLabelersPrefCborView<'a> {
    pub labelers: Vec<ActorDefsLabelerPrefItem>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsLabelersPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsLabelersPref, crate::cbor::CborError> {
        Ok(ActorDefsLabelersPref {
            labelers: self.labelers.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_labelers: Vec<ActorDefsLabelerPrefItem> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x68\x6c\x61\x62\x65\x6c\x65\x72\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_labelers.push(ActorDefsLabelerPrefItem::from_cbor_value(item)?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"labelers" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labelers.push(ActorDefsLabelerPrefItem::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            labelers: field_labelers,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsLiveEventPreferences — Preferences for live events.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsLiveEventPreferences {
    /// A list of feed IDs that the user has hidden from live events.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub hidden_feed_ids: Vec<String>,
    /// Whether to hide all feeds from live events.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_all_feeds: Option<bool>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsLiveEventPreferences {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.hide_all_feeds.is_some() {
                count += 1;
            }
            if !self.hidden_feed_ids.is_empty() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.hide_all_feeds.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideAllFeeds")?;
                if let Some(ref val) = self.hide_all_feeds {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if !self.hidden_feed_ids.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hiddenFeedIds")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.hidden_feed_ids.len() as u64)?;
                for item in &self.hidden_feed_ids {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(item)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.hide_all_feeds.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_all_feeds {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideAllFeeds", vbuf));
            }
            if !self.hidden_feed_ids.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.hidden_feed_ids.len() as u64)?;
                for item in &self.hidden_feed_ids {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item)?;
                }
                pairs.push(("hiddenFeedIds", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_hide_all_feeds: Option<bool> = None;
        let mut field_hidden_feed_ids: Vec<String> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "hideAllFeeds" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_all_feeds = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hiddenFeedIds" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_hidden_feed_ids.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsLiveEventPreferences {
            hide_all_feeds: field_hide_all_feeds,
            hidden_feed_ids: field_hidden_feed_ids,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_hide_all_feeds: Option<bool> = None;
        let mut field_hidden_feed_ids: Vec<String> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "hideAllFeeds" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_all_feeds = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "hiddenFeedIds" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_hidden_feed_ids.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsLiveEventPreferences {
            hide_all_feeds: field_hide_all_feeds,
            hidden_feed_ids: field_hidden_feed_ids,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsLiveEventPreferencesCborView<'a> {
    pub hide_all_feeds: Option<bool>,
    pub hidden_feed_ids: Vec<String>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsLiveEventPreferencesCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsLiveEventPreferences, crate::cbor::CborError> {
        Ok(ActorDefsLiveEventPreferences {
            hide_all_feeds: self.hide_all_feeds,
            hidden_feed_ids: self.hidden_feed_ids.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_hide_all_feeds: Option<bool> = None;
        let mut field_hidden_feed_ids: Vec<String> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x6c\x68\x69\x64\x65\x41\x6c\x6c\x46\x65\x65\x64\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_hide_all_feeds = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6d\x68\x69\x64\x64\x65\x6e\x46\x65\x65\x64\x49\x64\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Array(items) = value {
                    for item in items {
                        if let crate::cbor::Value::Text(s) = item {
                            field_hidden_feed_ids.push(s.to_string());
                        } else {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected text in array".into(),
                            ));
                        }
                    }
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                }
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"hideAllFeeds" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_all_feeds = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"hiddenFeedIds" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_hidden_feed_ids.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            hide_all_feeds: field_hide_all_feeds,
            hidden_feed_ids: field_hidden_feed_ids,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsMutedWord — A word that the account owner has muted.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsMutedWord {
    /// Groups of users to apply the muted word to. If undefined, applies to all users.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub actor_target: Option<String>,
    /// The date and time at which the muted word will expire and no longer be applied.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<crate::syntax::Datetime>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The intended targets of the muted word.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub targets: Vec<ActorDefsMutedWordTarget>,
    /// The muted word itself.
    pub value: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsMutedWord {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.id.is_some() {
                count += 1;
            }
            if self.expires_at.is_some() {
                count += 1;
            }
            if self.actor_target.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.id.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("id")?;
                if let Some(ref val) = self.id {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("value")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.value)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("targets")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.targets.len() as u64)?;
            for item in &self.targets {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item)?;
            }
            if self.expires_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("expiresAt")?;
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.actor_target.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("actorTarget")?;
                if let Some(ref val) = self.actor_target {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.id.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.id {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("id", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.value)?;
                pairs.push(("value", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.targets.len() as u64)?;
                for item in &self.targets {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item)?;
                }
                pairs.push(("targets", vbuf));
            }
            if self.expires_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("expiresAt", vbuf));
            }
            if self.actor_target.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.actor_target {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("actorTarget", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_id: Option<String> = None;
        let mut field_value: Option<String> = None;
        let mut field_targets: Vec<ActorDefsMutedWordTarget> = Vec::new();
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut field_actor_target: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "id" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "value" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_value = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "targets" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_targets.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "expiresAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "actorTarget" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_actor_target = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsMutedWord {
            id: field_id,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            targets: field_targets,
            expires_at: field_expires_at,
            actor_target: field_actor_target,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_id: Option<String> = None;
        let mut field_value: Option<String> = None;
        let mut field_targets: Vec<ActorDefsMutedWordTarget> = Vec::new();
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut field_actor_target: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "id" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "value" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_value = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "targets" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_targets.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "expiresAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "actorTarget" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_actor_target = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsMutedWord {
            id: field_id,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            targets: field_targets,
            expires_at: field_expires_at,
            actor_target: field_actor_target,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsMutedWordCborView<'a> {
    pub id: Option<&'a str>,
    pub value: &'a str,
    pub targets: Vec<ActorDefsMutedWordTarget>,
    pub expires_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub actor_target: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsMutedWordCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsMutedWord, crate::cbor::CborError> {
        Ok(ActorDefsMutedWord {
            id: self.id.as_ref().map(|value| (*value).to_owned()),
            value: self.value.to_owned(),
            targets: self.targets.clone(),
            expires_at: self.expires_at.as_ref().map(|value| (*value).to_owned()),
            actor_target: self.actor_target.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_id: Option<&'a str> = None;
        let mut field_value: Option<&'a str> = None;
        let mut field_targets: Vec<ActorDefsMutedWordTarget> = Vec::new();
        let mut field_expires_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_actor_target: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x62\x69\x64", |decoder| {
            field_id = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x76\x61\x6c\x75\x65", |decoder| {
            field_value = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x67\x74\x61\x72\x67\x65\x74\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_targets.push(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x65\x78\x70\x69\x72\x65\x73\x41\x74", |decoder| {
            field_expires_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x61\x63\x74\x6f\x72\x54\x61\x72\x67\x65\x74",
            |decoder| {
                field_actor_target = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"id" => {
                    field_id = Some(decoder.text()?);
                }
                b"value" => {
                    field_value = Some(decoder.text()?);
                }
                b"targets" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_targets.push(s.to_string());
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"expiresAt" => {
                    field_expires_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"actorTarget" => {
                    field_actor_target = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            id: field_id,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            targets: field_targets,
            expires_at: field_expires_at,
            actor_target: field_actor_target,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsMutedWordTarget is a string type from app.bsky.actor.defs.
pub type ActorDefsMutedWordTarget = String;

/// ActorDefsMutedWordsPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsMutedWordsPref {
    /// A list of words the account owner has muted.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub items: Vec<ActorDefsMutedWord>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsMutedWordsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("items")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.items.len() as u64)?;
            for item in &self.items {
                item.encode_cbor(buf)?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.items.len() as u64)?;
                for item in &self.items {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("items", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_items: Vec<ActorDefsMutedWord> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsMutedWord::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsMutedWordsPref {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_items: Vec<ActorDefsMutedWord> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "items" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsMutedWord::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsMutedWordsPref {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsMutedWordsPrefCborView<'a> {
    pub items: Vec<ActorDefsMutedWord>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsMutedWordsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsMutedWordsPref, crate::cbor::CborError> {
        Ok(ActorDefsMutedWordsPref {
            items: self.items.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_items: Vec<ActorDefsMutedWord> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x69\x74\x65\x6d\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_items.push(ActorDefsMutedWord::from_cbor_value(item)?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsMutedWord::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            items: field_items,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsNux — A new user experiences (NUX) storage object
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsNux {
    pub completed: bool,
    /// Arbitrary data for the NUX. The structure is defined by the NUX itself. Limited to 300 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data: Option<String>,
    /// The date and time at which the NUX will expire and should be considered completed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<crate::syntax::Datetime>,
    pub id: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsNux {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.data.is_some() {
                count += 1;
            }
            if self.expires_at.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("id")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.id)?;
            if self.data.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("data")?;
                if let Some(ref val) = self.data {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("completed")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.completed)?;
            if self.expires_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("expiresAt")?;
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.id)?;
                pairs.push(("id", vbuf));
            }
            if self.data.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.data {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("data", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.completed)?;
                pairs.push(("completed", vbuf));
            }
            if self.expires_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("expiresAt", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_id: Option<String> = None;
        let mut field_data: Option<String> = None;
        let mut field_completed: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "id" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "data" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_data = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completed" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_completed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "expiresAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsNux {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            data: field_data,
            completed: field_completed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'completed'".into())
            })?,
            expires_at: field_expires_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_id: Option<String> = None;
        let mut field_data: Option<String> = None;
        let mut field_completed: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "id" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "data" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_data = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completed" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_completed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "expiresAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsNux {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            data: field_data,
            completed: field_completed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'completed'".into())
            })?,
            expires_at: field_expires_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsNuxCborView<'a> {
    pub id: &'a str,
    pub data: Option<&'a str>,
    pub completed: bool,
    pub expires_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsNuxCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsNux, crate::cbor::CborError> {
        Ok(ActorDefsNux {
            id: self.id.to_owned(),
            data: self.data.as_ref().map(|value| (*value).to_owned()),
            completed: self.completed,
            expires_at: self.expires_at.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_id: Option<&'a str> = None;
        let mut field_data: Option<&'a str> = None;
        let mut field_completed: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x62\x69\x64", |decoder| {
            field_id = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x64\x64\x61\x74\x61", |decoder| {
            field_data = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x6f\x6d\x70\x6c\x65\x74\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_completed = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x65\x78\x70\x69\x72\x65\x73\x41\x74", |decoder| {
            field_expires_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"id" => {
                    field_id = Some(decoder.text()?);
                }
                b"data" => {
                    field_data = Some(decoder.text()?);
                }
                b"completed" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_completed = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"expiresAt" => {
                    field_expires_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            data: field_data,
            completed: field_completed.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'completed'".into())
            })?,
            expires_at: field_expires_at,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsPersonalDetailsPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsPersonalDetailsPref {
    /// The birth date of account owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub birth_date: Option<crate::syntax::Datetime>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsPersonalDetailsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.birth_date.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.birth_date.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("birthDate")?;
                if let Some(ref val) = self.birth_date {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.birth_date.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.birth_date {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("birthDate", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_birth_date: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "birthDate" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_birth_date = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsPersonalDetailsPref {
            birth_date: field_birth_date,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_birth_date: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "birthDate" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_birth_date = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsPersonalDetailsPref {
            birth_date: field_birth_date,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsPersonalDetailsPrefCborView<'a> {
    pub birth_date: Option<crate::syntax::DatetimeRef<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsPersonalDetailsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsPersonalDetailsPref, crate::cbor::CborError> {
        Ok(ActorDefsPersonalDetailsPref {
            birth_date: self.birth_date.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_birth_date: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x69\x62\x69\x72\x74\x68\x44\x61\x74\x65", |decoder| {
            field_birth_date = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"birthDate" => {
                    field_birth_date = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            birth_date: field_birth_date,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsPostInteractionSettingsPref — Default post interaction settings for the account. These values should be applied as default values when creating new posts. These refs should mirror the threadgate and postgate records exactly.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsPostInteractionSettingsPref {
    /// Matches postgate record. List of rules defining who can embed this users posts. If value is an empty array or is undefined, no particular rules apply and anyone can embed.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub postgate_embedding_rules:
        Vec<ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion>,
    /// Matches threadgate record. List of rules defining who can reply to this users posts. If value is an empty array, no one can reply. If value is undefined, anyone can reply.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub threadgate_allow_rules: Vec<ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

/// ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion is a union type.
#[derive(Debug, Clone)]
pub enum ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion {
    FeedPostgateDisableRule(Box<crate::api::app::bsky::FeedPostgateDisableRule>),
    Unknown(crate::api::UnknownUnionVariant),
}

impl serde::Serialize for ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::FeedPostgateDisableRule(inner) => {
                let mut map = serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert("$type".to_string(), serde_json::Value::String("app.bsky.feed.postgate#disableRule".to_string()));
                }
                map.serialize(serializer)
            }
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom("no JSON data for unknown union variant"))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de>
    for ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion
{
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "app.bsky.feed.postgate#disableRule" => {
                let inner: crate::api::app::bsky::FeedPostgateDisableRule =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::FeedPostgateDisableRule(Box::new(inner)))
            }
            _ => Ok(
                ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::Unknown(
                    crate::api::UnknownUnionVariant {
                        r#type: type_str.to_string(),
                        json: Some(value),
                        cbor: None,
                    },
                ),
            ),
        }
    }
}

impl ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        match self {
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::FeedPostgateDisableRule(inner) => inner.encode_cbor(buf),
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(crate::cbor::CborError::InvalidCbor("no CBOR data for unknown union variant".into()))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => {
                return Err(crate::cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                crate::cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "app.bsky.feed.postgate#disableRule" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = crate::api::app::bsky::FeedPostgateDisableRule::decode_cbor(&mut dec)?;
                Ok(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::FeedPostgateDisableRule(Box::new(inner)))
            }
            _ => Ok(
                ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::Unknown(
                    crate::api::UnknownUnionVariant {
                        r#type: type_str.to_string(),
                        json: None,
                        cbor: Some(raw.to_vec()),
                    },
                ),
            ),
        }
    }
}

/// ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion is a union type.
#[derive(Debug, Clone)]
pub enum ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion {
    FeedThreadgateMentionRule(Box<crate::api::app::bsky::FeedThreadgateMentionRule>),
    FeedThreadgateFollowerRule(Box<crate::api::app::bsky::FeedThreadgateFollowerRule>),
    FeedThreadgateFollowingRule(Box<crate::api::app::bsky::FeedThreadgateFollowingRule>),
    FeedThreadgateListRule(Box<crate::api::app::bsky::FeedThreadgateListRule>),
    Unknown(crate::api::UnknownUnionVariant),
}

impl serde::Serialize for ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateMentionRule(inner) => {
                let mut map = serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert("$type".to_string(), serde_json::Value::String("app.bsky.feed.threadgate#mentionRule".to_string()));
                }
                map.serialize(serializer)
            }
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowerRule(inner) => {
                let mut map = serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert("$type".to_string(), serde_json::Value::String("app.bsky.feed.threadgate#followerRule".to_string()));
                }
                map.serialize(serializer)
            }
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowingRule(inner) => {
                let mut map = serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert("$type".to_string(), serde_json::Value::String("app.bsky.feed.threadgate#followingRule".to_string()));
                }
                map.serialize(serializer)
            }
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateListRule(inner) => {
                let mut map = serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert("$type".to_string(), serde_json::Value::String("app.bsky.feed.threadgate#listRule".to_string()));
                }
                map.serialize(serializer)
            }
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom("no JSON data for unknown union variant"))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de>
    for ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion
{
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "app.bsky.feed.threadgate#mentionRule" => {
                let inner: crate::api::app::bsky::FeedThreadgateMentionRule =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateMentionRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#followerRule" => {
                let inner: crate::api::app::bsky::FeedThreadgateFollowerRule =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowerRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#followingRule" => {
                let inner: crate::api::app::bsky::FeedThreadgateFollowingRule =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowingRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#listRule" => {
                let inner: crate::api::app::bsky::FeedThreadgateListRule =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateListRule(Box::new(inner)))
            }
            _ => Ok(
                ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::Unknown(
                    crate::api::UnknownUnionVariant {
                        r#type: type_str.to_string(),
                        json: Some(value),
                        cbor: None,
                    },
                ),
            ),
        }
    }
}

impl ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        match self {
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateMentionRule(inner) => inner.encode_cbor(buf),
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowerRule(inner) => inner.encode_cbor(buf),
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowingRule(inner) => inner.encode_cbor(buf),
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateListRule(inner) => inner.encode_cbor(buf),
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(crate::cbor::CborError::InvalidCbor("no CBOR data for unknown union variant".into()))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => {
                return Err(crate::cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                crate::cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "app.bsky.feed.threadgate#mentionRule" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner =
                    crate::api::app::bsky::FeedThreadgateMentionRule::decode_cbor(&mut dec)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateMentionRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#followerRule" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner =
                    crate::api::app::bsky::FeedThreadgateFollowerRule::decode_cbor(&mut dec)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowerRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#followingRule" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner =
                    crate::api::app::bsky::FeedThreadgateFollowingRule::decode_cbor(&mut dec)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateFollowingRule(Box::new(inner)))
            }
            "app.bsky.feed.threadgate#listRule" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = crate::api::app::bsky::FeedThreadgateListRule::decode_cbor(&mut dec)?;
                Ok(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::FeedThreadgateListRule(Box::new(inner)))
            }
            _ => Ok(
                ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::Unknown(
                    crate::api::UnknownUnionVariant {
                        r#type: type_str.to_string(),
                        json: None,
                        cbor: Some(raw.to_vec()),
                    },
                ),
            ),
        }
    }
}

impl ActorDefsPostInteractionSettingsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if !self.threadgate_allow_rules.is_empty() {
                count += 1;
            }
            if !self.postgate_embedding_rules.is_empty() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if !self.threadgate_allow_rules.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("threadgateAllowRules")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.threadgate_allow_rules.len() as u64)?;
                for item in &self.threadgate_allow_rules {
                    item.encode_cbor(buf)?;
                }
            }
            if !self.postgate_embedding_rules.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("postgateEmbeddingRules")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.postgate_embedding_rules.len() as u64)?;
                for item in &self.postgate_embedding_rules {
                    item.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if !self.threadgate_allow_rules.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.threadgate_allow_rules.len() as u64)?;
                for item in &self.threadgate_allow_rules {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("threadgateAllowRules", vbuf));
            }
            if !self.postgate_embedding_rules.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.postgate_embedding_rules.len() as u64)?;
                for item in &self.postgate_embedding_rules {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("postgateEmbeddingRules", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_threadgate_allow_rules: Vec<
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion,
        > = Vec::new();
        let mut field_postgate_embedding_rules: Vec<
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion,
        > = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "threadgateAllowRules" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_threadgate_allow_rules.push(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "postgateEmbeddingRules" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_postgate_embedding_rules.push(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) { result?; }

        Ok(ActorDefsPostInteractionSettingsPref {
            threadgate_allow_rules: field_threadgate_allow_rules,
            postgate_embedding_rules: field_postgate_embedding_rules,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_threadgate_allow_rules: Vec<
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion,
        > = Vec::new();
        let mut field_postgate_embedding_rules: Vec<
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion,
        > = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "threadgateAllowRules" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_threadgate_allow_rules.push(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "postgateEmbeddingRules" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_postgate_embedding_rules.push(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsPostInteractionSettingsPref {
            threadgate_allow_rules: field_threadgate_allow_rules,
            postgate_embedding_rules: field_postgate_embedding_rules,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsPostInteractionSettingsPrefCborView<'a> {
    pub threadgate_allow_rules: Vec<ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion>,
    pub postgate_embedding_rules:
        Vec<ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsPostInteractionSettingsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsPostInteractionSettingsPref, crate::cbor::CborError> {
        Ok(ActorDefsPostInteractionSettingsPref {
            threadgate_allow_rules: self.threadgate_allow_rules.clone(),
            postgate_embedding_rules: self.postgate_embedding_rules.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_threadgate_allow_rules: Vec<
            ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion,
        > = Vec::new();
        let mut field_postgate_embedding_rules: Vec<
            ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion,
        > = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x74\x74\x68\x72\x65\x61\x64\x67\x61\x74\x65\x41\x6c\x6c\x6f\x77\x52\x75\x6c\x65\x73", |decoder| {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_threadgate_allow_rules.push(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
            Ok(())
        })?;
        entries.try_field(b"\x76\x70\x6f\x73\x74\x67\x61\x74\x65\x45\x6d\x62\x65\x64\x64\x69\x6e\x67\x52\x75\x6c\x65\x73", |decoder| {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_postgate_embedding_rules.push(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"threadgateAllowRules" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_threadgate_allow_rules.push(ActorDefsPostInteractionSettingsPrefThreadgateAllowRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"postgateEmbeddingRules" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            let raw = crate::cbor::encode_value(&item)?;
                            let mut dec = crate::cbor::Decoder::new(&raw);
                            field_postgate_embedding_rules.push(ActorDefsPostInteractionSettingsPrefPostgateEmbeddingRulesUnion::decode_cbor(&mut dec)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into()))?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) { result?; }
        drop(entries);
        Ok(Self {
            threadgate_allow_rules: field_threadgate_allow_rules,
            postgate_embedding_rules: field_postgate_embedding_rules,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsPreferencesItem is a union type.
#[derive(Debug, Clone)]
pub enum ActorDefsPreferencesItem {
    ActorDefsAdultContentPref(Box<ActorDefsAdultContentPref>),
    ActorDefsContentLabelPref(Box<ActorDefsContentLabelPref>),
    ActorDefsSavedFeedsPref(Box<ActorDefsSavedFeedsPref>),
    ActorDefsSavedFeedsPrefV2(Box<ActorDefsSavedFeedsPrefV2>),
    ActorDefsPersonalDetailsPref(Box<ActorDefsPersonalDetailsPref>),
    ActorDefsDeclaredAgePref(Box<ActorDefsDeclaredAgePref>),
    ActorDefsFeedViewPref(Box<ActorDefsFeedViewPref>),
    ActorDefsThreadViewPref(Box<ActorDefsThreadViewPref>),
    ActorDefsInterestsPref(Box<ActorDefsInterestsPref>),
    ActorDefsMutedWordsPref(Box<ActorDefsMutedWordsPref>),
    ActorDefsHiddenPostsPref(Box<ActorDefsHiddenPostsPref>),
    ActorDefsBskyAppStatePref(Box<ActorDefsBskyAppStatePref>),
    ActorDefsLabelersPref(Box<ActorDefsLabelersPref>),
    ActorDefsPostInteractionSettingsPref(Box<ActorDefsPostInteractionSettingsPref>),
    ActorDefsVerificationPrefs(Box<ActorDefsVerificationPrefs>),
    ActorDefsLiveEventPreferences(Box<ActorDefsLiveEventPreferences>),
    Unknown(crate::api::UnknownUnionVariant),
}

impl serde::Serialize for ActorDefsPreferencesItem {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorDefsPreferencesItem::ActorDefsAdultContentPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#adultContentPref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsContentLabelPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#contentLabelPref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsSavedFeedsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#savedFeedsPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsSavedFeedsPrefV2(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#savedFeedsPrefV2".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsPersonalDetailsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#personalDetailsPref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsDeclaredAgePref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#declaredAgePref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsFeedViewPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#feedViewPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsThreadViewPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#threadViewPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsInterestsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#interestsPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsMutedWordsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#mutedWordsPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsHiddenPostsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#hiddenPostsPref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsBskyAppStatePref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#bskyAppStatePref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsLabelersPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.actor.defs#labelersPref".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsPostInteractionSettingsPref(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#postInteractionSettingsPref".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsVerificationPrefs(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#verificationPrefs".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::ActorDefsLiveEventPreferences(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String(
                            "app.bsky.actor.defs#liveEventPreferences".to_string(),
                        ),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsPreferencesItem::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom(
                        "no JSON data for unknown union variant",
                    ))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de> for ActorDefsPreferencesItem {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "app.bsky.actor.defs#adultContentPref" => {
                let inner: ActorDefsAdultContentPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsAdultContentPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#contentLabelPref" => {
                let inner: ActorDefsContentLabelPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsContentLabelPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#savedFeedsPref" => {
                let inner: ActorDefsSavedFeedsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsSavedFeedsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#savedFeedsPrefV2" => {
                let inner: ActorDefsSavedFeedsPrefV2 =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsSavedFeedsPrefV2(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#personalDetailsPref" => {
                let inner: ActorDefsPersonalDetailsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsPersonalDetailsPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#declaredAgePref" => {
                let inner: ActorDefsDeclaredAgePref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsDeclaredAgePref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#feedViewPref" => {
                let inner: ActorDefsFeedViewPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsFeedViewPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#threadViewPref" => {
                let inner: ActorDefsThreadViewPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsThreadViewPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#interestsPref" => {
                let inner: ActorDefsInterestsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsInterestsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#mutedWordsPref" => {
                let inner: ActorDefsMutedWordsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsMutedWordsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#hiddenPostsPref" => {
                let inner: ActorDefsHiddenPostsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsHiddenPostsPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#bskyAppStatePref" => {
                let inner: ActorDefsBskyAppStatePref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsBskyAppStatePref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#labelersPref" => {
                let inner: ActorDefsLabelersPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsLabelersPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#postInteractionSettingsPref" => {
                let inner: ActorDefsPostInteractionSettingsPref =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsPostInteractionSettingsPref(Box::new(inner)))
            }
            "app.bsky.actor.defs#verificationPrefs" => {
                let inner: ActorDefsVerificationPrefs =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsVerificationPrefs(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#liveEventPreferences" => {
                let inner: ActorDefsLiveEventPreferences =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsPreferencesItem::ActorDefsLiveEventPreferences(
                    Box::new(inner),
                ))
            }
            _ => Ok(ActorDefsPreferencesItem::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: Some(value),
                    cbor: None,
                },
            )),
        }
    }
}

impl ActorDefsPreferencesItem {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        match self {
            ActorDefsPreferencesItem::ActorDefsAdultContentPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsContentLabelPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsSavedFeedsPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsSavedFeedsPrefV2(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsPersonalDetailsPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsDeclaredAgePref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsFeedViewPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsThreadViewPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsInterestsPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsMutedWordsPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsHiddenPostsPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsBskyAppStatePref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsLabelersPref(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsPostInteractionSettingsPref(inner) => {
                inner.encode_cbor(buf)
            }
            ActorDefsPreferencesItem::ActorDefsVerificationPrefs(inner) => inner.encode_cbor(buf),
            ActorDefsPreferencesItem::ActorDefsLiveEventPreferences(inner) => {
                inner.encode_cbor(buf)
            }
            ActorDefsPreferencesItem::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(crate::cbor::CborError::InvalidCbor(
                        "no CBOR data for unknown union variant".into(),
                    ))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => {
                return Err(crate::cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                crate::cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "app.bsky.actor.defs#adultContentPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsAdultContentPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsAdultContentPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#contentLabelPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsContentLabelPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsContentLabelPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#savedFeedsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsSavedFeedsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsSavedFeedsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#savedFeedsPrefV2" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsSavedFeedsPrefV2::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsSavedFeedsPrefV2(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#personalDetailsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsPersonalDetailsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsPersonalDetailsPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#declaredAgePref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsDeclaredAgePref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsDeclaredAgePref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#feedViewPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsFeedViewPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsFeedViewPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#threadViewPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsThreadViewPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsThreadViewPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#interestsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsInterestsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsInterestsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#mutedWordsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsMutedWordsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsMutedWordsPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#hiddenPostsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsHiddenPostsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsHiddenPostsPref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#bskyAppStatePref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsBskyAppStatePref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsBskyAppStatePref(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#labelersPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsLabelersPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsLabelersPref(Box::new(
                    inner,
                )))
            }
            "app.bsky.actor.defs#postInteractionSettingsPref" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsPostInteractionSettingsPref::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsPostInteractionSettingsPref(Box::new(inner)))
            }
            "app.bsky.actor.defs#verificationPrefs" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsVerificationPrefs::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsVerificationPrefs(
                    Box::new(inner),
                ))
            }
            "app.bsky.actor.defs#liveEventPreferences" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = ActorDefsLiveEventPreferences::decode_cbor(&mut dec)?;
                Ok(ActorDefsPreferencesItem::ActorDefsLiveEventPreferences(
                    Box::new(inner),
                ))
            }
            _ => Ok(ActorDefsPreferencesItem::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: None,
                    cbor: Some(raw.to_vec()),
                },
            )),
        }
    }
}

/// ActorDefsPreferences is an array type from app.bsky.actor.defs.
pub type ActorDefsPreferences = Vec<ActorDefsPreferencesItem>;

/// ActorDefsProfileAssociated object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileAssociated {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activity_subscription: Option<ActorDefsProfileAssociatedActivitySubscription>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub chat: Option<ActorDefsProfileAssociatedChat>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub feedgens: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub germ: Option<ActorDefsProfileAssociatedGerm>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labeler: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lists: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub starter_packs: Option<i64>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileAssociated {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.chat.is_some() {
                count += 1;
            }
            if self.germ.is_some() {
                count += 1;
            }
            if self.lists.is_some() {
                count += 1;
            }
            if self.labeler.is_some() {
                count += 1;
            }
            if self.feedgens.is_some() {
                count += 1;
            }
            if self.starter_packs.is_some() {
                count += 1;
            }
            if self.activity_subscription.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.chat.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("chat")?;
                if let Some(ref val) = self.chat {
                    val.encode_cbor(buf)?;
                }
            }
            if self.germ.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("germ")?;
                if let Some(ref val) = self.germ {
                    val.encode_cbor(buf)?;
                }
            }
            if self.lists.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("lists")?;
                if let Some(ref val) = self.lists {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.labeler.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labeler")?;
                if let Some(ref val) = self.labeler {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.feedgens.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("feedgens")?;
                if let Some(ref val) = self.feedgens {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.starter_packs.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("starterPacks")?;
                if let Some(ref val) = self.starter_packs {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.activity_subscription.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("activitySubscription")?;
                if let Some(ref val) = self.activity_subscription {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.chat.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.chat {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("chat", vbuf));
            }
            if self.germ.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.germ {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("germ", vbuf));
            }
            if self.lists.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.lists {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("lists", vbuf));
            }
            if self.labeler.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.labeler {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("labeler", vbuf));
            }
            if self.feedgens.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.feedgens {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("feedgens", vbuf));
            }
            if self.starter_packs.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.starter_packs {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("starterPacks", vbuf));
            }
            if self.activity_subscription.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.activity_subscription {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("activitySubscription", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_chat: Option<ActorDefsProfileAssociatedChat> = None;
        let mut field_germ: Option<ActorDefsProfileAssociatedGerm> = None;
        let mut field_lists: Option<i64> = None;
        let mut field_labeler: Option<bool> = None;
        let mut field_feedgens: Option<i64> = None;
        let mut field_starter_packs: Option<i64> = None;
        let mut field_activity_subscription: Option<
            ActorDefsProfileAssociatedActivitySubscription,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "chat" => {
                    field_chat = Some(ActorDefsProfileAssociatedChat::decode_cbor(decoder)?);
                }
                "germ" => {
                    field_germ = Some(ActorDefsProfileAssociatedGerm::decode_cbor(decoder)?);
                }
                "lists" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_lists = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_lists = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "labeler" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_labeler = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "feedgens" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_feedgens = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_feedgens = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "starterPacks" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_starter_packs = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_starter_packs = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "activitySubscription" => {
                    field_activity_subscription = Some(
                        ActorDefsProfileAssociatedActivitySubscription::decode_cbor(decoder)?,
                    );
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileAssociated {
            chat: field_chat,
            germ: field_germ,
            lists: field_lists,
            labeler: field_labeler,
            feedgens: field_feedgens,
            starter_packs: field_starter_packs,
            activity_subscription: field_activity_subscription,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_chat: Option<ActorDefsProfileAssociatedChat> = None;
        let mut field_germ: Option<ActorDefsProfileAssociatedGerm> = None;
        let mut field_lists: Option<i64> = None;
        let mut field_labeler: Option<bool> = None;
        let mut field_feedgens: Option<i64> = None;
        let mut field_starter_packs: Option<i64> = None;
        let mut field_activity_subscription: Option<
            ActorDefsProfileAssociatedActivitySubscription,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "chat" => {
                    field_chat = Some(ActorDefsProfileAssociatedChat::from_cbor_value(value)?);
                }
                "germ" => {
                    field_germ = Some(ActorDefsProfileAssociatedGerm::from_cbor_value(value)?);
                }
                "lists" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_lists = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_lists = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "labeler" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_labeler = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "feedgens" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_feedgens = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_feedgens = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "starterPacks" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_starter_packs = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_starter_packs = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "activitySubscription" => {
                    field_activity_subscription = Some(
                        ActorDefsProfileAssociatedActivitySubscription::from_cbor_value(value)?,
                    );
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileAssociated {
            chat: field_chat,
            germ: field_germ,
            lists: field_lists,
            labeler: field_labeler,
            feedgens: field_feedgens,
            starter_packs: field_starter_packs,
            activity_subscription: field_activity_subscription,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileAssociatedCborView<'a> {
    pub chat: Option<ActorDefsProfileAssociatedChatCborView<'a>>,
    pub germ: Option<ActorDefsProfileAssociatedGermCborView<'a>>,
    pub lists: Option<i64>,
    pub labeler: Option<bool>,
    pub feedgens: Option<i64>,
    pub starter_packs: Option<i64>,
    pub activity_subscription: Option<ActorDefsProfileAssociatedActivitySubscriptionCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileAssociatedCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileAssociated, crate::cbor::CborError> {
        Ok(ActorDefsProfileAssociated {
            chat: self
                .chat
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            germ: self
                .germ
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            lists: self.lists,
            labeler: self.labeler,
            feedgens: self.feedgens,
            starter_packs: self.starter_packs,
            activity_subscription: self
                .activity_subscription
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_chat: Option<ActorDefsProfileAssociatedChatCborView<'a>> = None;
        let mut field_germ: Option<ActorDefsProfileAssociatedGermCborView<'a>> = None;
        let mut field_lists: Option<i64> = None;
        let mut field_labeler: Option<bool> = None;
        let mut field_feedgens: Option<i64> = None;
        let mut field_starter_packs: Option<i64> = None;
        let mut field_activity_subscription: Option<
            ActorDefsProfileAssociatedActivitySubscriptionCborView<'a>,
        > = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x63\x68\x61\x74", |decoder| {
            field_chat = Some(ActorDefsProfileAssociatedChatCborView::decode_cbor(
                decoder,
            )?);
            Ok(())
        })?;
        entries.try_field(b"\x64\x67\x65\x72\x6d", |decoder| {
            field_germ = Some(ActorDefsProfileAssociatedGermCborView::decode_cbor(
                decoder,
            )?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x6c\x69\x73\x74\x73", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_lists = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_lists = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(b"\x67\x6c\x61\x62\x65\x6c\x65\x72", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_labeler = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x68\x66\x65\x65\x64\x67\x65\x6e\x73", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_feedgens = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_feedgens = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(
            b"\x6c\x73\x74\x61\x72\x74\x65\x72\x50\x61\x63\x6b\x73",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_starter_packs = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_starter_packs = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x74\x61\x63\x74\x69\x76\x69\x74\x79\x53\x75\x62\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_activity_subscription = Some(
                    ActorDefsProfileAssociatedActivitySubscriptionCborView::decode_cbor(decoder)?,
                );
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"chat" => {
                    field_chat = Some(ActorDefsProfileAssociatedChatCborView::decode_cbor(
                        decoder,
                    )?);
                }
                b"germ" => {
                    field_germ = Some(ActorDefsProfileAssociatedGermCborView::decode_cbor(
                        decoder,
                    )?);
                }
                b"lists" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_lists = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_lists = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"labeler" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_labeler = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"feedgens" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_feedgens = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_feedgens = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"starterPacks" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_starter_packs = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_starter_packs = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"activitySubscription" => {
                    field_activity_subscription = Some(
                        ActorDefsProfileAssociatedActivitySubscriptionCborView::decode_cbor(
                            decoder,
                        )?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            chat: field_chat,
            germ: field_germ,
            lists: field_lists,
            labeler: field_labeler,
            feedgens: field_feedgens,
            starter_packs: field_starter_packs,
            activity_subscription: field_activity_subscription,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileAssociatedActivitySubscription object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileAssociatedActivitySubscription {
    pub allow_subscriptions: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileAssociatedActivitySubscription {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("allowSubscriptions")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.allow_subscriptions)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.allow_subscriptions)?;
                pairs.push(("allowSubscriptions", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_allow_subscriptions: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "allowSubscriptions" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_subscriptions = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileAssociatedActivitySubscription {
            allow_subscriptions: field_allow_subscriptions.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'allowSubscriptions'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_allow_subscriptions: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "allowSubscriptions" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_subscriptions = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileAssociatedActivitySubscription {
            allow_subscriptions: field_allow_subscriptions.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'allowSubscriptions'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileAssociatedActivitySubscriptionCborView<'a> {
    pub allow_subscriptions: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileAssociatedActivitySubscriptionCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(
        &self,
    ) -> Result<ActorDefsProfileAssociatedActivitySubscription, crate::cbor::CborError> {
        Ok(ActorDefsProfileAssociatedActivitySubscription {
            allow_subscriptions: self.allow_subscriptions.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_allow_subscriptions: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x72\x61\x6c\x6c\x6f\x77\x53\x75\x62\x73\x63\x72\x69\x70\x74\x69\x6f\x6e\x73",
            |decoder| {
                field_allow_subscriptions = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"allowSubscriptions" => {
                    field_allow_subscriptions = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            allow_subscriptions: field_allow_subscriptions.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'allowSubscriptions'".into(),
                )
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileAssociatedChat object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileAssociatedChat {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allow_group_invites: Option<String>,
    pub allow_incoming: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileAssociatedChat {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.allow_group_invites.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("allowIncoming")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.allow_incoming)?;
            if self.allow_group_invites.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("allowGroupInvites")?;
                if let Some(ref val) = self.allow_group_invites {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.allow_incoming)?;
                pairs.push(("allowIncoming", vbuf));
            }
            if self.allow_group_invites.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.allow_group_invites {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("allowGroupInvites", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_allow_incoming: Option<String> = None;
        let mut field_allow_group_invites: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "allowIncoming" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_incoming = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "allowGroupInvites" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_group_invites = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileAssociatedChat {
            allow_incoming: field_allow_incoming.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'allowIncoming'".into())
            })?,
            allow_group_invites: field_allow_group_invites,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_allow_incoming: Option<String> = None;
        let mut field_allow_group_invites: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "allowIncoming" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_incoming = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "allowGroupInvites" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_allow_group_invites = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileAssociatedChat {
            allow_incoming: field_allow_incoming.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'allowIncoming'".into())
            })?,
            allow_group_invites: field_allow_group_invites,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileAssociatedChatCborView<'a> {
    pub allow_incoming: &'a str,
    pub allow_group_invites: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileAssociatedChatCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileAssociatedChat, crate::cbor::CborError> {
        Ok(ActorDefsProfileAssociatedChat {
            allow_incoming: self.allow_incoming.to_owned(),
            allow_group_invites: self
                .allow_group_invites
                .as_ref()
                .map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_allow_incoming: Option<&'a str> = None;
        let mut field_allow_group_invites: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x6d\x61\x6c\x6c\x6f\x77\x49\x6e\x63\x6f\x6d\x69\x6e\x67",
            |decoder| {
                field_allow_incoming = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x71\x61\x6c\x6c\x6f\x77\x47\x72\x6f\x75\x70\x49\x6e\x76\x69\x74\x65\x73",
            |decoder| {
                field_allow_group_invites = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"allowIncoming" => {
                    field_allow_incoming = Some(decoder.text()?);
                }
                b"allowGroupInvites" => {
                    field_allow_group_invites = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            allow_incoming: field_allow_incoming.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'allowIncoming'".into())
            })?,
            allow_group_invites: field_allow_group_invites,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileAssociatedGerm object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileAssociatedGerm {
    pub message_me_url: String,
    pub show_button_to: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileAssociatedGerm {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 2u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("messageMeUrl")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.message_me_url)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("showButtonTo")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.show_button_to)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.message_me_url)?;
                pairs.push(("messageMeUrl", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.show_button_to)?;
                pairs.push(("showButtonTo", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_message_me_url: Option<String> = None;
        let mut field_show_button_to: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "messageMeUrl" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_message_me_url = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "showButtonTo" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_show_button_to = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileAssociatedGerm {
            message_me_url: field_message_me_url.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'messageMeUrl'".into())
            })?,
            show_button_to: field_show_button_to.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'showButtonTo'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_message_me_url: Option<String> = None;
        let mut field_show_button_to: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "messageMeUrl" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_message_me_url = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "showButtonTo" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_show_button_to = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileAssociatedGerm {
            message_me_url: field_message_me_url.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'messageMeUrl'".into())
            })?,
            show_button_to: field_show_button_to.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'showButtonTo'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileAssociatedGermCborView<'a> {
    pub message_me_url: &'a str,
    pub show_button_to: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileAssociatedGermCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileAssociatedGerm, crate::cbor::CborError> {
        Ok(ActorDefsProfileAssociatedGerm {
            message_me_url: self.message_me_url.to_owned(),
            show_button_to: self.show_button_to.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_message_me_url: Option<&'a str> = None;
        let mut field_show_button_to: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x6c\x6d\x65\x73\x73\x61\x67\x65\x4d\x65\x55\x72\x6c",
            |decoder| {
                field_message_me_url = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6c\x73\x68\x6f\x77\x42\x75\x74\x74\x6f\x6e\x54\x6f",
            |decoder| {
                field_show_button_to = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"messageMeUrl" => {
                    field_message_me_url = Some(decoder.text()?);
                }
                b"showButtonTo" => {
                    field_show_button_to = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            message_me_url: field_message_me_url.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'messageMeUrl'".into())
            })?,
            show_button_to: field_show_button_to.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'showButtonTo'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileView object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileView {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub associated: Option<ActorDefsProfileAssociated>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avatar: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<crate::syntax::Datetime>,
    /// Debug information for internal development
    #[serde(default, skip_serializing_if = "serde_json::Value::is_null")]
    pub debug: serde_json::Value,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    pub did: crate::syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    pub handle: crate::syntax::Handle,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub indexed_at: Option<crate::syntax::Datetime>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pronouns: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ActorDefsStatusView>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub verification: Option<ActorDefsVerificationState>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viewer: Option<ActorDefsViewerState>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileView {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.avatar.is_some() {
                count += 1;
            }
            if !self.labels.is_empty() {
                count += 1;
            }
            if self.status.is_some() {
                count += 1;
            }
            if self.viewer.is_some() {
                count += 1;
            }
            if self.pronouns.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.indexed_at.is_some() {
                count += 1;
            }
            if self.associated.is_some() {
                count += 1;
            }
            if self.description.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            if self.verification.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            if self.avatar.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("avatar")?;
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("handle")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.handle.as_str())?;
            if !self.labels.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(buf)?;
                }
            }
            if self.status.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
                if let Some(ref val) = self.status {
                    val.encode_cbor(buf)?;
                }
            }
            if self.viewer.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("viewer")?;
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(buf)?;
                }
            }
            if self.pronouns.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("pronouns")?;
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.created_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.indexed_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("indexedAt")?;
                if let Some(ref val) = self.indexed_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.associated.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("associated")?;
                if let Some(ref val) = self.associated {
                    val.encode_cbor(buf)?;
                }
            }
            if self.description.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.verification.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("verification")?;
                if let Some(ref val) = self.verification {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            if self.avatar.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("avatar", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.handle.as_str())?;
                pairs.push(("handle", vbuf));
            }
            if !self.labels.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            if self.status.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.status {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("status", vbuf));
            }
            if self.viewer.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("viewer", vbuf));
            }
            if self.pronouns.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("pronouns", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.indexed_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.indexed_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("indexedAt", vbuf));
            }
            if self.associated.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.associated {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("associated", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            if self.verification.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.verification {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("verification", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_indexed_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::decode_cbor(decoder)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::decode_cbor(decoder)?);
                }
                "pronouns" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "indexedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_indexed_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::decode_cbor(decoder)?);
                }
                "description" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::decode_cbor(decoder)?);
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileView {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            description: field_description,
            display_name: field_display_name,
            verification: field_verification,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_indexed_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::from_cbor_value(value)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::from_cbor_value(value)?);
                }
                "pronouns" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "indexedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_indexed_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::from_cbor_value(value)?);
                }
                "description" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::from_cbor_value(value)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileView {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            description: field_description,
            display_name: field_display_name,
            verification: field_verification,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileViewCborView<'a> {
    pub did: crate::syntax::Did,
    pub debug: serde_json::Value,
    pub avatar: Option<&'a str>,
    pub handle: crate::syntax::Handle,
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    pub status: Option<ActorDefsStatusViewCborView<'a>>,
    pub viewer: Option<ActorDefsViewerStateCborView<'a>>,
    pub pronouns: Option<&'a str>,
    pub created_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub indexed_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub associated: Option<ActorDefsProfileAssociatedCborView<'a>>,
    pub description: Option<&'a str>,
    pub display_name: Option<&'a str>,
    pub verification: Option<ActorDefsVerificationStateCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileViewCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileView, crate::cbor::CborError> {
        Ok(ActorDefsProfileView {
            did: self.did.clone(),
            debug: self.debug.clone(),
            avatar: self.avatar.as_ref().map(|value| (*value).to_owned()),
            handle: self.handle.clone(),
            labels: self.labels.clone(),
            status: self
                .status
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            viewer: self
                .viewer
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            pronouns: self.pronouns.as_ref().map(|value| (*value).to_owned()),
            created_at: self.created_at.as_ref().map(|value| (*value).to_owned()),
            indexed_at: self.indexed_at.as_ref().map(|value| (*value).to_owned()),
            associated: self
                .associated
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            description: self.description.as_ref().map(|value| (*value).to_owned()),
            display_name: self.display_name.as_ref().map(|value| (*value).to_owned()),
            verification: self
                .verification
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<&'a str> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusViewCborView<'a>> = None;
        let mut field_viewer: Option<ActorDefsViewerStateCborView<'a>> = None;
        let mut field_pronouns: Option<&'a str> = None;
        let mut field_created_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_indexed_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_associated: Option<ActorDefsProfileAssociatedCborView<'a>> = None;
        let mut field_description: Option<&'a str> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut field_verification: Option<ActorDefsVerificationStateCborView<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x64\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x61\x76\x61\x74\x61\x72", |decoder| {
            field_avatar = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x68\x61\x6e\x64\x6c\x65", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_handle = Some(
                    crate::syntax::Handle::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x6c\x61\x62\x65\x6c\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_labels.push(crate::api::com::atproto::LabelDefsLabel::from_cbor_value(
                        item,
                    )?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x76\x69\x65\x77\x65\x72", |decoder| {
            field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x70\x72\x6f\x6e\x6f\x75\x6e\x73", |decoder| {
            field_pronouns = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x72\x65\x61\x74\x65\x64\x41\x74", |decoder| {
            field_created_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x69\x69\x6e\x64\x65\x78\x65\x64\x41\x74", |decoder| {
            field_indexed_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x61\x73\x73\x6f\x63\x69\x61\x74\x65\x64", |decoder| {
            field_associated = Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x65\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_description = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6c\x76\x65\x72\x69\x66\x69\x63\x61\x74\x69\x6f\x6e",
            |decoder| {
                field_verification =
                    Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"avatar" => {
                    field_avatar = Some(decoder.text()?);
                }
                b"handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"status" => {
                    field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
                }
                b"viewer" => {
                    field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
                }
                b"pronouns" => {
                    field_pronouns = Some(decoder.text()?);
                }
                b"createdAt" => {
                    field_created_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"indexedAt" => {
                    field_indexed_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"associated" => {
                    field_associated =
                        Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
                }
                b"description" => {
                    field_description = Some(decoder.text()?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                b"verification" => {
                    field_verification =
                        Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            description: field_description,
            display_name: field_display_name,
            verification: field_verification,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileViewBasic object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileViewBasic {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub associated: Option<ActorDefsProfileAssociated>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avatar: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<crate::syntax::Datetime>,
    /// Debug information for internal development
    #[serde(default, skip_serializing_if = "serde_json::Value::is_null")]
    pub debug: serde_json::Value,
    pub did: crate::syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    pub handle: crate::syntax::Handle,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pronouns: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ActorDefsStatusView>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub verification: Option<ActorDefsVerificationState>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viewer: Option<ActorDefsViewerState>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileViewBasic {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.avatar.is_some() {
                count += 1;
            }
            if !self.labels.is_empty() {
                count += 1;
            }
            if self.status.is_some() {
                count += 1;
            }
            if self.viewer.is_some() {
                count += 1;
            }
            if self.pronouns.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.associated.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            if self.verification.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            if self.avatar.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("avatar")?;
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("handle")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.handle.as_str())?;
            if !self.labels.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(buf)?;
                }
            }
            if self.status.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
                if let Some(ref val) = self.status {
                    val.encode_cbor(buf)?;
                }
            }
            if self.viewer.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("viewer")?;
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(buf)?;
                }
            }
            if self.pronouns.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("pronouns")?;
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.created_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.associated.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("associated")?;
                if let Some(ref val) = self.associated {
                    val.encode_cbor(buf)?;
                }
            }
            if self.display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.verification.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("verification")?;
                if let Some(ref val) = self.verification {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            if self.avatar.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("avatar", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.handle.as_str())?;
                pairs.push(("handle", vbuf));
            }
            if !self.labels.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            if self.status.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.status {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("status", vbuf));
            }
            if self.viewer.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("viewer", vbuf));
            }
            if self.pronouns.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("pronouns", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.associated.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.associated {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("associated", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            if self.verification.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.verification {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("verification", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::decode_cbor(decoder)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::decode_cbor(decoder)?);
                }
                "pronouns" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::decode_cbor(decoder)?);
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::decode_cbor(decoder)?);
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileViewBasic {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            associated: field_associated,
            display_name: field_display_name,
            verification: field_verification,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::from_cbor_value(value)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::from_cbor_value(value)?);
                }
                "pronouns" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::from_cbor_value(value)?);
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::from_cbor_value(value)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileViewBasic {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            associated: field_associated,
            display_name: field_display_name,
            verification: field_verification,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileViewBasicCborView<'a> {
    pub did: crate::syntax::Did,
    pub debug: serde_json::Value,
    pub avatar: Option<&'a str>,
    pub handle: crate::syntax::Handle,
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    pub status: Option<ActorDefsStatusViewCborView<'a>>,
    pub viewer: Option<ActorDefsViewerStateCborView<'a>>,
    pub pronouns: Option<&'a str>,
    pub created_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub associated: Option<ActorDefsProfileAssociatedCborView<'a>>,
    pub display_name: Option<&'a str>,
    pub verification: Option<ActorDefsVerificationStateCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileViewBasicCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileViewBasic, crate::cbor::CborError> {
        Ok(ActorDefsProfileViewBasic {
            did: self.did.clone(),
            debug: self.debug.clone(),
            avatar: self.avatar.as_ref().map(|value| (*value).to_owned()),
            handle: self.handle.clone(),
            labels: self.labels.clone(),
            status: self
                .status
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            viewer: self
                .viewer
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            pronouns: self.pronouns.as_ref().map(|value| (*value).to_owned()),
            created_at: self.created_at.as_ref().map(|value| (*value).to_owned()),
            associated: self
                .associated
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            display_name: self.display_name.as_ref().map(|value| (*value).to_owned()),
            verification: self
                .verification
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<&'a str> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusViewCborView<'a>> = None;
        let mut field_viewer: Option<ActorDefsViewerStateCborView<'a>> = None;
        let mut field_pronouns: Option<&'a str> = None;
        let mut field_created_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_associated: Option<ActorDefsProfileAssociatedCborView<'a>> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut field_verification: Option<ActorDefsVerificationStateCborView<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x64\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x61\x76\x61\x74\x61\x72", |decoder| {
            field_avatar = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x68\x61\x6e\x64\x6c\x65", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_handle = Some(
                    crate::syntax::Handle::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x6c\x61\x62\x65\x6c\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_labels.push(crate::api::com::atproto::LabelDefsLabel::from_cbor_value(
                        item,
                    )?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x76\x69\x65\x77\x65\x72", |decoder| {
            field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x70\x72\x6f\x6e\x6f\x75\x6e\x73", |decoder| {
            field_pronouns = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x72\x65\x61\x74\x65\x64\x41\x74", |decoder| {
            field_created_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x61\x73\x73\x6f\x63\x69\x61\x74\x65\x64", |decoder| {
            field_associated = Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6c\x76\x65\x72\x69\x66\x69\x63\x61\x74\x69\x6f\x6e",
            |decoder| {
                field_verification =
                    Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"avatar" => {
                    field_avatar = Some(decoder.text()?);
                }
                b"handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"status" => {
                    field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
                }
                b"viewer" => {
                    field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
                }
                b"pronouns" => {
                    field_pronouns = Some(decoder.text()?);
                }
                b"createdAt" => {
                    field_created_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"associated" => {
                    field_associated =
                        Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                b"verification" => {
                    field_verification =
                        Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            pronouns: field_pronouns,
            created_at: field_created_at,
            associated: field_associated,
            display_name: field_display_name,
            verification: field_verification,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsProfileViewDetailed object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsProfileViewDetailed {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub associated: Option<ActorDefsProfileAssociated>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avatar: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub banner: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_at: Option<crate::syntax::Datetime>,
    /// Debug information for internal development
    #[serde(default, skip_serializing_if = "serde_json::Value::is_null")]
    pub debug: serde_json::Value,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    pub did: crate::syntax::Did,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub followers_count: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub follows_count: Option<i64>,
    pub handle: crate::syntax::Handle,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub indexed_at: Option<crate::syntax::Datetime>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub joined_via_starter_pack: Option<crate::api::app::bsky::GraphDefsStarterPackViewBasic>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_post: Option<crate::api::com::atproto::RepoStrongRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub posts_count: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pronouns: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ActorDefsStatusView>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub verification: Option<ActorDefsVerificationState>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viewer: Option<ActorDefsViewerState>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub website: Option<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsProfileViewDetailed {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.avatar.is_some() {
                count += 1;
            }
            if self.banner.is_some() {
                count += 1;
            }
            if !self.labels.is_empty() {
                count += 1;
            }
            if self.status.is_some() {
                count += 1;
            }
            if self.viewer.is_some() {
                count += 1;
            }
            if self.website.is_some() {
                count += 1;
            }
            if self.pronouns.is_some() {
                count += 1;
            }
            if self.created_at.is_some() {
                count += 1;
            }
            if self.indexed_at.is_some() {
                count += 1;
            }
            if self.associated.is_some() {
                count += 1;
            }
            if self.pinned_post.is_some() {
                count += 1;
            }
            if self.posts_count.is_some() {
                count += 1;
            }
            if self.description.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            if self.follows_count.is_some() {
                count += 1;
            }
            if self.verification.is_some() {
                count += 1;
            }
            if self.followers_count.is_some() {
                count += 1;
            }
            if self.joined_via_starter_pack.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
            if self.avatar.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("avatar")?;
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.banner.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("banner")?;
                if let Some(ref val) = self.banner {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("handle")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.handle.as_str())?;
            if !self.labels.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(buf)?;
                }
            }
            if self.status.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
                if let Some(ref val) = self.status {
                    val.encode_cbor(buf)?;
                }
            }
            if self.viewer.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("viewer")?;
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(buf)?;
                }
            }
            if self.website.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("website")?;
                if let Some(ref val) = self.website {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.pronouns.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("pronouns")?;
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.created_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.indexed_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("indexedAt")?;
                if let Some(ref val) = self.indexed_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.associated.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("associated")?;
                if let Some(ref val) = self.associated {
                    val.encode_cbor(buf)?;
                }
            }
            if self.pinned_post.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("pinnedPost")?;
                if let Some(ref val) = self.pinned_post {
                    val.encode_cbor(buf)?;
                }
            }
            if self.posts_count.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("postsCount")?;
                if let Some(ref val) = self.posts_count {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.description.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.follows_count.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("followsCount")?;
                if let Some(ref val) = self.follows_count {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.verification.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("verification")?;
                if let Some(ref val) = self.verification {
                    val.encode_cbor(buf)?;
                }
            }
            if self.followers_count.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("followersCount")?;
                if let Some(ref val) = self.followers_count {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.joined_via_starter_pack.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("joinedViaStarterPack")?;
                if let Some(ref val) = self.joined_via_starter_pack {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            if self.avatar.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.avatar {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("avatar", vbuf));
            }
            if self.banner.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.banner {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("banner", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.handle.as_str())?;
                pairs.push(("handle", vbuf));
            }
            if !self.labels.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            if self.status.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.status {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("status", vbuf));
            }
            if self.viewer.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.viewer {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("viewer", vbuf));
            }
            if self.website.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.website {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("website", vbuf));
            }
            if self.pronouns.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pronouns {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("pronouns", vbuf));
            }
            if self.created_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.created_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("createdAt", vbuf));
            }
            if self.indexed_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.indexed_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("indexedAt", vbuf));
            }
            if self.associated.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.associated {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("associated", vbuf));
            }
            if self.pinned_post.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.pinned_post {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("pinnedPost", vbuf));
            }
            if self.posts_count.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.posts_count {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("postsCount", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            if self.follows_count.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.follows_count {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("followsCount", vbuf));
            }
            if self.verification.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.verification {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("verification", vbuf));
            }
            if self.followers_count.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.followers_count {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("followersCount", vbuf));
            }
            if self.joined_via_starter_pack.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.joined_via_starter_pack {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("joinedViaStarterPack", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_banner: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_website: Option<String> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_indexed_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_pinned_post: Option<crate::api::com::atproto::RepoStrongRef> = None;
        let mut field_posts_count: Option<i64> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_follows_count: Option<i64> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut field_followers_count: Option<i64> = None;
        let mut field_joined_via_starter_pack: Option<
            crate::api::app::bsky::GraphDefsStarterPackViewBasic,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "banner" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_banner = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::decode_cbor(decoder)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::decode_cbor(decoder)?);
                }
                "website" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_website = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "pronouns" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "indexedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_indexed_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::decode_cbor(decoder)?);
                }
                "pinnedPost" => {
                    field_pinned_post = Some(crate::api::com::atproto::RepoStrongRef::decode_cbor(
                        decoder,
                    )?);
                }
                "postsCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_posts_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_posts_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "description" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "followsCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_follows_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_follows_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::decode_cbor(decoder)?);
                }
                "followersCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_followers_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_followers_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "joinedViaStarterPack" => {
                    field_joined_via_starter_pack = Some(
                        crate::api::app::bsky::GraphDefsStarterPackViewBasic::decode_cbor(decoder)?,
                    );
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsProfileViewDetailed {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            banner: field_banner,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            website: field_website,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            pinned_post: field_pinned_post,
            posts_count: field_posts_count,
            description: field_description,
            display_name: field_display_name,
            follows_count: field_follows_count,
            verification: field_verification,
            followers_count: field_followers_count,
            joined_via_starter_pack: field_joined_via_starter_pack,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<String> = None;
        let mut field_banner: Option<String> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusView> = None;
        let mut field_viewer: Option<ActorDefsViewerState> = None;
        let mut field_website: Option<String> = None;
        let mut field_pronouns: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_indexed_at: Option<crate::syntax::Datetime> = None;
        let mut field_associated: Option<ActorDefsProfileAssociated> = None;
        let mut field_pinned_post: Option<crate::api::com::atproto::RepoStrongRef> = None;
        let mut field_posts_count: Option<i64> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut field_follows_count: Option<i64> = None;
        let mut field_verification: Option<ActorDefsVerificationState> = None;
        let mut field_followers_count: Option<i64> = None;
        let mut field_joined_via_starter_pack: Option<
            crate::api::app::bsky::GraphDefsStarterPackViewBasic,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "avatar" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_avatar = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "banner" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_banner = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "handle" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "labels" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    field_status = Some(ActorDefsStatusView::from_cbor_value(value)?);
                }
                "viewer" => {
                    field_viewer = Some(ActorDefsViewerState::from_cbor_value(value)?);
                }
                "website" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_website = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "pronouns" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_pronouns = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "indexedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_indexed_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "associated" => {
                    field_associated = Some(ActorDefsProfileAssociated::from_cbor_value(value)?);
                }
                "pinnedPost" => {
                    field_pinned_post = Some(
                        crate::api::com::atproto::RepoStrongRef::from_cbor_value(value)?,
                    );
                }
                "postsCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_posts_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_posts_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "description" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "followsCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_follows_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_follows_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "verification" => {
                    field_verification = Some(ActorDefsVerificationState::from_cbor_value(value)?);
                }
                "followersCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_followers_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_followers_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "joinedViaStarterPack" => {
                    field_joined_via_starter_pack = Some(
                        crate::api::app::bsky::GraphDefsStarterPackViewBasic::from_cbor_value(
                            value,
                        )?,
                    );
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsProfileViewDetailed {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            banner: field_banner,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            website: field_website,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            pinned_post: field_pinned_post,
            posts_count: field_posts_count,
            description: field_description,
            display_name: field_display_name,
            follows_count: field_follows_count,
            verification: field_verification,
            followers_count: field_followers_count,
            joined_via_starter_pack: field_joined_via_starter_pack,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsProfileViewDetailedCborView<'a> {
    pub did: crate::syntax::Did,
    pub debug: serde_json::Value,
    pub avatar: Option<&'a str>,
    pub banner: Option<&'a str>,
    pub handle: crate::syntax::Handle,
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    pub status: Option<ActorDefsStatusViewCborView<'a>>,
    pub viewer: Option<ActorDefsViewerStateCborView<'a>>,
    pub website: Option<&'a str>,
    pub pronouns: Option<&'a str>,
    pub created_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub indexed_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub associated: Option<ActorDefsProfileAssociatedCborView<'a>>,
    pub pinned_post: Option<crate::api::com::atproto::RepoStrongRefCborView<'a>>,
    pub posts_count: Option<i64>,
    pub description: Option<&'a str>,
    pub display_name: Option<&'a str>,
    pub follows_count: Option<i64>,
    pub verification: Option<ActorDefsVerificationStateCborView<'a>>,
    pub followers_count: Option<i64>,
    pub joined_via_starter_pack:
        Option<crate::api::app::bsky::GraphDefsStarterPackViewBasicCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsProfileViewDetailedCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsProfileViewDetailed, crate::cbor::CborError> {
        Ok(ActorDefsProfileViewDetailed {
            did: self.did.clone(),
            debug: self.debug.clone(),
            avatar: self.avatar.as_ref().map(|value| (*value).to_owned()),
            banner: self.banner.as_ref().map(|value| (*value).to_owned()),
            handle: self.handle.clone(),
            labels: self.labels.clone(),
            status: self
                .status
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            viewer: self
                .viewer
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            website: self.website.as_ref().map(|value| (*value).to_owned()),
            pronouns: self.pronouns.as_ref().map(|value| (*value).to_owned()),
            created_at: self.created_at.as_ref().map(|value| (*value).to_owned()),
            indexed_at: self.indexed_at.as_ref().map(|value| (*value).to_owned()),
            associated: self
                .associated
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            pinned_post: self
                .pinned_post
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            posts_count: self.posts_count,
            description: self.description.as_ref().map(|value| (*value).to_owned()),
            display_name: self.display_name.as_ref().map(|value| (*value).to_owned()),
            follows_count: self.follows_count,
            verification: self
                .verification
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            followers_count: self.followers_count,
            joined_via_starter_pack: self
                .joined_via_starter_pack
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut field_avatar: Option<&'a str> = None;
        let mut field_banner: Option<&'a str> = None;
        let mut field_handle: Option<crate::syntax::Handle> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<ActorDefsStatusViewCborView<'a>> = None;
        let mut field_viewer: Option<ActorDefsViewerStateCborView<'a>> = None;
        let mut field_website: Option<&'a str> = None;
        let mut field_pronouns: Option<&'a str> = None;
        let mut field_created_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_indexed_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_associated: Option<ActorDefsProfileAssociatedCborView<'a>> = None;
        let mut field_pinned_post: Option<crate::api::com::atproto::RepoStrongRefCborView<'a>> =
            None;
        let mut field_posts_count: Option<i64> = None;
        let mut field_description: Option<&'a str> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut field_follows_count: Option<i64> = None;
        let mut field_verification: Option<ActorDefsVerificationStateCborView<'a>> = None;
        let mut field_followers_count: Option<i64> = None;
        let mut field_joined_via_starter_pack: Option<
            crate::api::app::bsky::GraphDefsStarterPackViewBasicCborView<'a>,
        > = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x64\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x61\x76\x61\x74\x61\x72", |decoder| {
            field_avatar = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x62\x61\x6e\x6e\x65\x72", |decoder| {
            field_banner = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x68\x61\x6e\x64\x6c\x65", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_handle = Some(
                    crate::syntax::Handle::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x6c\x61\x62\x65\x6c\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_labels.push(crate::api::com::atproto::LabelDefsLabel::from_cbor_value(
                        item,
                    )?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x76\x69\x65\x77\x65\x72", |decoder| {
            field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x67\x77\x65\x62\x73\x69\x74\x65", |decoder| {
            field_website = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x70\x72\x6f\x6e\x6f\x75\x6e\x73", |decoder| {
            field_pronouns = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x72\x65\x61\x74\x65\x64\x41\x74", |decoder| {
            field_created_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x69\x69\x6e\x64\x65\x78\x65\x64\x41\x74", |decoder| {
            field_indexed_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x61\x73\x73\x6f\x63\x69\x61\x74\x65\x64", |decoder| {
            field_associated = Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x6a\x70\x69\x6e\x6e\x65\x64\x50\x6f\x73\x74", |decoder| {
            field_pinned_post =
                Some(crate::api::com::atproto::RepoStrongRefCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x6a\x70\x6f\x73\x74\x73\x43\x6f\x75\x6e\x74", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_posts_count = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_posts_count = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x65\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_description = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6c\x66\x6f\x6c\x6c\x6f\x77\x73\x43\x6f\x75\x6e\x74",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_follows_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_follows_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6c\x76\x65\x72\x69\x66\x69\x63\x61\x74\x69\x6f\x6e",
            |decoder| {
                field_verification =
                    Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6e\x66\x6f\x6c\x6c\x6f\x77\x65\x72\x73\x43\x6f\x75\x6e\x74",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_followers_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_followers_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x74\x6a\x6f\x69\x6e\x65\x64\x56\x69\x61\x53\x74\x61\x72\x74\x65\x72\x50\x61\x63\x6b",
            |decoder| {
                field_joined_via_starter_pack = Some(
                    crate::api::app::bsky::GraphDefsStarterPackViewBasicCborView::decode_cbor(
                        decoder,
                    )?,
                );
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"avatar" => {
                    field_avatar = Some(decoder.text()?);
                }
                b"banner" => {
                    field_banner = Some(decoder.text()?);
                }
                b"handle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"status" => {
                    field_status = Some(ActorDefsStatusViewCborView::decode_cbor(decoder)?);
                }
                b"viewer" => {
                    field_viewer = Some(ActorDefsViewerStateCborView::decode_cbor(decoder)?);
                }
                b"website" => {
                    field_website = Some(decoder.text()?);
                }
                b"pronouns" => {
                    field_pronouns = Some(decoder.text()?);
                }
                b"createdAt" => {
                    field_created_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"indexedAt" => {
                    field_indexed_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"associated" => {
                    field_associated =
                        Some(ActorDefsProfileAssociatedCborView::decode_cbor(decoder)?);
                }
                b"pinnedPost" => {
                    field_pinned_post = Some(
                        crate::api::com::atproto::RepoStrongRefCborView::decode_cbor(decoder)?,
                    );
                }
                b"postsCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_posts_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_posts_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"description" => {
                    field_description = Some(decoder.text()?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                b"followsCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_follows_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_follows_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"verification" => {
                    field_verification =
                        Some(ActorDefsVerificationStateCborView::decode_cbor(decoder)?);
                }
                b"followersCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_followers_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_followers_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"joinedViaStarterPack" => {
                    field_joined_via_starter_pack = Some(
                        crate::api::app::bsky::GraphDefsStarterPackViewBasicCborView::decode_cbor(
                            decoder,
                        )?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            debug: Default::default(),
            avatar: field_avatar,
            banner: field_banner,
            handle: field_handle.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'handle'".into())
            })?,
            labels: field_labels,
            status: field_status,
            viewer: field_viewer,
            website: field_website,
            pronouns: field_pronouns,
            created_at: field_created_at,
            indexed_at: field_indexed_at,
            associated: field_associated,
            pinned_post: field_pinned_post,
            posts_count: field_posts_count,
            description: field_description,
            display_name: field_display_name,
            follows_count: field_follows_count,
            verification: field_verification,
            followers_count: field_followers_count,
            joined_via_starter_pack: field_joined_via_starter_pack,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsSavedFeed object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsSavedFeed {
    pub id: String,
    pub pinned: bool,
    pub r#type: String,
    pub value: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsSavedFeed {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 4u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("id")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.id)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("type")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.r#type)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("value")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.value)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("pinned")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.pinned)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.id)?;
                pairs.push(("id", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.r#type)?;
                pairs.push(("type", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.value)?;
                pairs.push(("value", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.pinned)?;
                pairs.push(("pinned", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_id: Option<String> = None;
        let mut field_type: Option<String> = None;
        let mut field_value: Option<String> = None;
        let mut field_pinned: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "id" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "type" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_type = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "value" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_value = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "pinned" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_pinned = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsSavedFeed {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            r#type: field_type.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'type'".into())
            })?,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            pinned: field_pinned.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'pinned'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_id: Option<String> = None;
        let mut field_type: Option<String> = None;
        let mut field_value: Option<String> = None;
        let mut field_pinned: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "id" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "type" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_type = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "value" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_value = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "pinned" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_pinned = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsSavedFeed {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            r#type: field_type.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'type'".into())
            })?,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            pinned: field_pinned.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'pinned'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsSavedFeedCborView<'a> {
    pub id: &'a str,
    pub r#type: &'a str,
    pub value: &'a str,
    pub pinned: bool,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsSavedFeedCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsSavedFeed, crate::cbor::CborError> {
        Ok(ActorDefsSavedFeed {
            id: self.id.to_owned(),
            r#type: self.r#type.to_owned(),
            value: self.value.to_owned(),
            pinned: self.pinned,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_id: Option<&'a str> = None;
        let mut field_type: Option<&'a str> = None;
        let mut field_value: Option<&'a str> = None;
        let mut field_pinned: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x62\x69\x64", |decoder| {
            field_id = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x64\x74\x79\x70\x65", |decoder| {
            field_type = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x76\x61\x6c\x75\x65", |decoder| {
            field_value = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x70\x69\x6e\x6e\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_pinned = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"id" => {
                    field_id = Some(decoder.text()?);
                }
                b"type" => {
                    field_type = Some(decoder.text()?);
                }
                b"value" => {
                    field_value = Some(decoder.text()?);
                }
                b"pinned" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_pinned = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            id: field_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'id'".into())
            })?,
            r#type: field_type.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'type'".into())
            })?,
            value: field_value.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'value'".into())
            })?,
            pinned: field_pinned.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'pinned'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsSavedFeedsPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsSavedFeedsPref {
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub pinned: Vec<crate::syntax::AtUri>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub saved: Vec<crate::syntax::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeline_index: Option<i64>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsSavedFeedsPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.timeline_index.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("saved")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.saved.len() as u64)?;
            for item in &self.saved {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item.as_str())?;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("pinned")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.pinned.len() as u64)?;
            for item in &self.pinned {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item.as_str())?;
            }
            if self.timeline_index.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("timelineIndex")?;
                if let Some(ref val) = self.timeline_index {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.saved.len() as u64)?;
                for item in &self.saved {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item.as_str())?;
                }
                pairs.push(("saved", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.pinned.len() as u64)?;
                for item in &self.pinned {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item.as_str())?;
                }
                pairs.push(("pinned", vbuf));
            }
            if self.timeline_index.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.timeline_index {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("timelineIndex", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_saved: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_pinned: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_timeline_index: Option<i64> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "saved" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_saved.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "pinned" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_pinned.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "timelineIndex" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_timeline_index = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_timeline_index = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsSavedFeedsPref {
            saved: field_saved,
            pinned: field_pinned,
            timeline_index: field_timeline_index,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_saved: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_pinned: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_timeline_index: Option<i64> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "saved" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_saved.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "pinned" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_pinned.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "timelineIndex" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_timeline_index = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_timeline_index = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsSavedFeedsPref {
            saved: field_saved,
            pinned: field_pinned,
            timeline_index: field_timeline_index,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsSavedFeedsPrefCborView<'a> {
    pub saved: Vec<crate::syntax::AtUri>,
    pub pinned: Vec<crate::syntax::AtUri>,
    pub timeline_index: Option<i64>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsSavedFeedsPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsSavedFeedsPref, crate::cbor::CborError> {
        Ok(ActorDefsSavedFeedsPref {
            saved: self.saved.clone(),
            pinned: self.pinned.clone(),
            timeline_index: self.timeline_index,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_saved: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_pinned: Vec<crate::syntax::AtUri> = Vec::new();
        let mut field_timeline_index: Option<i64> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x73\x61\x76\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_saved.push(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x70\x69\x6e\x6e\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_pinned.push(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(
            b"\x6d\x74\x69\x6d\x65\x6c\x69\x6e\x65\x49\x6e\x64\x65\x78",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_timeline_index = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_timeline_index = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"saved" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_saved.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"pinned" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_pinned.push(crate::syntax::AtUri::try_from(s).map_err(
                                    |e| crate::cbor::CborError::InvalidCbor(e.to_string()),
                                )?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"timelineIndex" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_timeline_index = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_timeline_index = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            saved: field_saved,
            pinned: field_pinned,
            timeline_index: field_timeline_index,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsSavedFeedsPrefV2 object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsSavedFeedsPrefV2 {
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub items: Vec<ActorDefsSavedFeed>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsSavedFeedsPrefV2 {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("items")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.items.len() as u64)?;
            for item in &self.items {
                item.encode_cbor(buf)?;
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.items.len() as u64)?;
                for item in &self.items {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("items", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_items: Vec<ActorDefsSavedFeed> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsSavedFeed::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsSavedFeedsPrefV2 {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_items: Vec<ActorDefsSavedFeed> = Vec::new();
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "items" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsSavedFeed::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsSavedFeedsPrefV2 {
            items: field_items,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsSavedFeedsPrefV2CborView<'a> {
    pub items: Vec<ActorDefsSavedFeed>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsSavedFeedsPrefV2CborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsSavedFeedsPrefV2, crate::cbor::CborError> {
        Ok(ActorDefsSavedFeedsPrefV2 {
            items: self.items.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_items: Vec<ActorDefsSavedFeed> = Vec::new();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x69\x74\x65\x6d\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_items.push(ActorDefsSavedFeed::from_cbor_value(item)?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"items" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_items.push(ActorDefsSavedFeed::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            items: field_items,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsStatusView object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsStatusView {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cid: Option<String>,
    /// An optional embed associated with the status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embed: Option<ActorDefsStatusViewEmbedUnion>,
    /// The date when this status will expire. The application might choose to no longer return the status after expiration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<crate::syntax::Datetime>,
    /// True if the status is not expired, false if it is expired. Only present if expiration was set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_active: Option<bool>,
    /// True if the user's go-live access has been disabled by a moderator, false otherwise.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_disabled: Option<bool>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    pub record: serde_json::Value,
    /// The status for the account.
    pub status: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uri: Option<crate::syntax::AtUri>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

/// An optional embed associated with the status.
#[derive(Debug, Clone)]
pub enum ActorDefsStatusViewEmbedUnion {
    EmbedExternalView(Box<crate::api::app::bsky::EmbedExternalView>),
    Unknown(crate::api::UnknownUnionVariant),
}

impl serde::Serialize for ActorDefsStatusViewEmbedUnion {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        match self {
            ActorDefsStatusViewEmbedUnion::EmbedExternalView(inner) => {
                let mut map =
                    serde_json::to_value(inner.as_ref()).map_err(serde::ser::Error::custom)?;
                if let serde_json::Value::Object(ref mut m) = map {
                    m.insert(
                        "$type".to_string(),
                        serde_json::Value::String("app.bsky.embed.external#view".to_string()),
                    );
                }
                map.serialize(serializer)
            }
            ActorDefsStatusViewEmbedUnion::Unknown(v) => {
                if let Some(ref j) = v.json {
                    j.serialize(serializer)
                } else {
                    Err(serde::ser::Error::custom(
                        "no JSON data for unknown union variant",
                    ))
                }
            }
        }
    }
}

impl<'de> serde::Deserialize<'de> for ActorDefsStatusViewEmbedUnion {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = serde_json::Value::deserialize(deserializer)?;
        let type_str = value
            .get("$type")
            .and_then(|v| v.as_str())
            .unwrap_or_default();
        match type_str {
            "app.bsky.embed.external#view" => {
                let inner: crate::api::app::bsky::EmbedExternalView =
                    serde_json::from_value(value).map_err(serde::de::Error::custom)?;
                Ok(ActorDefsStatusViewEmbedUnion::EmbedExternalView(Box::new(
                    inner,
                )))
            }
            _ => Ok(ActorDefsStatusViewEmbedUnion::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: Some(value),
                    cbor: None,
                },
            )),
        }
    }
}

impl ActorDefsStatusViewEmbedUnion {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        match self {
            ActorDefsStatusViewEmbedUnion::EmbedExternalView(inner) => inner.encode_cbor(buf),
            ActorDefsStatusViewEmbedUnion::Unknown(v) => {
                if let Some(ref data) = v.cbor {
                    buf.extend_from_slice(data);
                    Ok(())
                } else {
                    Err(crate::cbor::CborError::InvalidCbor(
                        "no CBOR data for unknown union variant".into(),
                    ))
                }
            }
        }
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        // Save position, decode the value, look for $type key.
        let start = decoder.position();
        let val = decoder.decode()?;
        let end = decoder.position();
        let raw = &decoder.raw_input()[start..end];
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => {
                return Err(crate::cbor::CborError::InvalidCbor(
                    "expected map for union".into(),
                ));
            }
        };
        let type_str = entries
            .iter()
            .find(|(k, _)| *k == "$type")
            .and_then(|(_, v)| match v {
                crate::cbor::Value::Text(s) => Some(*s),
                _ => None,
            })
            .unwrap_or_default();
        match type_str {
            "app.bsky.embed.external#view" => {
                let mut dec = crate::cbor::Decoder::new(raw);
                let inner = crate::api::app::bsky::EmbedExternalView::decode_cbor(&mut dec)?;
                Ok(ActorDefsStatusViewEmbedUnion::EmbedExternalView(Box::new(
                    inner,
                )))
            }
            _ => Ok(ActorDefsStatusViewEmbedUnion::Unknown(
                crate::api::UnknownUnionVariant {
                    r#type: type_str.to_string(),
                    json: None,
                    cbor: Some(raw.to_vec()),
                },
            )),
        }
    }
}

impl ActorDefsStatusView {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.cid.is_some() {
                count += 1;
            }
            if self.uri.is_some() {
                count += 1;
            }
            if self.embed.is_some() {
                count += 1;
            }
            if !self.labels.is_empty() {
                count += 1;
            }
            if self.is_active.is_some() {
                count += 1;
            }
            if self.expires_at.is_some() {
                count += 1;
            }
            if self.is_disabled.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.cid.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("cid")?;
                if let Some(ref val) = self.cid {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.uri.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("uri")?;
                if let Some(ref val) = self.uri {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.embed.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("embed")?;
                if let Some(ref val) = self.embed {
                    val.encode_cbor(buf)?;
                }
            }
            if !self.labels.is_empty() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("labels")?;
                crate::cbor::Encoder::new(&mut *buf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(buf)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.status)?;
            if self.is_active.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isActive")?;
                if let Some(ref val) = self.is_active {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.expires_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("expiresAt")?;
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.is_disabled.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("isDisabled")?;
                if let Some(ref val) = self.is_disabled {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.cid.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.cid {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("cid", vbuf));
            }
            if self.uri.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.uri {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("uri", vbuf));
            }
            if self.embed.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.embed {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("embed", vbuf));
            }
            if !self.labels.is_empty() {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.labels.len() as u64)?;
                for item in &self.labels {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("labels", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.status)?;
                pairs.push(("status", vbuf));
            }
            if self.is_active.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_active {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isActive", vbuf));
            }
            if self.expires_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.expires_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("expiresAt", vbuf));
            }
            if self.is_disabled.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.is_disabled {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("isDisabled", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_cid: Option<String> = None;
        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut field_embed: Option<ActorDefsStatusViewEmbedUnion> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<String> = None;
        let mut field_is_active: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut field_is_disabled: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "cid" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_cid = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "uri" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "embed" => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_embed = Some(ActorDefsStatusViewEmbedUnion::decode_cbor(&mut dec)?);
                }
                "labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isActive" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_active = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "expiresAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isDisabled" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_disabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsStatusView {
            cid: field_cid,
            uri: field_uri,
            embed: field_embed,
            labels: field_labels,
            record: Default::default(),
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            is_active: field_is_active,
            expires_at: field_expires_at,
            is_disabled: field_is_disabled,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_cid: Option<String> = None;
        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut field_embed: Option<ActorDefsStatusViewEmbedUnion> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<String> = None;
        let mut field_is_active: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::Datetime> = None;
        let mut field_is_disabled: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "cid" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_cid = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "uri" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "embed" => {
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_embed = Some(ActorDefsStatusViewEmbedUnion::decode_cbor(&mut dec)?);
                }
                "labels" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isActive" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_active = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "expiresAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_expires_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isDisabled" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_disabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsStatusView {
            cid: field_cid,
            uri: field_uri,
            embed: field_embed,
            labels: field_labels,
            record: Default::default(),
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            is_active: field_is_active,
            expires_at: field_expires_at,
            is_disabled: field_is_disabled,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsStatusViewCborView<'a> {
    pub cid: Option<&'a str>,
    pub uri: Option<crate::syntax::AtUriRef<'a>>,
    pub embed: Option<ActorDefsStatusViewEmbedUnion>,
    pub labels: Vec<crate::api::com::atproto::LabelDefsLabel>,
    pub record: serde_json::Value,
    pub status: &'a str,
    pub is_active: Option<bool>,
    pub expires_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub is_disabled: Option<bool>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsStatusViewCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsStatusView, crate::cbor::CborError> {
        Ok(ActorDefsStatusView {
            cid: self.cid.as_ref().map(|value| (*value).to_owned()),
            uri: self.uri.as_ref().map(|value| (*value).to_owned()),
            embed: self.embed.clone(),
            labels: self.labels.clone(),
            record: self.record.clone(),
            status: self.status.to_owned(),
            is_active: self.is_active,
            expires_at: self.expires_at.as_ref().map(|value| (*value).to_owned()),
            is_disabled: self.is_disabled,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_cid: Option<&'a str> = None;
        let mut field_uri: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut field_embed: Option<ActorDefsStatusViewEmbedUnion> = None;
        let mut field_labels: Vec<crate::api::com::atproto::LabelDefsLabel> = Vec::new();
        let mut field_status: Option<&'a str> = None;
        let mut field_is_active: Option<bool> = None;
        let mut field_expires_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_is_disabled: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x63\x69\x64", |decoder| {
            field_cid = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x63\x75\x72\x69", |decoder| {
            field_uri = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x65\x65\x6d\x62\x65\x64", |decoder| {
            let value = decoder.decode()?;
            let raw = crate::cbor::encode_value(&value)?;
            let mut dec = crate::cbor::Decoder::new(&raw);
            field_embed = Some(ActorDefsStatusViewEmbedUnion::decode_cbor(&mut dec)?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x6c\x61\x62\x65\x6c\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_labels.push(crate::api::com::atproto::LabelDefsLabel::from_cbor_value(
                        item,
                    )?);
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x69\x73\x41\x63\x74\x69\x76\x65", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_is_active = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x65\x78\x70\x69\x72\x65\x73\x41\x74", |decoder| {
            field_expires_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x69\x73\x44\x69\x73\x61\x62\x6c\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_is_disabled = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"cid" => {
                    field_cid = Some(decoder.text()?);
                }
                b"uri" => {
                    field_uri = Some(
                        crate::syntax::AtUriRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"embed" => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    let mut dec = crate::cbor::Decoder::new(&raw);
                    field_embed = Some(ActorDefsStatusViewEmbedUnion::decode_cbor(&mut dec)?);
                }
                b"labels" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_labels.push(
                                crate::api::com::atproto::LabelDefsLabel::from_cbor_value(item)?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"status" => {
                    field_status = Some(decoder.text()?);
                }
                b"isActive" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_active = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"expiresAt" => {
                    field_expires_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"isDisabled" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_disabled = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            cid: field_cid,
            uri: field_uri,
            embed: field_embed,
            labels: field_labels,
            record: Default::default(),
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            is_active: field_is_active,
            expires_at: field_expires_at,
            is_disabled: field_is_disabled,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsThreadViewPref object from app.bsky.actor.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsThreadViewPref {
    /// Sorting mode for threads.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sort: Option<String>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsThreadViewPref {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.sort.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.sort.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("sort")?;
                if let Some(ref val) = self.sort {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.sort.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.sort {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("sort", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_sort: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "sort" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_sort = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsThreadViewPref {
            sort: field_sort,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_sort: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "sort" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_sort = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsThreadViewPref {
            sort: field_sort,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsThreadViewPrefCborView<'a> {
    pub sort: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsThreadViewPrefCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsThreadViewPref, crate::cbor::CborError> {
        Ok(ActorDefsThreadViewPref {
            sort: self.sort.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_sort: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x73\x6f\x72\x74", |decoder| {
            field_sort = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"sort" => {
                    field_sort = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            sort: field_sort,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsVerificationPrefs — Preferences for how verified accounts appear in the app.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsVerificationPrefs {
    /// Hide the blue check badges for verified accounts and trusted verifiers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hide_badges: Option<bool>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsVerificationPrefs {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.hide_badges.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.hide_badges.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("hideBadges")?;
                if let Some(ref val) = self.hide_badges {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.hide_badges.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.hide_badges {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("hideBadges", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_hide_badges: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "hideBadges" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_badges = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsVerificationPrefs {
            hide_badges: field_hide_badges,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_hide_badges: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "hideBadges" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_badges = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsVerificationPrefs {
            hide_badges: field_hide_badges,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsVerificationPrefsCborView<'a> {
    pub hide_badges: Option<bool>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsVerificationPrefsCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsVerificationPrefs, crate::cbor::CborError> {
        Ok(ActorDefsVerificationPrefs {
            hide_badges: self.hide_badges,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_hide_badges: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x6a\x68\x69\x64\x65\x42\x61\x64\x67\x65\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_hide_badges = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"hideBadges" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hide_badges = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            hide_badges: field_hide_badges,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsVerificationState — Represents the verification information about the user this object is attached to.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsVerificationState {
    /// The user's status as a trusted verifier.
    pub trusted_verifier_status: String,
    /// All verifications issued by trusted verifiers on behalf of this user. Verifications by untrusted verifiers are not included.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub verifications: Vec<ActorDefsVerificationView>,
    /// The user's status as a verified account.
    pub verified_status: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsVerificationState {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 3u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("verifications")?;
            crate::cbor::Encoder::new(&mut *buf)
                .encode_array_header(self.verifications.len() as u64)?;
            for item in &self.verifications {
                item.encode_cbor(buf)?;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("verifiedStatus")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.verified_status)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("trustedVerifierStatus")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.trusted_verifier_status)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.verifications.len() as u64)?;
                for item in &self.verifications {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("verifications", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.verified_status)?;
                pairs.push(("verifiedStatus", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.trusted_verifier_status)?;
                pairs.push(("trustedVerifierStatus", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_verifications: Vec<ActorDefsVerificationView> = Vec::new();
        let mut field_verified_status: Option<String> = None;
        let mut field_trusted_verifier_status: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "verifications" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_verifications
                                .push(ActorDefsVerificationView::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "verifiedStatus" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_verified_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "trustedVerifierStatus" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_trusted_verifier_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsVerificationState {
            verifications: field_verifications,
            verified_status: field_verified_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'verifiedStatus'".into(),
                )
            })?,
            trusted_verifier_status: field_trusted_verifier_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'trustedVerifierStatus'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_verifications: Vec<ActorDefsVerificationView> = Vec::new();
        let mut field_verified_status: Option<String> = None;
        let mut field_trusted_verifier_status: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "verifications" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_verifications
                                .push(ActorDefsVerificationView::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "verifiedStatus" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_verified_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "trustedVerifierStatus" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_trusted_verifier_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsVerificationState {
            verifications: field_verifications,
            verified_status: field_verified_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'verifiedStatus'".into(),
                )
            })?,
            trusted_verifier_status: field_trusted_verifier_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'trustedVerifierStatus'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsVerificationStateCborView<'a> {
    pub verifications: Vec<ActorDefsVerificationView>,
    pub verified_status: &'a str,
    pub trusted_verifier_status: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsVerificationStateCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsVerificationState, crate::cbor::CborError> {
        Ok(ActorDefsVerificationState {
            verifications: self.verifications.clone(),
            verified_status: self.verified_status.to_owned(),
            trusted_verifier_status: self.trusted_verifier_status.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_verifications: Vec<ActorDefsVerificationView> = Vec::new();
        let mut field_verified_status: Option<&'a str> = None;
        let mut field_trusted_verifier_status: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(
            b"\x6d\x76\x65\x72\x69\x66\x69\x63\x61\x74\x69\x6f\x6e\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Array(items) = value {
                    for item in items {
                        field_verifications.push(ActorDefsVerificationView::from_cbor_value(item)?);
                    }
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6e\x76\x65\x72\x69\x66\x69\x65\x64\x53\x74\x61\x74\x75\x73",
            |decoder| {
                field_verified_status = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(b"\x75\x74\x72\x75\x73\x74\x65\x64\x56\x65\x72\x69\x66\x69\x65\x72\x53\x74\x61\x74\x75\x73", |decoder| {
                    field_trusted_verifier_status = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"verifications" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_verifications
                                .push(ActorDefsVerificationView::from_cbor_value(item)?);
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"verifiedStatus" => {
                    field_verified_status = Some(decoder.text()?);
                }
                b"trustedVerifierStatus" => {
                    field_trusted_verifier_status = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            verifications: field_verifications,
            verified_status: field_verified_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'verifiedStatus'".into(),
                )
            })?,
            trusted_verifier_status: field_trusted_verifier_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'trustedVerifierStatus'".into(),
                )
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsVerificationView — An individual verification for an associated subject.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsVerificationView {
    /// Timestamp when the verification was created.
    pub created_at: crate::syntax::Datetime,
    /// True if the verification passes validation, otherwise false.
    pub is_valid: bool,
    /// The user who issued this verification.
    pub issuer: crate::syntax::Did,
    /// The display name of the issuer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub issuer_display_name: Option<String>,
    /// The handle of the issuer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub issuer_handle: Option<crate::syntax::Handle>,
    /// The AT-URI of the verification record.
    pub uri: crate::syntax::AtUri,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsVerificationView {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 4u64;
            if self.issuer_handle.is_some() {
                count += 1;
            }
            if self.issuer_display_name.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("uri")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.uri.as_str())?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("issuer")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.issuer.as_str())?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("isValid")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.is_valid)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.created_at.as_str())?;
            if self.issuer_handle.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("issuerHandle")?;
                if let Some(ref val) = self.issuer_handle {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.issuer_display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("issuerDisplayName")?;
                if let Some(ref val) = self.issuer_display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.uri.as_str())?;
                pairs.push(("uri", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.issuer.as_str())?;
                pairs.push(("issuer", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.is_valid)?;
                pairs.push(("isValid", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.created_at.as_str())?;
                pairs.push(("createdAt", vbuf));
            }
            if self.issuer_handle.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.issuer_handle {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("issuerHandle", vbuf));
            }
            if self.issuer_display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.issuer_display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("issuerDisplayName", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut field_issuer: Option<crate::syntax::Did> = None;
        let mut field_is_valid: Option<bool> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_issuer_handle: Option<crate::syntax::Handle> = None;
        let mut field_issuer_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "uri" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuer" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isValid" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_valid = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "createdAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuerHandle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuerDisplayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsVerificationView {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            issuer: field_issuer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'issuer'".into())
            })?,
            is_valid: field_is_valid.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'isValid'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            issuer_handle: field_issuer_handle,
            issuer_display_name: field_issuer_display_name,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut field_issuer: Option<crate::syntax::Did> = None;
        let mut field_is_valid: Option<bool> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_issuer_handle: Option<crate::syntax::Handle> = None;
        let mut field_issuer_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "uri" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuer" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "isValid" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_valid = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuerHandle" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "issuerDisplayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsVerificationView {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            issuer: field_issuer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'issuer'".into())
            })?,
            is_valid: field_is_valid.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'isValid'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            issuer_handle: field_issuer_handle,
            issuer_display_name: field_issuer_display_name,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsVerificationViewCborView<'a> {
    pub uri: crate::syntax::AtUriRef<'a>,
    pub issuer: crate::syntax::Did,
    pub is_valid: bool,
    pub created_at: crate::syntax::DatetimeRef<'a>,
    pub issuer_handle: Option<crate::syntax::Handle>,
    pub issuer_display_name: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsVerificationViewCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsVerificationView, crate::cbor::CborError> {
        Ok(ActorDefsVerificationView {
            uri: self.uri.to_owned(),
            issuer: self.issuer.clone(),
            is_valid: self.is_valid,
            created_at: self.created_at.to_owned(),
            issuer_handle: self.issuer_handle.clone(),
            issuer_display_name: self
                .issuer_display_name
                .as_ref()
                .map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_uri: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut field_issuer: Option<crate::syntax::Did> = None;
        let mut field_is_valid: Option<bool> = None;
        let mut field_created_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_issuer_handle: Option<crate::syntax::Handle> = None;
        let mut field_issuer_display_name: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x75\x72\x69", |decoder| {
            field_uri = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x66\x69\x73\x73\x75\x65\x72", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_issuer = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x67\x69\x73\x56\x61\x6c\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_is_valid = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x72\x65\x61\x74\x65\x64\x41\x74", |decoder| {
            field_created_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(
            b"\x6c\x69\x73\x73\x75\x65\x72\x48\x61\x6e\x64\x6c\x65",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Text(s) = value {
                    field_issuer_handle = Some(
                        crate::syntax::Handle::try_from(s)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x71\x69\x73\x73\x75\x65\x72\x44\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_issuer_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"uri" => {
                    field_uri = Some(
                        crate::syntax::AtUriRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"issuer" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"isValid" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_is_valid = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"createdAt" => {
                    field_created_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"issuerHandle" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_issuer_handle = Some(
                            crate::syntax::Handle::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                b"issuerDisplayName" => {
                    field_issuer_display_name = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            issuer: field_issuer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'issuer'".into())
            })?,
            is_valid: field_is_valid.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'isValid'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            issuer_handle: field_issuer_handle,
            issuer_display_name: field_issuer_display_name,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// ActorDefsViewerState — Metadata about the requesting account's relationship with the subject account. Only has meaningful content for authed requests.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ActorDefsViewerState {
    /// This property is present only in selected cases, as an optimization.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activity_subscription: Option<crate::api::app::bsky::NotificationDefsActivitySubscription>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub blocked_by: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub blocking: Option<crate::syntax::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub blocking_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub followed_by: Option<crate::syntax::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub following: Option<crate::syntax::AtUri>,
    /// This property is present only in selected cases, as an optimization.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub known_followers: Option<ActorDefsKnownFollowers>,
    /// Whether the account is fully muted, directly or via a mutelist. False when the mute is scoped to specific kinds; see mutedOnlyReposts and mutedOnlyQuoteposts.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub muted: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub muted_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic>,
    /// Whether the account's quote posts are muted. Scoped mutes are exclusive with muted: this can be true while muted is false. If muted is true, this will be false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub muted_only_quoteposts: Option<bool>,
    /// Whether the account's reposts are muted. Scoped mutes are exclusive with muted: this can be true while muted is false. If muted is true, this will be false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub muted_only_reposts: Option<bool>,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl ActorDefsViewerState {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 0u64;
            if self.muted.is_some() {
                count += 1;
            }
            if self.blocking.is_some() {
                count += 1;
            }
            if self.blocked_by.is_some() {
                count += 1;
            }
            if self.following.is_some() {
                count += 1;
            }
            if self.followed_by.is_some() {
                count += 1;
            }
            if self.muted_by_list.is_some() {
                count += 1;
            }
            if self.blocking_by_list.is_some() {
                count += 1;
            }
            if self.known_followers.is_some() {
                count += 1;
            }
            if self.muted_only_reposts.is_some() {
                count += 1;
            }
            if self.muted_only_quoteposts.is_some() {
                count += 1;
            }
            if self.activity_subscription.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.muted.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("muted")?;
                if let Some(ref val) = self.muted {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.blocking.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("blocking")?;
                if let Some(ref val) = self.blocking {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.blocked_by.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("blockedBy")?;
                if let Some(ref val) = self.blocked_by {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.following.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("following")?;
                if let Some(ref val) = self.following {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.followed_by.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("followedBy")?;
                if let Some(ref val) = self.followed_by {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
            if self.muted_by_list.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("mutedByList")?;
                if let Some(ref val) = self.muted_by_list {
                    val.encode_cbor(buf)?;
                }
            }
            if self.blocking_by_list.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("blockingByList")?;
                if let Some(ref val) = self.blocking_by_list {
                    val.encode_cbor(buf)?;
                }
            }
            if self.known_followers.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("knownFollowers")?;
                if let Some(ref val) = self.known_followers {
                    val.encode_cbor(buf)?;
                }
            }
            if self.muted_only_reposts.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("mutedOnlyReposts")?;
                if let Some(ref val) = self.muted_only_reposts {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.muted_only_quoteposts.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("mutedOnlyQuoteposts")?;
                if let Some(ref val) = self.muted_only_quoteposts {
                    crate::cbor::Encoder::new(&mut *buf).encode_bool(*val)?;
                }
            }
            if self.activity_subscription.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("activitySubscription")?;
                if let Some(ref val) = self.activity_subscription {
                    val.encode_cbor(buf)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.muted.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.muted {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("muted", vbuf));
            }
            if self.blocking.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.blocking {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("blocking", vbuf));
            }
            if self.blocked_by.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.blocked_by {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("blockedBy", vbuf));
            }
            if self.following.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.following {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("following", vbuf));
            }
            if self.followed_by.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.followed_by {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("followedBy", vbuf));
            }
            if self.muted_by_list.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.muted_by_list {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("mutedByList", vbuf));
            }
            if self.blocking_by_list.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.blocking_by_list {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("blockingByList", vbuf));
            }
            if self.known_followers.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.known_followers {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("knownFollowers", vbuf));
            }
            if self.muted_only_reposts.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.muted_only_reposts {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("mutedOnlyReposts", vbuf));
            }
            if self.muted_only_quoteposts.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.muted_only_quoteposts {
                    crate::cbor::Encoder::new(&mut vbuf).encode_bool(*val)?;
                }
                pairs.push(("mutedOnlyQuoteposts", vbuf));
            }
            if self.activity_subscription.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.activity_subscription {
                    val.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("activitySubscription", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_muted: Option<bool> = None;
        let mut field_blocking: Option<crate::syntax::AtUri> = None;
        let mut field_blocked_by: Option<bool> = None;
        let mut field_following: Option<crate::syntax::AtUri> = None;
        let mut field_followed_by: Option<crate::syntax::AtUri> = None;
        let mut field_muted_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic> = None;
        let mut field_blocking_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic> =
            None;
        let mut field_known_followers: Option<ActorDefsKnownFollowers> = None;
        let mut field_muted_only_reposts: Option<bool> = None;
        let mut field_muted_only_quoteposts: Option<bool> = None;
        let mut field_activity_subscription: Option<
            crate::api::app::bsky::NotificationDefsActivitySubscription,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "muted" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "blocking" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_blocking = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "blockedBy" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_blocked_by = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "following" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_following = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "followedBy" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_followed_by = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "mutedByList" => {
                    field_muted_by_list = Some(
                        crate::api::app::bsky::GraphDefsListViewBasic::decode_cbor(decoder)?,
                    );
                }
                "blockingByList" => {
                    field_blocking_by_list = Some(
                        crate::api::app::bsky::GraphDefsListViewBasic::decode_cbor(decoder)?,
                    );
                }
                "knownFollowers" => {
                    field_known_followers = Some(ActorDefsKnownFollowers::decode_cbor(decoder)?);
                }
                "mutedOnlyReposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "mutedOnlyQuoteposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_quoteposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "activitySubscription" => {
                    field_activity_subscription = Some(
                        crate::api::app::bsky::NotificationDefsActivitySubscription::decode_cbor(
                            decoder,
                        )?,
                    );
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(ActorDefsViewerState {
            muted: field_muted,
            blocking: field_blocking,
            blocked_by: field_blocked_by,
            following: field_following,
            followed_by: field_followed_by,
            muted_by_list: field_muted_by_list,
            blocking_by_list: field_blocking_by_list,
            known_followers: field_known_followers,
            muted_only_reposts: field_muted_only_reposts,
            muted_only_quoteposts: field_muted_only_quoteposts,
            activity_subscription: field_activity_subscription,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_muted: Option<bool> = None;
        let mut field_blocking: Option<crate::syntax::AtUri> = None;
        let mut field_blocked_by: Option<bool> = None;
        let mut field_following: Option<crate::syntax::AtUri> = None;
        let mut field_followed_by: Option<crate::syntax::AtUri> = None;
        let mut field_muted_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic> = None;
        let mut field_blocking_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasic> =
            None;
        let mut field_known_followers: Option<ActorDefsKnownFollowers> = None;
        let mut field_muted_only_reposts: Option<bool> = None;
        let mut field_muted_only_quoteposts: Option<bool> = None;
        let mut field_activity_subscription: Option<
            crate::api::app::bsky::NotificationDefsActivitySubscription,
        > = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "muted" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "blocking" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_blocking = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "blockedBy" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_blocked_by = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "following" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_following = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "followedBy" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_followed_by = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "mutedByList" => {
                    field_muted_by_list = Some(
                        crate::api::app::bsky::GraphDefsListViewBasic::from_cbor_value(value)?,
                    );
                }
                "blockingByList" => {
                    field_blocking_by_list = Some(
                        crate::api::app::bsky::GraphDefsListViewBasic::from_cbor_value(value)?,
                    );
                }
                "knownFollowers" => {
                    field_known_followers = Some(ActorDefsKnownFollowers::from_cbor_value(value)?);
                }
                "mutedOnlyReposts" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "mutedOnlyQuoteposts" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_quoteposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "activitySubscription" => {
                    field_activity_subscription = Some(crate::api::app::bsky::NotificationDefsActivitySubscription::from_cbor_value(value)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(ActorDefsViewerState {
            muted: field_muted,
            blocking: field_blocking,
            blocked_by: field_blocked_by,
            following: field_following,
            followed_by: field_followed_by,
            muted_by_list: field_muted_by_list,
            blocking_by_list: field_blocking_by_list,
            known_followers: field_known_followers,
            muted_only_reposts: field_muted_only_reposts,
            muted_only_quoteposts: field_muted_only_quoteposts,
            activity_subscription: field_activity_subscription,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct ActorDefsViewerStateCborView<'a> {
    pub muted: Option<bool>,
    pub blocking: Option<crate::syntax::AtUriRef<'a>>,
    pub blocked_by: Option<bool>,
    pub following: Option<crate::syntax::AtUriRef<'a>>,
    pub followed_by: Option<crate::syntax::AtUriRef<'a>>,
    pub muted_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasicCborView<'a>>,
    pub blocking_by_list: Option<crate::api::app::bsky::GraphDefsListViewBasicCborView<'a>>,
    pub known_followers: Option<ActorDefsKnownFollowersCborView<'a>>,
    pub muted_only_reposts: Option<bool>,
    pub muted_only_quoteposts: Option<bool>,
    pub activity_subscription:
        Option<crate::api::app::bsky::NotificationDefsActivitySubscriptionCborView<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> ActorDefsViewerStateCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<ActorDefsViewerState, crate::cbor::CborError> {
        Ok(ActorDefsViewerState {
            muted: self.muted,
            blocking: self.blocking.as_ref().map(|value| (*value).to_owned()),
            blocked_by: self.blocked_by,
            following: self.following.as_ref().map(|value| (*value).to_owned()),
            followed_by: self.followed_by.as_ref().map(|value| (*value).to_owned()),
            muted_by_list: self
                .muted_by_list
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            blocking_by_list: self
                .blocking_by_list
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            known_followers: self
                .known_followers
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            muted_only_reposts: self.muted_only_reposts,
            muted_only_quoteposts: self.muted_only_quoteposts,
            activity_subscription: self
                .activity_subscription
                .as_ref()
                .map(|value| value.to_owned())
                .transpose()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_muted: Option<bool> = None;
        let mut field_blocking: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut field_blocked_by: Option<bool> = None;
        let mut field_following: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut field_followed_by: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut field_muted_by_list: Option<
            crate::api::app::bsky::GraphDefsListViewBasicCborView<'a>,
        > = None;
        let mut field_blocking_by_list: Option<
            crate::api::app::bsky::GraphDefsListViewBasicCborView<'a>,
        > = None;
        let mut field_known_followers: Option<ActorDefsKnownFollowersCborView<'a>> = None;
        let mut field_muted_only_reposts: Option<bool> = None;
        let mut field_muted_only_quoteposts: Option<bool> = None;
        let mut field_activity_subscription: Option<
            crate::api::app::bsky::NotificationDefsActivitySubscriptionCborView<'a>,
        > = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x6d\x75\x74\x65\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_muted = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x68\x62\x6c\x6f\x63\x6b\x69\x6e\x67", |decoder| {
            field_blocking = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x69\x62\x6c\x6f\x63\x6b\x65\x64\x42\x79", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_blocked_by = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x66\x6f\x6c\x6c\x6f\x77\x69\x6e\x67", |decoder| {
            field_following = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x66\x6f\x6c\x6c\x6f\x77\x65\x64\x42\x79", |decoder| {
            field_followed_by = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x6d\x75\x74\x65\x64\x42\x79\x4c\x69\x73\x74",
            |decoder| {
                field_muted_by_list = Some(
                    crate::api::app::bsky::GraphDefsListViewBasicCborView::decode_cbor(decoder)?,
                );
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6e\x62\x6c\x6f\x63\x6b\x69\x6e\x67\x42\x79\x4c\x69\x73\x74",
            |decoder| {
                field_blocking_by_list = Some(
                    crate::api::app::bsky::GraphDefsListViewBasicCborView::decode_cbor(decoder)?,
                );
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6e\x6b\x6e\x6f\x77\x6e\x46\x6f\x6c\x6c\x6f\x77\x65\x72\x73",
            |decoder| {
                field_known_followers =
                    Some(ActorDefsKnownFollowersCborView::decode_cbor(decoder)?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x70\x6d\x75\x74\x65\x64\x4f\x6e\x6c\x79\x52\x65\x70\x6f\x73\x74\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_muted_only_reposts = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x73\x6d\x75\x74\x65\x64\x4f\x6e\x6c\x79\x51\x75\x6f\x74\x65\x70\x6f\x73\x74\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_muted_only_quoteposts = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(b"\x74\x61\x63\x74\x69\x76\x69\x74\x79\x53\x75\x62\x73\x63\x72\x69\x70\x74\x69\x6f\x6e", |decoder| {
                    field_activity_subscription = Some(crate::api::app::bsky::NotificationDefsActivitySubscriptionCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"muted" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"blocking" => {
                    field_blocking = Some(crate::syntax::AtUriRef::try_from(decoder.text()?).map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?);
                }
                b"blockedBy" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_blocked_by = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"following" => {
                    field_following = Some(crate::syntax::AtUriRef::try_from(decoder.text()?).map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?);
                }
                b"followedBy" => {
                    field_followed_by = Some(crate::syntax::AtUriRef::try_from(decoder.text()?).map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?);
                }
                b"mutedByList" => {
                    field_muted_by_list = Some(crate::api::app::bsky::GraphDefsListViewBasicCborView::decode_cbor(decoder)?);
                }
                b"blockingByList" => {
                    field_blocking_by_list = Some(crate::api::app::bsky::GraphDefsListViewBasicCborView::decode_cbor(decoder)?);
                }
                b"knownFollowers" => {
                    field_known_followers = Some(ActorDefsKnownFollowersCborView::decode_cbor(decoder)?);
                }
                b"mutedOnlyReposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_reposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"mutedOnlyQuoteposts" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_only_quoteposts = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"activitySubscription" => {
                    field_activity_subscription = Some(crate::api::app::bsky::NotificationDefsActivitySubscriptionCborView::decode_cbor(decoder)?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into()))?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) { result?; }
        drop(entries);
        Ok(Self {
            muted: field_muted,
            blocking: field_blocking,
            blocked_by: field_blocked_by,
            following: field_following,
            followed_by: field_followed_by,
            muted_by_list: field_muted_by_list,
            blocking_by_list: field_blocking_by_list,
            known_followers: field_known_followers,
            muted_only_reposts: field_muted_only_reposts,
            muted_only_quoteposts: field_muted_only_quoteposts,
            activity_subscription: field_activity_subscription,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}
