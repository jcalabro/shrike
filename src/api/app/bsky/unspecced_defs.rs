// Code generated by lexgen. DO NOT EDIT.

/// UnspeccedDefsAgeAssuranceEvent — Object used to store age assurance data in stash.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsAgeAssuranceEvent {
    /// The unique identifier for this instance of the age assurance flow, in UUID format.
    pub attempt_id: String,
    /// The IP address used when completing the AA flow.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub complete_ip: Option<String>,
    /// The user agent used when completing the AA flow.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub complete_ua: Option<String>,
    /// The date and time of this write operation.
    pub created_at: crate::syntax::Datetime,
    /// The email used for AA.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// The IP address used when initiating the AA flow.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub init_ip: Option<String>,
    /// The user agent used when initiating the AA flow.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub init_ua: Option<String>,
    /// The status of the age assurance process.
    pub status: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsAgeAssuranceEvent {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 3u64;
            if self.email.is_some() {
                count += 1;
            }
            if self.init_ip.is_some() {
                count += 1;
            }
            if self.init_ua.is_some() {
                count += 1;
            }
            if self.complete_ip.is_some() {
                count += 1;
            }
            if self.complete_ua.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            if self.email.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("email")?;
                if let Some(ref val) = self.email {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.init_ip.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("initIp")?;
                if let Some(ref val) = self.init_ip {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.init_ua.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("initUa")?;
                if let Some(ref val) = self.init_ua {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.status)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("attemptId")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.attempt_id)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("createdAt")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.created_at.as_str())?;
            if self.complete_ip.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("completeIp")?;
                if let Some(ref val) = self.complete_ip {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.complete_ua.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("completeUa")?;
                if let Some(ref val) = self.complete_ua {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            if self.email.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.email {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("email", vbuf));
            }
            if self.init_ip.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.init_ip {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("initIp", vbuf));
            }
            if self.init_ua.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.init_ua {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("initUa", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.status)?;
                pairs.push(("status", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.attempt_id)?;
                pairs.push(("attemptId", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.created_at.as_str())?;
                pairs.push(("createdAt", vbuf));
            }
            if self.complete_ip.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.complete_ip {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("completeIp", vbuf));
            }
            if self.complete_ua.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.complete_ua {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("completeUa", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_email: Option<String> = None;
        let mut field_init_ip: Option<String> = None;
        let mut field_init_ua: Option<String> = None;
        let mut field_status: Option<String> = None;
        let mut field_attempt_id: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_complete_ip: Option<String> = None;
        let mut field_complete_ua: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "email" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_email = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "initIp" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_init_ip = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "initUa" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_init_ua = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "status" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "attemptId" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_attempt_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completeIp" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_complete_ip = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completeUa" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_complete_ua = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsAgeAssuranceEvent {
            email: field_email,
            init_ip: field_init_ip,
            init_ua: field_init_ua,
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            attempt_id: field_attempt_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'attemptId'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            complete_ip: field_complete_ip,
            complete_ua: field_complete_ua,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_email: Option<String> = None;
        let mut field_init_ip: Option<String> = None;
        let mut field_init_ua: Option<String> = None;
        let mut field_status: Option<String> = None;
        let mut field_attempt_id: Option<String> = None;
        let mut field_created_at: Option<crate::syntax::Datetime> = None;
        let mut field_complete_ip: Option<String> = None;
        let mut field_complete_ua: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "email" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_email = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "initIp" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_init_ip = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "initUa" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_init_ua = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "status" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "attemptId" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_attempt_id = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "createdAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_created_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completeIp" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_complete_ip = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "completeUa" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_complete_ua = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsAgeAssuranceEvent {
            email: field_email,
            init_ip: field_init_ip,
            init_ua: field_init_ua,
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            attempt_id: field_attempt_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'attemptId'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            complete_ip: field_complete_ip,
            complete_ua: field_complete_ua,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsAgeAssuranceEventCborView<'a> {
    pub email: Option<&'a str>,
    pub init_ip: Option<&'a str>,
    pub init_ua: Option<&'a str>,
    pub status: &'a str,
    pub attempt_id: &'a str,
    pub created_at: crate::syntax::DatetimeRef<'a>,
    pub complete_ip: Option<&'a str>,
    pub complete_ua: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsAgeAssuranceEventCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsAgeAssuranceEvent, crate::cbor::CborError> {
        Ok(UnspeccedDefsAgeAssuranceEvent {
            email: self.email.as_ref().map(|value| (*value).to_owned()),
            init_ip: self.init_ip.as_ref().map(|value| (*value).to_owned()),
            init_ua: self.init_ua.as_ref().map(|value| (*value).to_owned()),
            status: self.status.to_owned(),
            attempt_id: self.attempt_id.to_owned(),
            created_at: self.created_at.to_owned(),
            complete_ip: self.complete_ip.as_ref().map(|value| (*value).to_owned()),
            complete_ua: self.complete_ua.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_email: Option<&'a str> = None;
        let mut field_init_ip: Option<&'a str> = None;
        let mut field_init_ua: Option<&'a str> = None;
        let mut field_status: Option<&'a str> = None;
        let mut field_attempt_id: Option<&'a str> = None;
        let mut field_created_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_complete_ip: Option<&'a str> = None;
        let mut field_complete_ua: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x65\x65\x6d\x61\x69\x6c", |decoder| {
            field_email = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x69\x6e\x69\x74\x49\x70", |decoder| {
            field_init_ip = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x69\x6e\x69\x74\x55\x61", |decoder| {
            field_init_ua = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x61\x74\x74\x65\x6d\x70\x74\x49\x64", |decoder| {
            field_attempt_id = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x63\x72\x65\x61\x74\x65\x64\x41\x74", |decoder| {
            field_created_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(b"\x6a\x63\x6f\x6d\x70\x6c\x65\x74\x65\x49\x70", |decoder| {
            field_complete_ip = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x6a\x63\x6f\x6d\x70\x6c\x65\x74\x65\x55\x61", |decoder| {
            field_complete_ua = Some(decoder.text()?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"email" => {
                    field_email = Some(decoder.text()?);
                }
                b"initIp" => {
                    field_init_ip = Some(decoder.text()?);
                }
                b"initUa" => {
                    field_init_ua = Some(decoder.text()?);
                }
                b"status" => {
                    field_status = Some(decoder.text()?);
                }
                b"attemptId" => {
                    field_attempt_id = Some(decoder.text()?);
                }
                b"createdAt" => {
                    field_created_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"completeIp" => {
                    field_complete_ip = Some(decoder.text()?);
                }
                b"completeUa" => {
                    field_complete_ua = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            email: field_email,
            init_ip: field_init_ip,
            init_ua: field_init_ua,
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            attempt_id: field_attempt_id.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'attemptId'".into())
            })?,
            created_at: field_created_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'createdAt'".into())
            })?,
            complete_ip: field_complete_ip,
            complete_ua: field_complete_ua,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsAgeAssuranceState — The computed state of the age assurance process, returned to the user in question on certain authenticated requests.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsAgeAssuranceState {
    /// The timestamp when this state was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_initiated_at: Option<crate::syntax::Datetime>,
    /// The status of the age assurance process.
    pub status: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsAgeAssuranceState {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 1u64;
            if self.last_initiated_at.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.status)?;
            if self.last_initiated_at.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("lastInitiatedAt")?;
                if let Some(ref val) = self.last_initiated_at {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val.as_str())?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.status)?;
                pairs.push(("status", vbuf));
            }
            if self.last_initiated_at.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.last_initiated_at {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val.as_str())?;
                }
                pairs.push(("lastInitiatedAt", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_status: Option<String> = None;
        let mut field_last_initiated_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "status" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "lastInitiatedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_last_initiated_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsAgeAssuranceState {
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            last_initiated_at: field_last_initiated_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_status: Option<String> = None;
        let mut field_last_initiated_at: Option<crate::syntax::Datetime> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "status" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "lastInitiatedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_last_initiated_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsAgeAssuranceState {
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            last_initiated_at: field_last_initiated_at,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsAgeAssuranceStateCborView<'a> {
    pub status: &'a str,
    pub last_initiated_at: Option<crate::syntax::DatetimeRef<'a>>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsAgeAssuranceStateCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsAgeAssuranceState, crate::cbor::CborError> {
        Ok(UnspeccedDefsAgeAssuranceState {
            status: self.status.to_owned(),
            last_initiated_at: self
                .last_initiated_at
                .as_ref()
                .map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_status: Option<&'a str> = None;
        let mut field_last_initiated_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(
            b"\x6f\x6c\x61\x73\x74\x49\x6e\x69\x74\x69\x61\x74\x65\x64\x41\x74",
            |decoder| {
                field_last_initiated_at = Some(
                    crate::syntax::DatetimeRef::try_from(decoder.text()?)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"status" => {
                    field_status = Some(decoder.text()?);
                }
                b"lastInitiatedAt" => {
                    field_last_initiated_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            status: field_status.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'status'".into())
            })?,
            last_initiated_at: field_last_initiated_at,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsSkeletonSearchActor object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsSkeletonSearchActor {
    pub did: crate::syntax::Did,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsSkeletonSearchActor {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("did")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.did.as_str())?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.did.as_str())?;
                pairs.push(("did", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsSkeletonSearchActor {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "did" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsSkeletonSearchActor {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsSkeletonSearchActorCborView<'a> {
    pub did: crate::syntax::Did,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsSkeletonSearchActorCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsSkeletonSearchActor, crate::cbor::CborError> {
        Ok(UnspeccedDefsSkeletonSearchActor {
            did: self.did.clone(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_did: Option<crate::syntax::Did> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x64\x69\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Text(s) = value {
                field_did = Some(
                    crate::syntax::Did::try_from(s)
                        .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                );
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
            }
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"did" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_did = Some(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            did: field_did.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'did'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsSkeletonSearchPost object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsSkeletonSearchPost {
    pub uri: crate::syntax::AtUri,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsSkeletonSearchPost {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("uri")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.uri.as_str())?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.uri.as_str())?;
                pairs.push(("uri", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "uri" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsSkeletonSearchPost {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "uri" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsSkeletonSearchPost {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsSkeletonSearchPostCborView<'a> {
    pub uri: crate::syntax::AtUriRef<'a>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsSkeletonSearchPostCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsSkeletonSearchPost, crate::cbor::CborError> {
        Ok(UnspeccedDefsSkeletonSearchPost {
            uri: self.uri.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_uri: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x75\x72\x69", |decoder| {
            field_uri = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"uri" => {
                    field_uri = Some(
                        crate::syntax::AtUriRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsSkeletonSearchStarterPack object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsSkeletonSearchStarterPack {
    pub uri: crate::syntax::AtUri,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsSkeletonSearchStarterPack {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("uri")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.uri.as_str())?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.uri.as_str())?;
                pairs.push(("uri", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "uri" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsSkeletonSearchStarterPack {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_uri: Option<crate::syntax::AtUri> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "uri" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_uri = Some(
                            crate::syntax::AtUri::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsSkeletonSearchStarterPack {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsSkeletonSearchStarterPackCborView<'a> {
    pub uri: crate::syntax::AtUriRef<'a>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsSkeletonSearchStarterPackCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(
        &self,
    ) -> Result<UnspeccedDefsSkeletonSearchStarterPack, crate::cbor::CborError> {
        Ok(UnspeccedDefsSkeletonSearchStarterPack {
            uri: self.uri.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_uri: Option<crate::syntax::AtUriRef<'a>> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x63\x75\x72\x69", |decoder| {
            field_uri = Some(
                crate::syntax::AtUriRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"uri" => {
                    field_uri = Some(
                        crate::syntax::AtUriRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            uri: field_uri.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'uri'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsSkeletonTrend object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsSkeletonTrend {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub category: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dids: Vec<crate::syntax::Did>,
    pub display_name: String,
    pub link: String,
    pub post_count: i64,
    pub started_at: crate::syntax::Datetime,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    pub topic: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsSkeletonTrend {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 6u64;
            if self.status.is_some() {
                count += 1;
            }
            if self.category.is_some() {
                count += 1;
            }
            if self.description.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("dids")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.dids.len() as u64)?;
            for item in &self.dids {
                crate::cbor::Encoder::new(&mut *buf).encode_text(item.as_str())?;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("link")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.link)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("topic")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.topic)?;
            if self.status.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
                if let Some(ref val) = self.status {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.category.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("category")?;
                if let Some(ref val) = self.category {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("postCount")?;
            crate::cbor::Encoder::new(&mut *buf).encode_i64(self.post_count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("startedAt")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.started_at.as_str())?;
            if self.description.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.display_name)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_array_header(self.dids.len() as u64)?;
                for item in &self.dids {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(item.as_str())?;
                }
                pairs.push(("dids", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.link)?;
                pairs.push(("link", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.topic)?;
                pairs.push(("topic", vbuf));
            }
            if self.status.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.status {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("status", vbuf));
            }
            if self.category.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.category {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("category", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_i64(self.post_count)?;
                pairs.push(("postCount", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.started_at.as_str())?;
                pairs.push(("startedAt", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.display_name)?;
                pairs.push(("displayName", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_dids: Vec<crate::syntax::Did> = Vec::new();
        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_status: Option<String> = None;
        let mut field_category: Option<String> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::Datetime> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "dids" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_dids.push(crate::syntax::Did::try_from(s).map_err(|e| {
                                    crate::cbor::CborError::InvalidCbor(e.to_string())
                                })?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "link" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "status" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "category" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_category = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "postCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "startedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_started_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsSkeletonTrend {
            dids: field_dids,
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_dids: Vec<crate::syntax::Did> = Vec::new();
        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_status: Option<String> = None;
        let mut field_category: Option<String> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::Datetime> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "dids" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_dids.push(crate::syntax::Did::try_from(s).map_err(|e| {
                                    crate::cbor::CborError::InvalidCbor(e.to_string())
                                })?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "link" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "status" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "category" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_category = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "postCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_post_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_post_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "startedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_started_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsSkeletonTrend {
            dids: field_dids,
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsSkeletonTrendCborView<'a> {
    pub dids: Vec<crate::syntax::Did>,
    pub link: &'a str,
    pub topic: &'a str,
    pub status: Option<&'a str>,
    pub category: Option<&'a str>,
    pub post_count: i64,
    pub started_at: crate::syntax::DatetimeRef<'a>,
    pub description: Option<&'a str>,
    pub display_name: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsSkeletonTrendCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsSkeletonTrend, crate::cbor::CborError> {
        Ok(UnspeccedDefsSkeletonTrend {
            dids: self.dids.clone(),
            link: self.link.to_owned(),
            topic: self.topic.to_owned(),
            status: self.status.as_ref().map(|value| (*value).to_owned()),
            category: self.category.as_ref().map(|value| (*value).to_owned()),
            post_count: self.post_count,
            started_at: self.started_at.to_owned(),
            description: self.description.as_ref().map(|value| (*value).to_owned()),
            display_name: self.display_name.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_dids: Vec<crate::syntax::Did> = Vec::new();
        let mut field_link: Option<&'a str> = None;
        let mut field_topic: Option<&'a str> = None;
        let mut field_status: Option<&'a str> = None;
        let mut field_category: Option<&'a str> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_description: Option<&'a str> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x64\x69\x64\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    if let crate::cbor::Value::Text(s) = item {
                        field_dids.push(
                            crate::syntax::Did::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected text in array".into(),
                        ));
                    }
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x64\x6c\x69\x6e\x6b", |decoder| {
            field_link = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x74\x6f\x70\x69\x63", |decoder| {
            field_topic = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x63\x61\x74\x65\x67\x6f\x72\x79", |decoder| {
            field_category = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x70\x6f\x73\x74\x43\x6f\x75\x6e\x74", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_post_count = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_post_count = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x73\x74\x61\x72\x74\x65\x64\x41\x74", |decoder| {
            field_started_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x65\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_description = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"dids" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            if let crate::cbor::Value::Text(s) = item {
                                field_dids.push(crate::syntax::Did::try_from(s).map_err(|e| {
                                    crate::cbor::CborError::InvalidCbor(e.to_string())
                                })?);
                            } else {
                                return Err(crate::cbor::CborError::InvalidCbor(
                                    "expected text in array".into(),
                                ));
                            }
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"link" => {
                    field_link = Some(decoder.text()?);
                }
                b"topic" => {
                    field_topic = Some(decoder.text()?);
                }
                b"status" => {
                    field_status = Some(decoder.text()?);
                }
                b"category" => {
                    field_category = Some(decoder.text()?);
                }
                b"postCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"startedAt" => {
                    field_started_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"description" => {
                    field_description = Some(decoder.text()?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            dids: field_dids,
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsThreadItemBlocked object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsThreadItemBlocked {
    pub author: crate::api::app::bsky::FeedDefsBlockedAuthor,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsThreadItemBlocked {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 1u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("author")?;
            self.author.encode_cbor(buf)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                self.author.encode_cbor(&mut vbuf)?;
                pairs.push(("author", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_author: Option<crate::api::app::bsky::FeedDefsBlockedAuthor> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "author" => {
                    field_author = Some(crate::api::app::bsky::FeedDefsBlockedAuthor::decode_cbor(
                        decoder,
                    )?);
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsThreadItemBlocked {
            author: field_author.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'author'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_author: Option<crate::api::app::bsky::FeedDefsBlockedAuthor> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "author" => {
                    field_author =
                        Some(crate::api::app::bsky::FeedDefsBlockedAuthor::from_cbor_value(value)?);
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsThreadItemBlocked {
            author: field_author.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'author'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsThreadItemBlockedCborView<'a> {
    pub author: crate::api::app::bsky::FeedDefsBlockedAuthorCborView<'a>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsThreadItemBlockedCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsThreadItemBlocked, crate::cbor::CborError> {
        Ok(UnspeccedDefsThreadItemBlocked {
            author: self.author.to_owned()?,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_author: Option<crate::api::app::bsky::FeedDefsBlockedAuthorCborView<'a>> =
            None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x66\x61\x75\x74\x68\x6f\x72", |decoder| {
            field_author =
                Some(crate::api::app::bsky::FeedDefsBlockedAuthorCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"author" => {
                    field_author = Some(
                        crate::api::app::bsky::FeedDefsBlockedAuthorCborView::decode_cbor(decoder)?,
                    );
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            author: field_author.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'author'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsThreadItemNoUnauthenticated object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsThreadItemNoUnauthenticated {
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsThreadItemNoUnauthenticated {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 0u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsThreadItemNoUnauthenticated {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsThreadItemNoUnauthenticated {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsThreadItemNoUnauthenticatedCborView<'a> {
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsThreadItemNoUnauthenticatedCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(
        &self,
    ) -> Result<UnspeccedDefsThreadItemNoUnauthenticated, crate::cbor::CborError> {
        Ok(UnspeccedDefsThreadItemNoUnauthenticated {
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsThreadItemNotFound object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsThreadItemNotFound {
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsThreadItemNotFound {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let count = 0u64;
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsThreadItemNotFound {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsThreadItemNotFound {
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsThreadItemNotFoundCborView<'a> {
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsThreadItemNotFoundCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsThreadItemNotFound, crate::cbor::CborError> {
        Ok(UnspeccedDefsThreadItemNotFound {
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsThreadItemPost object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsThreadItemPost {
    /// The threadgate created by the author indicates this post as a reply to be hidden for everyone consuming the thread.
    pub hidden_by_threadgate: bool,
    /// This post has more parents that were not present in the response. This is just a boolean, without the number of parents.
    pub more_parents: bool,
    /// This post has more replies that were not present in the response. This is a numeric value, which is best-effort and might not be accurate.
    pub more_replies: i64,
    /// This is by an account muted by the viewer requesting it.
    pub muted_by_viewer: bool,
    /// This post is part of a contiguous thread by the OP from the thread root. Sub-threads by OP deeper in the tree are not considered an OP thread.
    pub op_thread: bool,
    /// The total number of posts in the contiguous OP thread that this post belongs to. Only present when this post is part of the OP thread (see `opThread`).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub op_thread_post_count: Option<i64>,
    /// The 1-indexed position of this post within the contiguous OP thread. Only present when this post is part of the OP thread (see `opThread`).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub op_thread_post_index: Option<i64>,
    pub post: crate::api::app::bsky::FeedDefsPostView,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsThreadItemPost {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 6u64;
            if self.op_thread_post_count.is_some() {
                count += 1;
            }
            if self.op_thread_post_index.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("post")?;
            self.post.encode_cbor(buf)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("opThread")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.op_thread)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("moreParents")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.more_parents)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("moreReplies")?;
            crate::cbor::Encoder::new(&mut *buf).encode_i64(self.more_replies)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("mutedByViewer")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.muted_by_viewer)?;
            if self.op_thread_post_count.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("opThreadPostCount")?;
                if let Some(ref val) = self.op_thread_post_count {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            if self.op_thread_post_index.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("opThreadPostIndex")?;
                if let Some(ref val) = self.op_thread_post_index {
                    crate::cbor::Encoder::new(&mut *buf).encode_i64(*val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("hiddenByThreadgate")?;
            crate::cbor::Encoder::new(&mut *buf).encode_bool(self.hidden_by_threadgate)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                self.post.encode_cbor(&mut vbuf)?;
                pairs.push(("post", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.op_thread)?;
                pairs.push(("opThread", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.more_parents)?;
                pairs.push(("moreParents", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_i64(self.more_replies)?;
                pairs.push(("moreReplies", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.muted_by_viewer)?;
                pairs.push(("mutedByViewer", vbuf));
            }
            if self.op_thread_post_count.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.op_thread_post_count {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("opThreadPostCount", vbuf));
            }
            if self.op_thread_post_index.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.op_thread_post_index {
                    crate::cbor::Encoder::new(&mut vbuf).encode_i64(*val)?;
                }
                pairs.push(("opThreadPostIndex", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_bool(self.hidden_by_threadgate)?;
                pairs.push(("hiddenByThreadgate", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_post: Option<crate::api::app::bsky::FeedDefsPostView> = None;
        let mut field_op_thread: Option<bool> = None;
        let mut field_more_parents: Option<bool> = None;
        let mut field_more_replies: Option<i64> = None;
        let mut field_muted_by_viewer: Option<bool> = None;
        let mut field_op_thread_post_count: Option<i64> = None;
        let mut field_op_thread_post_index: Option<i64> = None;
        let mut field_hidden_by_threadgate: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "post" => {
                    field_post = Some(crate::api::app::bsky::FeedDefsPostView::decode_cbor(
                        decoder,
                    )?);
                }
                "opThread" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_op_thread = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "moreParents" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_more_parents = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "moreReplies" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_more_replies = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_more_replies = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "mutedByViewer" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_by_viewer = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "opThreadPostCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_op_thread_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_op_thread_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "opThreadPostIndex" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_op_thread_post_index = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_op_thread_post_index = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "hiddenByThreadgate" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hidden_by_threadgate = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsThreadItemPost {
            post: field_post.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'post'".into())
            })?,
            op_thread: field_op_thread.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'opThread'".into())
            })?,
            more_parents: field_more_parents.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreParents'".into())
            })?,
            more_replies: field_more_replies.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreReplies'".into())
            })?,
            muted_by_viewer: field_muted_by_viewer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'mutedByViewer'".into())
            })?,
            op_thread_post_count: field_op_thread_post_count,
            op_thread_post_index: field_op_thread_post_index,
            hidden_by_threadgate: field_hidden_by_threadgate.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'hiddenByThreadgate'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_post: Option<crate::api::app::bsky::FeedDefsPostView> = None;
        let mut field_op_thread: Option<bool> = None;
        let mut field_more_parents: Option<bool> = None;
        let mut field_more_replies: Option<i64> = None;
        let mut field_muted_by_viewer: Option<bool> = None;
        let mut field_op_thread_post_count: Option<i64> = None;
        let mut field_op_thread_post_index: Option<i64> = None;
        let mut field_hidden_by_threadgate: Option<bool> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "post" => {
                    field_post = Some(crate::api::app::bsky::FeedDefsPostView::from_cbor_value(
                        value,
                    )?);
                }
                "opThread" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_op_thread = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "moreParents" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_more_parents = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "moreReplies" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_more_replies = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_more_replies = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "mutedByViewer" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_by_viewer = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                "opThreadPostCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_op_thread_post_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_op_thread_post_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "opThreadPostIndex" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_op_thread_post_index = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_op_thread_post_index = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "hiddenByThreadgate" => {
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hidden_by_threadgate = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsThreadItemPost {
            post: field_post.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'post'".into())
            })?,
            op_thread: field_op_thread.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'opThread'".into())
            })?,
            more_parents: field_more_parents.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreParents'".into())
            })?,
            more_replies: field_more_replies.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreReplies'".into())
            })?,
            muted_by_viewer: field_muted_by_viewer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'mutedByViewer'".into())
            })?,
            op_thread_post_count: field_op_thread_post_count,
            op_thread_post_index: field_op_thread_post_index,
            hidden_by_threadgate: field_hidden_by_threadgate.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'hiddenByThreadgate'".into(),
                )
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsThreadItemPostCborView<'a> {
    pub post: crate::api::app::bsky::FeedDefsPostViewCborView<'a>,
    pub op_thread: bool,
    pub more_parents: bool,
    pub more_replies: i64,
    pub muted_by_viewer: bool,
    pub op_thread_post_count: Option<i64>,
    pub op_thread_post_index: Option<i64>,
    pub hidden_by_threadgate: bool,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsThreadItemPostCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsThreadItemPost, crate::cbor::CborError> {
        Ok(UnspeccedDefsThreadItemPost {
            post: self.post.to_owned()?,
            op_thread: self.op_thread,
            more_parents: self.more_parents,
            more_replies: self.more_replies,
            muted_by_viewer: self.muted_by_viewer,
            op_thread_post_count: self.op_thread_post_count,
            op_thread_post_index: self.op_thread_post_index,
            hidden_by_threadgate: self.hidden_by_threadgate,
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_post: Option<crate::api::app::bsky::FeedDefsPostViewCborView<'a>> = None;
        let mut field_op_thread: Option<bool> = None;
        let mut field_more_parents: Option<bool> = None;
        let mut field_more_replies: Option<i64> = None;
        let mut field_muted_by_viewer: Option<bool> = None;
        let mut field_op_thread_post_count: Option<i64> = None;
        let mut field_op_thread_post_index: Option<i64> = None;
        let mut field_hidden_by_threadgate: Option<bool> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x70\x6f\x73\x74", |decoder| {
            field_post =
                Some(crate::api::app::bsky::FeedDefsPostViewCborView::decode_cbor(decoder)?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x6f\x70\x54\x68\x72\x65\x61\x64", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Bool(b) = value {
                field_op_thread = Some(b);
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
            }
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x6d\x6f\x72\x65\x50\x61\x72\x65\x6e\x74\x73",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_more_parents = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x6d\x6f\x72\x65\x52\x65\x70\x6c\x69\x65\x73",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_more_replies = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_more_replies = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6d\x6d\x75\x74\x65\x64\x42\x79\x56\x69\x65\x77\x65\x72",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_muted_by_viewer = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x71\x6f\x70\x54\x68\x72\x65\x61\x64\x50\x6f\x73\x74\x43\x6f\x75\x6e\x74",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_op_thread_post_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_op_thread_post_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x71\x6f\x70\x54\x68\x72\x65\x61\x64\x50\x6f\x73\x74\x49\x6e\x64\x65\x78",
            |decoder| {
                let value = decoder.decode()?;
                match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_op_thread_post_index = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_op_thread_post_index = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                }
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x72\x68\x69\x64\x64\x65\x6e\x42\x79\x54\x68\x72\x65\x61\x64\x67\x61\x74\x65",
            |decoder| {
                let value = decoder.decode()?;
                if let crate::cbor::Value::Bool(b) = value {
                    field_hidden_by_threadgate = Some(b);
                } else {
                    return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                }
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"post" => {
                    field_post = Some(
                        crate::api::app::bsky::FeedDefsPostViewCborView::decode_cbor(decoder)?,
                    );
                }
                b"opThread" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_op_thread = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"moreParents" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_more_parents = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"moreReplies" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_more_replies = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_more_replies = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"mutedByViewer" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_muted_by_viewer = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                b"opThreadPostCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_op_thread_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_op_thread_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"opThreadPostIndex" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_op_thread_post_index = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_op_thread_post_index = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"hiddenByThreadgate" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Bool(b) = value {
                        field_hidden_by_threadgate = Some(b);
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected bool".into()));
                    }
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            post: field_post.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'post'".into())
            })?,
            op_thread: field_op_thread.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'opThread'".into())
            })?,
            more_parents: field_more_parents.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreParents'".into())
            })?,
            more_replies: field_more_replies.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'moreReplies'".into())
            })?,
            muted_by_viewer: field_muted_by_viewer.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'mutedByViewer'".into())
            })?,
            op_thread_post_count: field_op_thread_post_count,
            op_thread_post_index: field_op_thread_post_index,
            hidden_by_threadgate: field_hidden_by_threadgate.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor(
                    "missing required field 'hiddenByThreadgate'".into(),
                )
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsTrendView object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsTrendView {
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub actors: Vec<crate::api::app::bsky::ActorDefsProfileViewBasic>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub category: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    pub display_name: String,
    pub link: String,
    pub post_count: i64,
    pub started_at: crate::syntax::Datetime,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    pub topic: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsTrendView {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 6u64;
            if self.status.is_some() {
                count += 1;
            }
            if self.category.is_some() {
                count += 1;
            }
            if self.description.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("link")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.link)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("topic")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.topic)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("actors")?;
            crate::cbor::Encoder::new(&mut *buf).encode_array_header(self.actors.len() as u64)?;
            for item in &self.actors {
                item.encode_cbor(buf)?;
            }
            if self.status.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("status")?;
                if let Some(ref val) = self.status {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.category.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("category")?;
                if let Some(ref val) = self.category {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("postCount")?;
            crate::cbor::Encoder::new(&mut *buf).encode_i64(self.post_count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("startedAt")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(self.started_at.as_str())?;
            if self.description.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.display_name)?;
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.link)?;
                pairs.push(("link", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.topic)?;
                pairs.push(("topic", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf)
                    .encode_array_header(self.actors.len() as u64)?;
                for item in &self.actors {
                    item.encode_cbor(&mut vbuf)?;
                }
                pairs.push(("actors", vbuf));
            }
            if self.status.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.status {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("status", vbuf));
            }
            if self.category.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.category {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("category", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_i64(self.post_count)?;
                pairs.push(("postCount", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(self.started_at.as_str())?;
                pairs.push(("startedAt", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.display_name)?;
                pairs.push(("displayName", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_actors: Vec<crate::api::app::bsky::ActorDefsProfileViewBasic> = Vec::new();
        let mut field_status: Option<String> = None;
        let mut field_category: Option<String> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::Datetime> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "link" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "actors" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_actors.push(
                                crate::api::app::bsky::ActorDefsProfileViewBasic::from_cbor_value(
                                    item,
                                )?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "category" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_category = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "postCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                "startedAt" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_started_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsTrendView {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            actors: field_actors,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_actors: Vec<crate::api::app::bsky::ActorDefsProfileViewBasic> = Vec::new();
        let mut field_status: Option<String> = None;
        let mut field_category: Option<String> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::Datetime> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "link" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "actors" => {
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_actors.push(
                                crate::api::app::bsky::ActorDefsProfileViewBasic::from_cbor_value(
                                    item,
                                )?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                "status" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_status = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "category" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_category = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "postCount" => match value {
                    crate::cbor::Value::Unsigned(n) => {
                        field_post_count = Some(i64::try_from(n).map_err(|_| {
                            crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                        })?);
                    }
                    crate::cbor::Value::Signed(n) => {
                        field_post_count = Some(n);
                    }
                    _ => {
                        return Err(crate::cbor::CborError::InvalidCbor(
                            "expected integer".into(),
                        ));
                    }
                },
                "startedAt" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_started_at = Some(
                            crate::syntax::Datetime::try_from(s)
                                .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                        );
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsTrendView {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            actors: field_actors,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsTrendViewCborView<'a> {
    pub link: &'a str,
    pub topic: &'a str,
    pub actors: Vec<crate::api::app::bsky::ActorDefsProfileViewBasic>,
    pub status: Option<&'a str>,
    pub category: Option<&'a str>,
    pub post_count: i64,
    pub started_at: crate::syntax::DatetimeRef<'a>,
    pub description: Option<&'a str>,
    pub display_name: &'a str,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsTrendViewCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsTrendView, crate::cbor::CborError> {
        Ok(UnspeccedDefsTrendView {
            link: self.link.to_owned(),
            topic: self.topic.to_owned(),
            actors: self.actors.clone(),
            status: self.status.as_ref().map(|value| (*value).to_owned()),
            category: self.category.as_ref().map(|value| (*value).to_owned()),
            post_count: self.post_count,
            started_at: self.started_at.to_owned(),
            description: self.description.as_ref().map(|value| (*value).to_owned()),
            display_name: self.display_name.to_owned(),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_link: Option<&'a str> = None;
        let mut field_topic: Option<&'a str> = None;
        let mut field_actors: Vec<crate::api::app::bsky::ActorDefsProfileViewBasic> = Vec::new();
        let mut field_status: Option<&'a str> = None;
        let mut field_category: Option<&'a str> = None;
        let mut field_post_count: Option<i64> = None;
        let mut field_started_at: Option<crate::syntax::DatetimeRef<'a>> = None;
        let mut field_description: Option<&'a str> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x6c\x69\x6e\x6b", |decoder| {
            field_link = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x74\x6f\x70\x69\x63", |decoder| {
            field_topic = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x66\x61\x63\x74\x6f\x72\x73", |decoder| {
            let value = decoder.decode()?;
            if let crate::cbor::Value::Array(items) = value {
                for item in items {
                    field_actors.push(
                        crate::api::app::bsky::ActorDefsProfileViewBasic::from_cbor_value(item)?,
                    );
                }
            } else {
                return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
            }
            Ok(())
        })?;
        entries.try_field(b"\x66\x73\x74\x61\x74\x75\x73", |decoder| {
            field_status = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x68\x63\x61\x74\x65\x67\x6f\x72\x79", |decoder| {
            field_category = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x69\x70\x6f\x73\x74\x43\x6f\x75\x6e\x74", |decoder| {
            let value = decoder.decode()?;
            match value {
                crate::cbor::Value::Unsigned(n) => {
                    field_post_count = Some(i64::try_from(n).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("integer out of i64 range".into())
                    })?);
                }
                crate::cbor::Value::Signed(n) => {
                    field_post_count = Some(n);
                }
                _ => {
                    return Err(crate::cbor::CborError::InvalidCbor(
                        "expected integer".into(),
                    ));
                }
            }
            Ok(())
        })?;
        entries.try_field(b"\x69\x73\x74\x61\x72\x74\x65\x64\x41\x74", |decoder| {
            field_started_at = Some(
                crate::syntax::DatetimeRef::try_from(decoder.text()?)
                    .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
            );
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x65\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_description = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"link" => {
                    field_link = Some(decoder.text()?);
                }
                b"topic" => {
                    field_topic = Some(decoder.text()?);
                }
                b"actors" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Array(items) = value {
                        for item in items {
                            field_actors.push(
                                crate::api::app::bsky::ActorDefsProfileViewBasic::from_cbor_value(
                                    item,
                                )?,
                            );
                        }
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected array".into()));
                    }
                }
                b"status" => {
                    field_status = Some(decoder.text()?);
                }
                b"category" => {
                    field_category = Some(decoder.text()?);
                }
                b"postCount" => {
                    let value = decoder.decode()?;
                    match value {
                        crate::cbor::Value::Unsigned(n) => {
                            field_post_count = Some(i64::try_from(n).map_err(|_| {
                                crate::cbor::CborError::InvalidCbor(
                                    "integer out of i64 range".into(),
                                )
                            })?);
                        }
                        crate::cbor::Value::Signed(n) => {
                            field_post_count = Some(n);
                        }
                        _ => {
                            return Err(crate::cbor::CborError::InvalidCbor(
                                "expected integer".into(),
                            ));
                        }
                    }
                }
                b"startedAt" => {
                    field_started_at = Some(
                        crate::syntax::DatetimeRef::try_from(decoder.text()?)
                            .map_err(|e| crate::cbor::CborError::InvalidCbor(e.to_string()))?,
                    );
                }
                b"description" => {
                    field_description = Some(decoder.text()?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            actors: field_actors,
            status: field_status,
            category: field_category,
            post_count: field_post_count.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'postCount'".into())
            })?,
            started_at: field_started_at.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'startedAt'".into())
            })?,
            description: field_description,
            display_name: field_display_name.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'displayName'".into())
            })?,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}

/// UnspeccedDefsTrendingTopic object from app.bsky.unspecced.defs.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UnspeccedDefsTrendingTopic {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    pub link: String,
    pub topic: String,
    /// Extra fields not defined in the schema (JSON).
    #[serde(flatten)]
    pub extra: std::collections::HashMap<String, serde_json::Value>,
    /// Extra fields not defined in the schema (CBOR).
    #[serde(skip)]
    pub extra_cbor: Vec<(String, Vec<u8>)>,
}

impl UnspeccedDefsTrendingTopic {
    pub fn to_cbor(&self) -> Result<Vec<u8>, crate::cbor::CborError> {
        let mut buf = Vec::new();
        self.encode_cbor(&mut buf)?;
        Ok(buf)
    }

    pub fn encode_cbor(&self, buf: &mut Vec<u8>) -> Result<(), crate::cbor::CborError> {
        if self.extra_cbor.is_empty() {
            // Fast path: no extra fields to merge.
            let mut count = 2u64;
            if self.description.is_some() {
                count += 1;
            }
            if self.display_name.is_some() {
                count += 1;
            }
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(count)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("link")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.link)?;
            crate::cbor::Encoder::new(&mut *buf).encode_text("topic")?;
            crate::cbor::Encoder::new(&mut *buf).encode_text(&self.topic)?;
            if self.description.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("description")?;
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
            if self.display_name.is_some() {
                crate::cbor::Encoder::new(&mut *buf).encode_text("displayName")?;
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut *buf).encode_text(val)?;
                }
            }
        } else {
            // Slow path: merge known fields with extra_cbor, sort, encode.
            let mut pairs: Vec<(&str, Vec<u8>)> = Vec::new();
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.link)?;
                pairs.push(("link", vbuf));
            }
            {
                let mut vbuf = Vec::new();
                crate::cbor::Encoder::new(&mut vbuf).encode_text(&self.topic)?;
                pairs.push(("topic", vbuf));
            }
            if self.description.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.description {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("description", vbuf));
            }
            if self.display_name.is_some() {
                let mut vbuf = Vec::new();
                if let Some(ref val) = self.display_name {
                    crate::cbor::Encoder::new(&mut vbuf).encode_text(val)?;
                }
                pairs.push(("displayName", vbuf));
            }
            for (k, v) in &self.extra_cbor {
                pairs.push((k.as_str(), v.clone()));
            }
            pairs.sort_by(|a, b| crate::cbor::cbor_key_cmp(a.0, b.0));
            crate::cbor::Encoder::new(&mut *buf).encode_map_header(pairs.len() as u64)?;
            for (k, v) in &pairs {
                crate::cbor::Encoder::new(&mut *buf).encode_text(k)?;
                buf.extend_from_slice(v);
            }
        }
        Ok(())
    }

    pub fn from_cbor(data: &[u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }

    pub fn decode_cbor(decoder: &mut crate::cbor::Decoder) -> Result<Self, crate::cbor::CborError> {
        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        let mut entries = decoder.map_entries()?;
        while let Some(result) = entries.next_with(|key, decoder| {
            match key {
                "link" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    let value = decoder.decode()?;
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let value = decoder.decode()?;
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
            Ok(())
        }) {
            result?;
        }

        Ok(UnspeccedDefsTrendingTopic {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            description: field_description,
            display_name: field_display_name,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }

    #[allow(dead_code)]
    pub(crate) fn from_cbor_value(
        val: crate::cbor::Value<'_>,
    ) -> Result<Self, crate::cbor::CborError> {
        let entries = match val {
            crate::cbor::Value::Map(entries) => entries,
            _ => return Err(crate::cbor::CborError::InvalidCbor("expected map".into())),
        };

        let mut field_link: Option<String> = None;
        let mut field_topic: Option<String> = None;
        let mut field_description: Option<String> = None;
        let mut field_display_name: Option<String> = None;
        let mut extra_cbor: Vec<(String, Vec<u8>)> = Vec::new();

        for (key, value) in entries {
            match key {
                "link" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_link = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "topic" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_topic = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "description" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_description = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                "displayName" => {
                    if let crate::cbor::Value::Text(s) = value {
                        field_display_name = Some(s.to_string());
                    } else {
                        return Err(crate::cbor::CborError::InvalidCbor("expected text".into()));
                    }
                }
                _ => {
                    let raw = crate::cbor::encode_value(&value)?;
                    extra_cbor.push((key.to_string(), raw));
                }
            }
        }

        Ok(UnspeccedDefsTrendingTopic {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            description: field_description,
            display_name: field_display_name,
            extra: std::collections::HashMap::new(),
            extra_cbor,
        })
    }
}

/// A validated CBOR view. Borrowed fields cannot outlive the input.
#[derive(Debug)]
pub struct UnspeccedDefsTrendingTopicCborView<'a> {
    pub link: &'a str,
    pub topic: &'a str,
    pub description: Option<&'a str>,
    pub display_name: Option<&'a str>,
    pub extra_cbor: Vec<(&'a str, &'a [u8])>,
    raw_cbor: &'a [u8],
}
impl<'a> UnspeccedDefsTrendingTopicCborView<'a> {
    #[inline]
    pub fn from_cbor(data: &'a [u8]) -> Result<Self, crate::cbor::CborError> {
        let mut decoder = crate::cbor::Decoder::new(data);
        let result = Self::decode_cbor(&mut decoder)?;
        if !decoder.is_empty() {
            return Err(crate::cbor::CborError::InvalidCbor("trailing data".into()));
        }
        Ok(result)
    }
    pub fn to_owned(&self) -> Result<UnspeccedDefsTrendingTopic, crate::cbor::CborError> {
        Ok(UnspeccedDefsTrendingTopic {
            link: self.link.to_owned(),
            topic: self.topic.to_owned(),
            description: self.description.as_ref().map(|value| (*value).to_owned()),
            display_name: self.display_name.as_ref().map(|value| (*value).to_owned()),
            extra: std::collections::HashMap::new(),
            extra_cbor: self
                .extra_cbor
                .iter()
                .map(|(key, value)| ((*key).to_owned(), value.to_vec()))
                .collect(),
        })
    }
    /// The original input, unaffected by changes to public view fields.
    pub fn original_cbor(&self) -> &'a [u8] {
        self.raw_cbor
    }
    #[inline]
    pub fn decode_cbor(
        decoder: &mut crate::cbor::Decoder<'a>,
    ) -> Result<Self, crate::cbor::CborError> {
        let start = decoder.position();
        let mut field_link: Option<&'a str> = None;
        let mut field_topic: Option<&'a str> = None;
        let mut field_description: Option<&'a str> = None;
        let mut field_display_name: Option<&'a str> = None;
        let mut extra_cbor = Vec::new();
        let mut entries = decoder.map_entries()?;
        entries.try_field(b"\x64\x6c\x69\x6e\x6b", |decoder| {
            field_link = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(b"\x65\x74\x6f\x70\x69\x63", |decoder| {
            field_topic = Some(decoder.text()?);
            Ok(())
        })?;
        entries.try_field(
            b"\x6b\x64\x65\x73\x63\x72\x69\x70\x74\x69\x6f\x6e",
            |decoder| {
                field_description = Some(decoder.text()?);
                Ok(())
            },
        )?;
        entries.try_field(
            b"\x6b\x64\x69\x73\x70\x6c\x61\x79\x4e\x61\x6d\x65",
            |decoder| {
                field_display_name = Some(decoder.text()?);
                Ok(())
            },
        )?;
        while let Some(result) = entries.next_raw(|key, decoder| {
            match key {
                b"link" => {
                    field_link = Some(decoder.text()?);
                }
                b"topic" => {
                    field_topic = Some(decoder.text()?);
                }
                b"description" => {
                    field_description = Some(decoder.text()?);
                }
                b"displayName" => {
                    field_display_name = Some(decoder.text()?);
                }
                _ => {
                    let key = core::str::from_utf8(key).map_err(|_| {
                        crate::cbor::CborError::InvalidCbor("invalid UTF-8 in text string".into())
                    })?;
                    let start = decoder.position();
                    let _ = decoder.decode()?;
                    extra_cbor.push((key, &decoder.raw_input()[start..decoder.position()]));
                }
            }
            Ok(())
        }) {
            result?;
        }
        drop(entries);
        Ok(Self {
            link: field_link.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'link'".into())
            })?,
            topic: field_topic.ok_or_else(|| {
                crate::cbor::CborError::InvalidCbor("missing required field 'topic'".into())
            })?,
            description: field_description,
            display_name: field_display_name,
            extra_cbor,
            raw_cbor: &decoder.raw_input()[start..decoder.position()],
        })
    }
}
